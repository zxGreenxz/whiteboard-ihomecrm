# E2E Copilot — 4 spec chạy trên trình duyệt thật, KHÔNG chạy theo mỗi PR.
#
# VÌ SAO TÁCH RA KHỎI ci-gates.yml
#   Job quality-gates cố ý KHÔNG cần secret nào: nhờ vậy nó xanh trên mọi PR, kể
#   cả PR từ fork. Bốn spec dưới đây cần mật khẩu tài khoản DEMO và một URL trỏ tới
#   bản build đang chạy — gộp vào đó là biến cửa bắt buộc thành cửa đỏ-vì-thiếu-
#   credential, và một cửa đỏ thường trực là cửa không ai còn đọc.
#
# SECRET PHẢI ĐƯỢC NẠP TAY (Settings → Secrets and variables → Actions)
#   - FLEET_BASE_URL       : URL bản dựng ĐANG chạy để thử. KHÔNG được là
#                            https://ptcrm.vercel.app — hai spec ghi dữ liệu gọi
#                            `chanChayTrenProduction()` và sẽ NÉM nếu thấy
#                            production (xem .e2e-fleet/specs/buildAttestation.ts).
#   - FLEET_PASS_CHUNHA    : mật khẩu demo.chunha@username.ihomecrm.local
#   - FLEET_PASS_KETOAN    : mật khẩu demo.ketoan@username.ihomecrm.local
#   - FLEET_PASS_QUANLY    : mật khẩu demo.quanly@username.ihomecrm.local
#   - FLEET_PASS_QUANLY2   : mật khẩu demo.quanly2@username.ihomecrm.local. Khoá
#                            `quanly2` đã có trong .e2e-fleet/specs/auth.ts từ
#                            03/09/2026 (G2-F) — ma trận hành động cần một danh
#                            tính DEMO thứ hai để thử "nonce của người này người
#                            kia không dùng được".
#   - FLEET_PASS_SYSADMIN  : mật khẩu tài khoản HỆ THỐNG nguyentamca165@gmail.com
#                            (THÊM 03/09/2026, G2-F). Đây KHÔNG phải tài khoản
#                            DEMO. Nó có mặt vì đúng MỘT việc mà không vai nào
#                            khác làm được: `set_copilot_feature_flag_v2` đòi
#                            `is_super_admin()`, và hai ca kill switch phải lật
#                            cờ rollout thật. Đo 03/09/2026: tài khoản này KHÔNG
#                            có `income_expenses.edit` trên org DEMO, nên nó
#                            không ghi được gì qua đường Copilot — ma trận dùng
#                            chính điều đó làm ca "thiếu quyền".
#   - VERCEL_AUTOMATION_BYPASS_SECRET : mật khẩu mở khoá bản preview — xem khối
#                            "PREVIEW BỊ KHOÁ" ngay dưới.
#   Thiếu secret nào thì spec NÉM với thông báo chỉ đúng tên biến, không fail mơ hồ.
#
# MA TRẬN HÀNH ĐỘNG LẬT CỜ ROLLOUT THẬT — ĐỌC TRƯỚC KHI SỬA
#   `copilot-action-matrix.spec.ts` tắt cờ `action:income_expense.create_draft`
#   khoảng một giây trong hai ca kill switch rồi TỰ bật lại (`disabled → shadow →
#   enabled`, và `afterAll` bật lại lần nữa cho chắc). Cờ đó được chọn vì nó đang
#   `enabled` với canary NULL + hạn NULL, nên hoàn nguyên là CHÍNH XÁC từng cột;
#   lật cờ `income_expense.annotate` (canary DEMO + có hạn) sẽ nới nó thành
#   bật-toàn-cục-vĩnh-viễn vì `set_copilot_feature_flag_v2` ép hai cột đó về NULL
#   khi chuyển sang `disabled` và không role nào đọc lại được giá trị cũ.
#   Trạng thái xấu nhất nếu job chết giữa chừng là `disabled` — fail-CLOSED, nhìn
#   thấy được ở /settings/ai-copilot → tab Rollout.
#
# PREVIEW BỊ KHOÁ — VÌ SAO PHẢI GỬI HEADER BYPASS
#   FLEET_BASE_URL hiện trỏ vào alias preview
#   (https://ihomecrm-git-main-zxgreenxzs-projects.vercel.app), và deployment
#   preview của Vercel bật **Vercel Authentication**: mọi request không mang
#   chứng chỉ đều nhận về một trang đăng nhập SSO — kể cả `curl`, kể cả trình
#   duyệt Playwright. Hệ quả nếu thiếu header: bước đọc SHA nhận trang đăng nhập
#   (không có thẻ <meta name="build-sha">) và cả suite chết vì một lý do chẳng
#   liên quan gì tới Copilot — đúng kiểu đỏ thường trực khiến người ta ngừng đọc CI.
#
#   Cách mở đúng của Vercel là "Protection Bypass for Automation": một giá trị bí
#   mật gửi qua header `x-vercel-protection-bypass`. Nó phải đi ở CẢ HAI nơi —
#   `curl` của bước đọc SHA (header dựng ngay trong bước đó), và phía trình duyệt
#   (.e2e-fleet/global-setup.ts). Thiếu một chỗ là hỏng một nửa: hoặc không đọc
#   được SHA, hoặc đọc được rồi mà trình duyệt vẫn chỉ thấy trang đăng nhập.
#
#   Phía trình duyệt KHÔNG dùng `use.extraHTTPHeaders` (bản 02/09/2026 đã dùng và
#   sai): Playwright gắn header đó vào MỌI request của context, kể cả `fetch` của
#   app sang `https://<ref>.supabase.co/functions/v1/llm-proxy` — origin đó
#   preflight và từ chối `x-vercel-set-bypass-cookie`
#   ("not allowed by Access-Control-Allow-Headers"), làm 3/7 test Copilot đỏ vì
#   một lý do chẳng liên quan gì tới Copilot. Cách đang dùng: `globalSetup` đưa
#   chìa ĐÚNG MỘT LẦN tới origin preview, nhận cookie `_vercel_jwt` (gắn với host
#   preview), lưu vào storageState. Cookie theo luật same-origin nên không bao giờ
#   rời host preview.
#
#   Giá trị này là MẬT KHẨU của bản preview (ai có nó thì mở được mọi deployment
#   preview của project), nên nó chịu ĐÚNG bộ lọc che và ĐÚNG guard như
#   FLEET_PASS_* — xem bước chạy test và bước guard bên dưới.
#
# PHẠM VI GHI: CHỈ ORG DEMO (Contract §8)
#   Mọi spec đăng nhập bằng tài khoản demo.* thuộc org DEMO (dddd…0001) và chỉ
#   ghi trong phạm vi đó. `COPILOT_DRAFT_MATRIX_LIVE=1` mở phần ma trận nháp có
#   GHI (preview nháp chưa duyệt) — vẫn trong org DEMO.
#
#   Ngoại lệ DUY NHẤT về tài khoản là `FLEET_PASS_SYSADMIN` (tài khoản hệ thống,
#   không thuộc công ty nào), và nó chỉ được dùng để lật cờ rollout — nó KHÔNG
#   ghi được dữ liệu nghiệp vụ nào vì thiếu quyền, xem khối secret ở trên.
#   `copilot-action-matrix.spec.ts` ghi vào ĐÚNG ba phiếu nháp DEMO có tên bắt
#   đầu bằng "E2E G2-F", tra theo tên trước khi tạo nên chạy 100 lượt vẫn đúng
#   ba phiếu — nó không làm DEMO phình thêm một hàng nào mỗi đêm.
#
# BẢN NÀO ĐANG ĐƯỢC THỬ — VÌ SAO **KHÔNG** DÙNG `github.sha`
#   Mọi spec Copilot gọi `xacMinhBanBuild()`: nó so `EXPECTED_SOURCE_SHA` với thẻ
#   <meta name="build-sha"> của trang (index.html:12 ← VITE_BUILD_SHA, tức commit
#   mà Vercel đã DỰNG). `github.sha` là HEAD của nhánh lúc workflow chạy — hai số
#   này chỉ trùng khi nhánh không nhích kể từ lần deploy gần nhất. Bản đầu
#   (02/09/2026) đặt thẳng `github.sha`, và như vậy lượt cron ĐỎ THEO THIẾT KẾ:
#   mỗi commit mới vào main làm lượt đêm hôm đó thất bại vì lý do chẳng liên quan
#   gì tới Copilot — đúng kiểu đỏ thường trực khiến người ta ngừng đọc CI.
#
#   Cách đúng là hỏi chính bản đang chạy xem nó là ai, rồi kiểm SHA đó có thuộc
#   repo này không. Hai việc, hai lý do:
#     - đọc thẻ meta ⇒ so đúng thứ đang phục vụ người dùng, không phải thứ ta đoán;
#     - `git cat-file -e` ⇒ một FLEET_BASE_URL trỏ nhầm sang deployment của repo
#       KHÁC (hoặc một bản dựng từ lịch sử đã bị viết lại) bị chặn NGAY, thay vì
#       chạy trót lọt rồi báo cáo về một hệ thống khác. Thiếu dữ liệu là THẤT BẠI
#       chứ không phải "bỏ qua" — cùng luật với buildAttestation.ts.
#   Chạy tay có thể ép bằng input `expected_sha` (khi đang thử một preview cụ
#   thể); để trống thì tự dò.

name: Copilot E2E

on:
  workflow_dispatch:
    inputs:
      expected_sha:
        description: 'SHA 40 hex của bản build đang chạy ở FLEET_BASE_URL. Để trống = tự đọc thẻ <meta name="build-sha"> của trang.'
        required: false
        type: string
  schedule:
    # 19:00 UTC = 02:00 giờ Việt Nam (UTC+7) — ngoài giờ làm việc.
    - cron: '0 19 * * *'

permissions:
  contents: read

concurrency:
  group: copilot-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  copilot-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      # fetch-depth: 0 — bước xác minh phía dưới cần TOÀN BỘ lịch sử để trả lời
      # được "SHA của bản deploy có thuộc repo này không". Với clone nông thì một
      # commit hợp lệ nhưng cũ vẫn trượt thành "không thuộc repo": phép kiểm báo
      # động giả, và một phép kiểm báo động giả sẽ bị tắt.
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Setup Node 24.18.0
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '24.18.0'
          cache: 'npm'

      - name: Cài dependency (npm ci — khớp package-lock.json)
        run: npm ci

      # .e2e-fleet KHÔNG có package.json riêng: nó dùng @playwright/test của root
      # (1.61.1). Chỉ cài chromium — ba spec này không chạy firefox/webkit.
      - name: Cài Chromium cho Playwright
        run: npx playwright install --with-deps chromium

      - name: Xác định SHA của bản đang chạy ở FLEET_BASE_URL
        env:
          FLEET_BASE_URL: ${{ secrets.FLEET_BASE_URL }}
          SHA_NHAP: ${{ inputs.expected_sha }}
          # Preview bật Vercel Authentication: không có header dựng từ biến này
          # thì curl nhận trang đăng nhập SSO chứ không nhận index.html.
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          set -euo pipefail
          if [ -z "${FLEET_BASE_URL:-}" ]; then
            echo "::error::Thieu secret FLEET_BASE_URL — khong biet dang thu ban nao."
            exit 1
          fi
          if [ -n "${SHA_NHAP:-}" ]; then
            sha="$SHA_NHAP"
            nguon="input expected_sha (chay tay)"
          else
            nguon="the meta build-sha cua $FLEET_BASE_URL"
            # Header bypass gui KE CA khi bien rong: mot ban khong bat bao ve se
            # bo qua header la, con mot ban CO bat ma thieu secret thi that bai
            # ngay duoi voi thong bao noi dung nguyen nhan.
            if ! trang=$(curl -fsSL --max-time 60 \
                  -H "x-vercel-protection-bypass: ${VERCEL_AUTOMATION_BYPASS_SECRET:-}" \
                  "$FLEET_BASE_URL/"); then
              echo "::error::curl that bai khi tai $FLEET_BASE_URL/. Neu day la preview co Vercel Authentication thi kiem secret VERCEL_AUTOMATION_BYPASS_SECRET."
              exit 1
            fi
            sha=$(printf '%s' "$trang" | grep -o 'build-sha"[[:space:]]*content="[0-9a-f]\{40\}' | head -1 | grep -o '[0-9a-f]\{40\}' || true)
          fi
          if ! printf '%s' "$sha" | grep -qE '^[0-9a-f]{40}$'; then
            echo "::error::Khong lay duoc build SHA 40 hex (nguon: $nguon). Hai kha nang: (1) ban dang chay KHONG khai minh la ai — thieu VITE_BUILD_SHA/VERCEL_GIT_COMMIT_SHA; (2) request bi Vercel Authentication chan nen tra ve trang dang nhap thay vi index.html — kiem secret VERCEL_AUTOMATION_BYPASS_SECRET."
            exit 1
          fi
          if ! git cat-file -e "$sha^{commit}" 2>/dev/null; then
            echo "::error::Ban deploy tai $FLEET_BASE_URL dung tu commit $sha, commit nay KHONG thuoc lich su repo nay. FLEET_BASE_URL dang tro nham he thong khac."
            exit 1
          fi
          echo "EXPECTED_SOURCE_SHA=$sha" >> "$GITHUB_ENV"
          echo "Ban dang chay: $sha (nguon: $nguon)"

      # KHÔNG sinh HTML report trên CI — `--reporter=list`, không `html`.
      #
      # ĐO 02/09/2026, Playwright 1.61.1, với `trace: 'off'` + `screenshot: 'off'`:
      #   (1) `playwright-report/data/<hash>.md` (attachment "error-context.md")
      #       chứa ARIA snapshot của trang, trong đó giá trị ô `type="password"`
      #       nằm NGUYÊN VĂN: `- textbox "Mat khau": SUPERSECRETPW123`. Sinh ra
      #       mỗi khi một test đỏ, không phụ thuộc cài đặt trace/screenshot.
      #   (2) Nặng hơn: `index.html` NHÚNG report JSON dưới dạng
      #       `data:application/zip;base64,…` (deflate). Trong đó có tiêu đề bước
      #       `"title":"Fill \"<giá trị>\" locator(...)"` — tức mật khẩu có mặt ở
      #       MỌI lượt chạy gọi `fill()`, kể cả lượt XANH, chứ không riêng lượt đỏ.
      #       Vì đã nén, `grep -rF "$mat_khau"` trên thư mục report luôn báo SẠCH.
      #       Một guard chỉ dựa vào grep sẽ tự tin gật đầu rồi tải mật khẩu lên.
      # Kết luận: dọn `data/` là CẦN nhưng KHÔNG ĐỦ; chừng nào mật khẩu còn đi qua
      # `fill()` thì report HTML không thể chứng minh là sạch. Nên CI không sinh nó.
      #
      # Đầu ra duy nhất được giữ là stdout của reporter `list`, và nó CŨNG KHÔNG
      # sạch sẵn — đây là đính chính lời khai sai của chính file này: bản 02/09/2026
      # trước đó ghi "call log chỉ có selector và thông báo lỗi".
      #   `_fill()` ghi THẲNG giá trị vào call log — playwright-core/lib/coreBundle.js
      #   dòng 19648: progress.log(  fill("<giá trị>") ). `formatCallLog` nối dòng đó
      #   vào thông báo lỗi và reporter `list` in ra nguyên văn.
      #   Xảy ra khi locator PHÂN GIẢI ĐƯỢC rồi `fill` mới timeout — đúng cảnh ô mật
      #   khẩu bị disabled/che ở auth.ts:52. Đã tái hiện 02/09/2026: dòng 13 của log
      #   chứa `- fill("Fl33t!…")`.
      # Nên có HAI lớp, không một:
      #   - LỚP ĐẦU (lọc che): đặt trên đường ống TRƯỚC `tee`, thay mọi giá trị
      #     `FLEET_PASS_*` bằng `***`. Thứ nằm trên đĩa sạch ngay từ đầu, nên artifact
      #     vẫn dùng được để đọc lỗi.
      #   - LƯỚI (bước guard bên dưới): vẫn grep nguyên văn từng mật khẩu. Lọc che có
      #     thể sót (giá trị quá ngắn, một đường ghi khác) — guard mới là thứ CHẶN
      #     upload.
      # Đừng gỡ lớp nào vì "lớp kia lo rồi": lớp đầu giữ cho log còn đọc được, lưới
      # giữ cho một lần sót không thành một lần rò.
      #
      # `--output` và thư mục log đặt TƯỜNG MINH trong $RUNNER_TEMP. Không phải cho
      # gọn: Playwright phân giải `playwright-report/`/`test-results/` mặc định
      # theo thư mục chứa **package.json gần nhất** (playwright/lib/util.js:243),
      # mà `.e2e-fleet/` KHÔNG có package.json — nên chúng rơi vào GỐC REPO chứ
      # không phải `.e2e-fleet/`. Bản trước tải lên `.e2e-fleet/playwright-report/`
      # và sẽ luôn nhận được thư mục rỗng.
      # BỘ LỌC CHE nằm trong MỘT FILE, không phải hai khối `node -e` chép tay.
      # Từ 03/09/2026 có HAI bước chạy test (lý do ngay dưới), và một bộ lọc bí
      # mật tồn tại hai bản là một bộ lọc sẽ lệch nhau — bản này được sửa, bản kia
      # thì không, rồi một ngày log của bước bị quên mang mật khẩu nguyên văn.
      - name: Sinh bộ lọc che bí mật (dùng chung cho cả hai bước chạy test)
        env:
          LOC_CHE: ${{ runner.temp }}/che-bi-mat.mjs
        run: |
          set -euo pipefail
          cat > "$LOC_CHE" <<'HET_LOC'
          // Lọc che: thay mọi giá trị BÍ MẬT bằng ***, xử theo TỪNG DÒNG.
          // Danh sách gồm FLEET_PASS_* VÀ VERCEL_AUTOMATION_BYPASS_SECRET: cái
          // sau cũng là mật khẩu (ai có nó thì mở được mọi deployment preview),
          // nên nó phải chịu đúng bộ lọc và đúng guard như mật khẩu DEMO chứ
          // không phải "chỉ là một header".
          // Dùng split/join chứ KHÔNG regex: mật khẩu chứa `!`, `_`, `-` và quên
          // escape một metachar là lọc âm thầm sai. So khớp chuỗi thuần thì không
          // có metachar nào để quên. Dài trước ngắn sau, để một mật khẩu là tiền
          // tố của mật khẩu khác vẫn bị che trọn.
          // Bỏ qua giá trị dưới 8 ký tự: một biến đặt nhầm thành "a" sẽ biến cả
          // log thành ***. Trường hợp đó guard vẫn là lưới.
          const NL = String.fromCharCode(10);
          const LA_BI_MAT = /^(?:FLEET_PASS_|VERCEL_AUTOMATION_BYPASS_SECRET$)/;
          const bi = Object.keys(process.env)
            .filter((k) => LA_BI_MAT.test(k))
            .map((k) => process.env[k])
            .filter((v) => v && v.length >= 8)
            .sort((a, b) => b.length - a.length);
          const che = (s) => { for (const v of bi) s = s.split(v).join("***"); return s; };
          let du = "";
          process.stdin.setEncoding("utf8");
          process.stdin.on("data", (c) => {
            du += c;
            const dong = du.split(NL);
            du = dong.pop();
            for (const d of dong) process.stdout.write(che(d) + NL);
          });
          process.stdin.on("end", () => { if (du) process.stdout.write(che(du) + NL); });
          HET_LOC
          node --check "$LOC_CHE"
          echo "Da sinh bo loc che: $LOC_CHE"

      - name: 3 spec Copilot (readonly smoke · xác nhận · ma trận nháp)
        env:
          FLEET_BASE_URL: ${{ secrets.FLEET_BASE_URL }}
          FLEET_PASS_CHUNHA: ${{ secrets.FLEET_PASS_CHUNHA }}
          FLEET_PASS_KETOAN: ${{ secrets.FLEET_PASS_KETOAN }}
          FLEET_PASS_QUANLY: ${{ secrets.FLEET_PASS_QUANLY }}
          FLEET_PASS_QUANLY2: ${{ secrets.FLEET_PASS_QUANLY2 }}
          # .e2e-fleet/global-setup.ts đọc biến này, đổi lấy cookie `_vercel_jwt`
          # một lần rồi cả suite chạy bằng cookie (storageState). Không có nó thì
          # Playwright chỉ thấy trang đăng nhập SSO của Vercel, không thấy app.
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
          # 2 worker: đủ song song để suite không lê thê, đủ ít để 3 tài khoản
          # DEMO không tự đá nhau khi cùng ghi nháp trong một org.
          FLEET_WORKERS: '2'
          # Mở 2 test ghi nháp trong copilot-draft-matrix.spec.ts. Cờ này nằm
          # trong spec (`test.skip` khi khác '1') — KHÔNG gỡ cờ, chỉ đặt env.
          COPILOT_DRAFT_MATRIX_LIVE: '1'
          # EXPECTED_SOURCE_SHA đến từ $GITHUB_ENV của bước trên. Cố ý KHÔNG khai
          # lại ở đây: env cấp-bước sẽ ĐÈ giá trị đó và ta quay về đúng lỗi cũ.
          THU_MUC_TAI_LEN: ${{ runner.temp }}/copilot-e2e
          THU_MUC_RAC: ${{ runner.temp }}/copilot-e2e-artifacts
          LOC_CHE: ${{ runner.temp }}/che-bi-mat.mjs
        run: |
          set -uo pipefail
          mkdir -p "$THU_MUC_TAI_LEN"
          cd .e2e-fleet
          npx playwright test             specs/copilot-readonly-smoke.spec.ts             specs/copilot-confirmation.spec.ts             specs/copilot-draft-matrix.spec.ts             --reporter=list             --trace=off             --output "$THU_MUC_RAC" 2>&1 | node "$LOC_CHE" | tee "$THU_MUC_TAI_LEN/list.log"

      # MA TRẬN HÀNH ĐỘNG CHẠY MỘT MÌNH — ĐÂY LÀ ĐIỀU KIỆN ĐÚNG ĐẮN, KHÔNG PHẢI SỞ THÍCH
      #   Hai ca kill switch của spec đó TẮT cờ `action:income_expense.create_draft`
      #   ở phạm vi TOÀN CỤC khoảng một giây. `copilot-confirmation.spec.ts` ghi
      #   đúng action đó trên đúng org DEMO. Gộp chung một lượt `playwright test`
      #   với `FLEET_WORKERS=2` thì hai chuyện xảy ra, cả hai đều là đỏ-vì-lý-do-bịa:
      #     - thẻ xác nhận của spec kia không bao giờ hiện (server đã từ chối phát
      #       nonce trong đúng cửa sổ một giây đó);
      #     - dòng sổ `action_executed` của spec kia rơi vào giữa hai lượt đếm
      #       `soTruoc`/`soSau` của ma trận.
      #   `test.describe.configure({ mode: 'serial' })` KHÔNG chữa được: nó chỉ xếp
      #   hàng trong MỘT file. Cách chữa là một lượt gọi riêng, `FLEET_WORKERS=1`.
      #   (Spec còn một lớp thứ hai: mọi phép đếm sổ đều lọc theo `action_id` +
      #   `entity_id`. Hai lớp, vì lớp này là quy ước còn lớp kia là phép đo.)
      #
      #   `!cancelled()` chứ không phải mặc định: bước trên đỏ KHÔNG được làm ma
      #   trận không chạy — một hàng rào bảo mật không được đo chỉ vì một smoke
      #   test khác vừa hỏng là đúng thứ ta không muốn.
      - name: Ma trận hành động Copilot (chạy RIÊNG, 1 worker)
        if: ${{ !cancelled() }}
        env:
          FLEET_BASE_URL: ${{ secrets.FLEET_BASE_URL }}
          FLEET_PASS_CHUNHA: ${{ secrets.FLEET_PASS_CHUNHA }}
          FLEET_PASS_KETOAN: ${{ secrets.FLEET_PASS_KETOAN }}
          FLEET_PASS_QUANLY: ${{ secrets.FLEET_PASS_QUANLY }}
          FLEET_PASS_QUANLY2: ${{ secrets.FLEET_PASS_QUANLY2 }}
          FLEET_PASS_SYSADMIN: ${{ secrets.FLEET_PASS_SYSADMIN }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
          FLEET_WORKERS: '1'
          # Mở ca chat "bỏ qua xác nhận" của copilot-action-matrix.spec.ts. Ca đó
          # cần một provider mô hình SỐNG, nên nó có thể đỏ khi 9Router chết —
          # đó là một tín hiệu thật (đường chat không dùng được), không phải
          # nhiễu. Muốn tách nó khỏi cửa thì đặt '0', đừng sửa spec.
          COPILOT_LIVE_MODEL: '1'
          THU_MUC_TAI_LEN: ${{ runner.temp }}/copilot-e2e
          THU_MUC_RAC: ${{ runner.temp }}/copilot-e2e-artifacts
          LOC_CHE: ${{ runner.temp }}/che-bi-mat.mjs
        run: |
          set -uo pipefail
          mkdir -p "$THU_MUC_TAI_LEN"
          cd .e2e-fleet
          npx playwright test             specs/copilot-action-matrix.spec.ts             --reporter=list             --trace=off             --output "$THU_MUC_RAC" 2>&1 | node "$LOC_CHE"             | tee "$THU_MUC_TAI_LEN/list-ma-tran-hanh-dong.log"

      # Dọn TRƯỚC guard, guard TRƯỚC upload. Ba bước tách rời để log nói rõ bước
      # nào làm gì; gộp lại thì một lần `rm` hỏng sẽ ẩn trong bước khác.
      #
      # Lệnh `find … -delete` giữ đúng như review yêu cầu, và chạy trên MỌI vị trí
      # report có thể rơi vào (xem chú thích package.json ở trên). Sau đó xoá hẳn
      # cả report lẫn test-results: không thứ nào trong đó được tải lên, và thứ
      # không tồn tại thì không rò rỉ được.
      - name: Dọn mọi artifact Playwright có thể mang giá trị đã điền
        if: always()
        env:
          THU_MUC_RAC: ${{ runner.temp }}/copilot-e2e-artifacts
        run: |
          set -uo pipefail
          for bc in playwright-report .e2e-fleet/playwright-report; do
            [ -d "$bc/data" ] || continue
            truoc=$(find "$bc/data" -type f | wc -l)
            find "$bc/data" -type f ! -name '*.png' ! -name '*.jpg' ! -name '*.jpeg' ! -name '*.webp' -delete
            echo "Don attachment $bc/data: $truoc file -> $(find "$bc/data" -type f | wc -l)"
          done
          rm -rf playwright-report .e2e-fleet/playwright-report test-results .e2e-fleet/test-results "$THU_MUC_RAC"
          echo "Da xoa report HTML + test-results (khong buoc nao tai chung len)."

      # Guard: thứ SẮP tải lên phải CHỨNG MINH ĐƯỢC là sạch, không chỉ "trông sạch".
      # Hai phép, vì một phép không đủ:
      #   - grep nguyên văn từng mật khẩu ⇒ bắt bản rõ;
      #   - chặn mọi file có `data:application/zip;base64,` ⇒ bắt thứ grep KHÔNG
      #     đọc nổi. Report HTML của Playwright nhúng report JSON đã nén ở đúng
      #     dạng đó, và tiêu đề bước `Fill "<giá trị>"` nằm trong. Cố ý KHÔNG giải
      #     nén để "kiểm cho kỹ": không chứng minh được sạch thì KHÔNG tải lên.
      # `grep -q` nên nội dung khớp không bị in ra log.
      - name: Guard — artifact không được chứa mật khẩu DEMO
        id: guard
        if: always()
        env:
          THU_MUC_TAI_LEN: ${{ runner.temp }}/copilot-e2e
          FLEET_PASS_CHUNHA: ${{ secrets.FLEET_PASS_CHUNHA }}
          FLEET_PASS_KETOAN: ${{ secrets.FLEET_PASS_KETOAN }}
          FLEET_PASS_QUANLY: ${{ secrets.FLEET_PASS_QUANLY }}
          FLEET_PASS_QUANLY2: ${{ secrets.FLEET_PASS_QUANLY2 }}
          FLEET_PASS_SYSADMIN: ${{ secrets.FLEET_PASS_SYSADMIN }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          set -uo pipefail
          if [ ! -d "$THU_MUC_TAI_LEN" ]; then
            echo "Khong co thu muc tai len — khong co gi de canh."
            exit 0
          fi
          loi=0
          dem=0
          dem_pass=0
          # Tên biến lấy ĐỘNG thay vì chép cứng: thêm một giá trị bí mật vào khối
          # `env:` ở trên là guard tự canh luôn. Một danh sách chép tay chính là
          # cách một secret mới lọt lưới mà không ai thấy.
          #
          # VERCEL_AUTOMATION_BYPASS_SECRET nằm trong CÙNG lưới với mật khẩu DEMO:
          # nó mở được MỌI deployment preview của project, nên rò nó ra artifact
          # cũng nghiêm trọng như rò mật khẩu.
          for ten in $(env | cut -d= -f1 | grep -E "^(FLEET_PASS_|VERCEL_AUTOMATION_BYPASS_SECRET$)" | sort -u); do
            mk="${!ten:-}"
            [ -n "$mk" ] || continue
            dem=$((dem + 1))
            case "$ten" in FLEET_PASS_*) dem_pass=$((dem_pass + 1)) ;; esac
            if grep -rqF -- "$mk" "$THU_MUC_TAI_LEN"; then
              echo "::error::artifact chua gia tri bi mat ($ten) — khong upload"
              loi=1
            fi
          done
          if grep -rqF -- 'data:application/zip;base64,' "$THU_MUC_TAI_LEN"; then
            echo "::error::artifact chua report nhung zip (grep khong kiem duoc) — khong upload"
            loi=1
          fi
          if [ "$dem_pass" -eq 0 ]; then
            # Đếm RIÊNG FLEET_PASS_*, không đếm gộp. Nhìn tổng thì một môi trường chỉ
            # có mỗi VERCEL_AUTOMATION_BYPASS_SECRET sẽ cho `dem=1` và guard gật đầu —
            # trong khi bốn mật khẩu DEMO chưa hề được đo. Thêm một secret vào lưới
            # không được phép làm YẾU phép đo đã có.
            #
            # Không thấy biến FLEET_PASS_* nào ⇒ guard KHÔNG ĐO được gì. Đó KHÔNG phải
            # là "sạch", nên thoát khác 0 (fail-closed): upload đòi guard success, nên
            # không có gì rời máy chạy khi phép đo hỏng. Dùng mã 3 để phân biệt với
            # mã 1 "đã đo và BẮT ĐƯỢC mật khẩu" — hai chuyện rất khác nhau khi đọc log.
            echo "KHONG DO DUOC: guard khong thay bien FLEET_PASS_* nao trong moi truong."
            echo "Khong phai da kiem va sach — kiem lai khoi env: cua buoc chay test."
            exit 3
          fi
          if [ "$loi" -ne 0 ]; then
            rm -rf "$THU_MUC_TAI_LEN"
            exit 1
          fi
          echo "Guard: da do $dem gia tri bi mat ($dem_pass mat khau DEMO), khong thay cai nao trong $THU_MUC_TAI_LEN."

      # `!cancelled() && guard == success`: vẫn tải lên khi suite ĐỎ (đó là lúc log
      # có giá trị nhất), nhưng KHÔNG tải khi guard chưa chạy hoặc vừa bắt được rò
      # rỉ. `if: always()` trần sẽ tải lên kể cả sau khi guard đỏ — đúng thứ guard
      # sinh ra để chặn.
      - name: Tải lên log của reporter list (đã qua guard)
        if: ${{ !cancelled() && steps.guard.conclusion == 'success' }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: copilot-e2e-log
          path: ${{ runner.temp }}/copilot-e2e/
          if-no-files-found: warn
          retention-days: 14
