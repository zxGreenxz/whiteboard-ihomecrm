# AUDIT toàn plan AUTHORIZATION-PLAN.md vs runtime thật (2026-07-18)

> Audit đa-agent đối kháng: **103 agent, 117 requirement kiểm, 89 gap xác minh**
> (9 critical / 39 high / 33 medium / 8 low; 49 PARTIAL, 20 DEFECT, 20 MISSING),
> ~4,3M token. Read-only trên production. Đây là báo cáo TRUNG THỰC — không tô hồng.
> Nguồn gap đầy đủ (89 mục + fix_hint): xem tail commit này / journal workflow.

## A. ĐÃ FIX NGAY trong window này (an toàn, additive/reversible, đã verify)

| # | Fix | Artifact | Bằng chứng |
|---|---|---|---|
| A1 | **Revert over-activation** (audit §0/§1.4/§27.4): 10 flag non-payment → OFF + revoke 10 wrapper khỏi authenticated. GIỮ payment ON (đã có W1 canary evidence). Khôi phục kỷ luật §27.7 (không để 10 money-writer callable-toàn-tổ-chức không cap/window). | CAS OFF + revoke | flag: chỉ `invoice.record_payment.v1=ON`; wrapper authenticated còn payment-only |
| A2 | **Definer ACL burn-down** (§1.3/§9.1/§20.12): revoke client EXECUTE khỏi 63 trigger fn (fire như owner, không gãy) + revoke anon khỏi 6 money/admin fn. anon-exec secdef 100→58. | `t8_acl_01_definer_burndown.sql` | trigger-fn-client-exec 63→0; sensitive-anon 6→0; 251 trigger vẫn fire; live payment sau đó OK |
| A3 | **accounts org-boundary** (§14/§20.3 T6a, W1-F4): demo user đọc 37 account org thật → thêm RESTRICTIVE org-boundary. | `t6a_01_accounts_org_boundary.sql` | demo cross-read 37→0; own 3 + real 47 giữ nguyên |
| A4 | **TENANT_OWNER full materialization** (§11 T2, owner parity): role owner chỉ 12/213 perm → owner bị resolver DEFAULT_DENY. Cấp đủ 213. | `t2_04_owner_full_materialization.sql` | owner DEFAULT_DENY→ROLE_ALLOW; shadow mismatch 135→75 |
| A5 | **Audit-log immutability** (§3.4): invoice/income_expense_audit_log GRANT TRUNCATE/DELETE/UPDATE cho client → xoá sạch audit. Revoke + append-only trigger. | `t3_13_audit_log_immutability.sql` | client TRUNCATE/DELETE/UPDATE revoked; INSERT/SELECT giữ |

Money invariant real-org == baseline TUYỆT ĐỐI xuyên suốt (payments 3.886.037.563 /
invoices 4.112.182.330 / IE 10.390.345.149). Live payment smoke sau mọi fix: OK.

## B. Disposition phần còn lại (vì sao KHÔNG fix ngay được — trung thực)

- **Parachute / cần drain per-domain (T7, gate lịch không retention):** revoke 8 legacy
  money writer + table DML (§8.3/§20.5). Cần FE wire canonical + canary từng domain rồi
  mới revoke. Đang an toàn vì UI dùng legacy, money bất biến.
- **Cần frontend-per-domain (không phải SQL):** wire refund/salary/invoice-status/
  invoice-create/meter+invoice/contract renew-transfer-terminate sang canonical writer;
  một số writer (invoice-status machine, meter+invoice atomic, contract renew/transfer/
  terminate) CÒN PHẢI VIẾT. Mỗi domain = 1 window kiểu W1.
- **Storage T8 (§14, cross-tenant PII đọc được — DEFECT sống):** 7 policy PII authenticated-
  wide. Cần `storage_object_links` + backfill + rewrite policy dùng authorize. Build được
  ngay trên staging (không retention) nhưng cutover cần gate. **Đây là exposure đáng ưu tiên
  nhất trong nhóm chưa-đóng-được-ngay.**
- **Non-owner staff parity (T2 Gate G):** 75 shadow mismatch. Cần authoritative mapping
  resource.action→permission_key + materialize staff JSONB→normalized. Parachute che.
- **Bị khoá retention (T9, KHÔNG làm hôm nay):** revoke/drop legacy, xoá nguồn storage cũ,
  §21 P3, independent audit sign-off. Retention ≥90 ngày từ 2026-07-18.

## C. Backlog "fix-được-ngay" còn lại (an toàn, chờ lệnh tiếp) — audit §8

Các mục additive/reversible, không FE, không retention — có thể làm tiếp nếu owner muốn:
1. `approve_voucher` gỡ nhánh self-approve `ie.user_id=auth.uid()` + guard approval_status/amount (LƯU Ý: đổi hành vi duyệt legacy — cần cân nhắc nghiệp vụ).
2. Approval-engine integrity batch (~7 constraint/precedence fix; 0 row vi phạm hiện tại) — làm trước khi engine nhận traffic.
3. `authorization_version` bump trigger (cache invalidation — resolver v3 đã đọc bảng mới).
4. Org NOT NULL + composite UNIQUE(org,id) cho 5 bảng money + `_autofill_org` fail-closed.
5. send-push edge: bỏ decode-JWT-không-verify, so sánh constant-time + `auth.getUser()`.
6. last-owner guard trigger (chặn suspend/revoke owner cuối của org ACTIVE).
7. `create_income_expense_v1` → `authorize_tenant_action_v3` (0 UI caller nên an toàn).
8. Drop orphan `authorize_v2` + 4 orphan `room-sale-images` public policy (kiểm public-intent trước).
9. CI gate wiring 4 harness + mở rộng reconcile-money full-domain + bật track_functions.
10. Membership lifecycle RPC (invite/accept/suspend/revoke/update) — build mới (T6b).

---

# BÁO CÁO ĐẦY ĐỦ TỪ AUDIT (nguyên văn, đối kháng)

# Báo cáo hành động — Authorization Program (audit đối kháng, 2026-07-18)

Tổng hợp từ ~60 requirement đã xác minh đối kháng trên production (read-only). 22 requirement khác đã DONE, không lặp ở đây. Báo cáo trung thực: phần lớn gap KHÔNG phải data-corruption đang xảy ra (money invariant real-org == baseline vẫn khớp nhờ parachute + 0 UI caller ngoài payment), nhưng **chính "0 UI caller" và "self-authorizing wrapper" là boundary mà §27.1/§27.7 CẤM coi là an toàn** — nên đây là bề mặt tấn công thật, không phải trạng thái đạt.

---

## 0. Vấn đề gốc bao trùm (đọc trước tất cả)

Ba sự thật runtime tạo ra hầu hết gap critical, và chúng **mâu thuẫn trực tiếp với chính docs của chương trình**:

1. **11/11 feature flag = ON org-wide**, cap gỡ (`max_operation_count=2147483647`, `max_single_amount_vnd=999999999999`), `ends_at=NULL`, chung một `approval_reference='OWNER-DELEGATED-lam-het-mot-luot'` + `maintenance_window_id=MEGA-20260718`. Đây là 1 uỷ quyền blanket "làm hết một lượt" — đúng thứ §27.1/§27.4/§27.6 cấm (phải per-tranche, có gate review, window hữu hạn, cap thật).
2. **12 canonical wrapper GRANT EXECUTE cho `authenticated`** → callable trực tiếp qua PostgREST bởi mọi user đăng nhập, bất kể UI. Resolver `evaluate_feature_route` gặp `mode=ON` trả `CANONICAL` cho MỌI org → bỏ qua nhánh canary/cap.
3. **Legacy paths chưa revoke** (parachute): `approve_voucher`, `unapprove_voucher`, `record_invoice_payment_v2/v3`, `update_income_expense_quick`, `delete_staff_member`… vẫn `authenticated`/`anon`-callable.

➡️ **Hành động ưu tiên tuyệt đối (owner decision, làm được ngay không chờ retention):** chọn MỘT trong hai để đóng mâu thuẫn "flag ON + wrapper granted + legacy callable" cho 10 domain non-payment:
- **(A) Hạ 10 flag non-payment về OFF/SHADOW** và/hoặc **REVOKE 12 wrapper khỏi `authenticated`** (trả service_role/postgres-only). Giữ payment (đã có W1 canary evidence) nếu owner muốn.
- **(B) Nếu giữ ON:** phải per-tranche re-authorize với SHA/hash/window hữu hạn/cap thật + cross-org REST negative test 2 org thật per-domain.

Tôi khuyến nghị (A): nó reversible, additive-an toàn (không xoá data), và khôi phục ngay boundary §27.7. Chi phí duy nhất là 10 domain vẫn chạy legacy (vốn đang chạy legacy 100% qua UI rồi).

---

## 1. CRITICAL

### 1.1 Legacy financial write path vẫn client-callable + self-approve (§21/2/8, §8.1.1, §8.3, §20.5, §8.2, §20.8)
**(a) Yêu cầu:** Client không tự ghi APPROVED/POSTED/ledger; mọi write qua canonical writer; maker-checker mặc định (creator không tự duyệt); revoke direct DML + legacy definer.

**(b) Chưa đóng:**
- `approve_voucher(uuid)` SECURITY DEFINER, `authenticated`-EXECUTE=t, body vẫn có nhánh `ie.user_id = auth.uid()` → **creator tự duyệt bất kỳ phiếu nào của mình, không hạn mức, không loại force-category** (hoa hồng/refund/lương). Vi phạm P1 maker-checker + §20.8 ngoại lệ đóng.
- 8 legacy money writer (`record_invoice_payment_v3/v2`, `approve_voucher`, `unapprove_voucher`, `update_income_expense_quick`, `restore_income_expense`, `create_commission_voucher`, `pay_utility_bill`) đều secdef + `authenticated`-EXECUTE → post APPROVED/POSTED/ledger trực tiếp, bypass v4.
- Table DML: `authenticated`+`anon` giữ INSERT/UPDATE trên `income_expenses/invoices/payments/accounts/excess_amounts`. Freeze trigger `guard_income_expense_owned_payload` **return new cho row legacy chưa claim** (0/1961 row flow-owned → freeze bảo vệ 0% data thật). `_guard_ie_financial_columns` chặn giả mạo `approved_by`/`posting_id` nhưng **KHÔNG chặn `approval_status` và `amount`** → client set `approval_status='APPROVED'`+`approved_by=self`, sửa `amount` tự do trên row legacy.

**(c) Đóng an toàn (no-delete, forward-fix):**
- **FIX NGAY (forward-fix guard, additive, chỉ siết):** gỡ nhánh `ie.user_id=auth.uid()` trong `approve_voucher` (hoặc thêm provenance guard chặn self-approve cho force-category) — chặn self-approve ngay cả trước cutover. Mở rộng `_guard_ie_financial_columns` chặn transition sang `approval_status='APPROVED'` và sửa `amount` từ role `authenticated`/`anon` trên row chưa-claim.
- **BLOCKED theo gate (T7 per-domain):** REVOKE EXECUTE 8 legacy writer + REVOKE table DML — chỉ sau khi drain UI từng domain sang canonical (cần frontend wiring, xem §2).

**(d) Irreversible/gate?** Guard forward-fix = reversible, làm được ngay (cần owner apply lệnh SQL). Revoke = cần per-domain canary drain (gate lịch, không retention).

### 1.2 Storage cross-tenant PII đọc được (§14.2, §14.1, §20.13, §20.3, §4.3, §16.8)
**(a) Yêu cầu:** SELECT/signed URL phải `authorize(<resource>.view, org, resource)`; bảng `storage_object_links(bucket,object,org,resource,…)` làm nguồn ánh xạ; bỏ authenticated-wide PII read; PII bucket key riêng.

**(b) Chưa đóng — đây là DEFECT sống, không chỉ thiếu:** 7 SELECT policy PII (`customer_id_cards`, `payment_receipts`, `ie_attachments`, `customer_images`, `meter_images`, `job_attachments`, `ui_references`) đều PERMISSIVE, `TO authenticated`, qual **chỉ `bucket_id='…'`** — không org/resource/owner. Bất kỳ authenticated user tenant A biết/đoán object name có thể đọc + ký signed URL cho CCCD/receipt của tenant B. `storage_object_links` KHÔNG tồn tại (`to_regclass=null`). `authorize_v2` tồn tại nhưng 0 policy tham chiếu. Còn 4 orphan policy `room-sale-images` (có `Public view` = SELECT public).

**(c) Đóng an toàn:** Tạo `storage_object_links` + backfill (row không map → quarantine, không xoá nguồn). Rewrite 7 policy dùng JOIN link-table → `authorize_v2(resource.view, org, resource)`. Tách PII bucket key. DROP 4 orphan room-sale-images. **Gate bắt buộc:** negative test user A đọc exact object B phải DENY mới coi DONE.

**(d) Gate:** T8, `IN_DESIGN/BLOCKED`. Cần owner slice riêng + phụ thuộc T2 authorize_v2 + T6a org-derivation. **KHÔNG fix ngay được** (cần metadata model + backfill), nhưng KHÔNG bị khoá retention — có thể bắt đầu build link-table + policy trên staging bất cứ lúc nào. Đây là exposure PII đang sống → nên ưu tiên cao nhất trong nhóm chưa-fix-được-ngay.

### 1.3 Definer ACL: internal/trigger/helper callable bởi anon/PUBLIC (§20.12, §9.1/9.3, §21 P1)
**(a) Yêu cầu:** ZERO internal/helper/trigger function executable bởi anon/PUBLIC; REVOKE ALL FROM anon rồi allowlist theo signature; CI gate chống regress.

**(b) Chưa đóng:** 100 public secdef còn anon-EXECUTE (baseline 110, mới revoke ~10 = ~9% burn-down). Trigger/helper nội bộ vẫn anon+auth+PUBLIC: `handle_new_user`, `set_user_id_from_auth`, `recompute_invoice_after_*`, `trg_*`, `is_admin`, `is_super_admin`, `staff_can`, `_termination_pick_account`. Money/nhạy cảm anon-callable: `delete_staff_member`, `record_invoice_payment_v2`, `super_admin_force_cancel_invoice`, `award_job_bonus`, `zalo_*`, `restore_income_expense`. `definer-acl-baseline` chỉ freeze count=100 (anti-regress) chứ không burn-down.

**(c) Đóng an toàn — phần lớn FIX NGAY:**
- **FIX NGAY (an toàn, trigger vẫn chạy dưới quyền owner nên không cần EXECUTE grant):** `REVOKE EXECUTE FROM PUBLIC, anon, authenticated` trên toàn bộ trigger fn (`handle_new_user`, `set_user_id_from_auth`, `recompute_*`, `trg_*`, `_invoice_audit_*`, `_termination_pick_account`, `_termination_ensure_type` sibling đã sót). Helper (`is_admin/is_super_admin/staff_can`): revoke anon+PUBLIC, giữ authenticated nếu RLS cần.
- **Ưu tiên revoke ngay** anon khỏi `delete_staff_member`, `record_invoice_payment_v2`, `super_admin_force_cancel_invoice`.
- Đổi `definer-acl-baseline` thành enforce allowlist (burn-down 100→allowlist) + CI gate.

**(d) Không irreversible, không cần retention.** Là điều kiện mở khoá T8. Rủi ro thấp (trigger không mất chức năng). Chỉ cần owner apply — **đây là cụm critical FIX-ĐƯỢC-NGAY lớn nhất.**

### 1.4 Rollout governance vi phạm §27 (§27.1, §27.4, §27.6, §27.7, §22, §27.5)
Đã nêu ở mục 0. Bổ sung:
- **§27.4:** chỉ `invoice.record_payment.v1` đi qua CANARY thật; 10 flag còn lại OFF→ON trực tiếp trong 1 batch 17:48:29, 0 canary_org, 0 operation logged.
- **§22:** release-identity của 10 domain money dán chung `commit_sha`/`migration_sha256` của **payment** (provenance sai domain); `approval_reference` là chuỗi chat không phải sign-off.
- **§27.5/§3.2:** STATUS.md + PLAN §27.5 **stale** — vẫn ghi T2/T5/T6b/T7=BLOCKED trong khi runtime 11 flag ON + 12 writer granted org-wide. `grep mega docs/` = 0 hit. Tracker understate đúng fact an-toàn-nhất (money writers live) → vi phạm §0.1 "tracker không tin mù".

**Đóng:** (mục 0 phương án A/B) + **FIX NGAY:** cập nhật STATUS.md/§27.5 phản ánh runtime thật (mega-window, 11 flag ON, 12 grant, commit/hash, cap, observation, owner-gate) + thêm entry EVIDENCE-INDEX cho MEGA-20260718 với evidence hash. Sửa release-identity per-domain nếu giữ ON. Tài liệu = reversible, làm ngay.

---

## 2. HIGH — Frontend-per-domain wiring (khoá bởi FE, không phải retention)

Các gap này cần adapter FE gọi canonical writer + drain legacy; **không fix bằng SQL đơn thuần**. Backend writer phần lớn đã live.

| Domain | Requirement | Gap | Trạng thái |
|---|---|---|---|
| **Refund/Salary atomic** (§21/8.1.2, §20.9) | reversal & salary payout = 1 RPC atomic | `useDeletePayment.ts:57-76` làm 3 request rời (soft-delete IE + delete excess + delete payment), KHÔNG gọi `reverse_invoice_payment_v3`; `useManagerSalary.ts:815-927` vẫn `.insert` rời, `salary_payout_v1` **0 caller** | Writer live, FE chưa wire |
| **Invoice status lifecycle** (§21 P1/8.1.1, §7, §20.10, §27.2) | approve/unapprove/cancel qua state machine, server set actor | KHÔNG có RPC `approve_invoice/cancel_invoice`; FE `useInvoices.ts` direct `.update({status:'APPROVED',approved_by:user.id})` (dòng 928-951, 978, 1041, 1199, 1530); `useCreateInvoice` hard-code status APPROVED bỏ qua `auto_approve_invoice` setting | **MISSING writer** + FE bypass |
| **Invoice create path** (§27.2, §20.10) | mọi create path enforce `auto_approve_invoice` server-side | `create_invoice_v1` live nhưng **0 UI caller**; GenerateInvoiceDialog + Excel bulk direct-insert | Writer live, FE chưa wire (masked vì 2 org đều auto=true) |
| **Meter+invoice atomic** (§21/8) | reading APPROVED + invoice cùng transaction | `create_meter_reading_v1` không flag-gated, không in-tx với invoice; FE direct-INSERT APPROVED (GenerateInvoiceDialog:488, useExcelInvoiceData:183, comment cố ý không rollback) | **MISSING atomic writer** |
| **Contract/deposit orchestration** (§21/2) | renew/transfer/terminate atomic | Chỉ `create_contract_v1`/`create_reservation_deposit_v1` có writer (0 UI caller); **KHÔNG có `renew/transfer/terminate_contract_v1`**; FE `useContracts.ts` 4 request rời (split-brain), `useContractOperations` gọi legacy | **MISSING renew/transfer/terminate writer** + FE chưa wire |
| **Domain create (income-expense/cashbook)** (§6/8.1, §8.3, §11.5) | UI qua canonical writer | `create_income_expense_v1` live nhưng 0 UI caller VÀ **không dùng `authorize_tenant_action_v3`** (dùng legacy `authorize_income_expense_on_building`, bỏ qua deny-override → deny-bypass) | Writer live nhưng resolver lệch |

**Cách đóng:** (1) viết writer còn thiếu (invoice status machine, meter+invoice atomic, renew/transfer/terminate); (2) wire FE per-domain; (3) canary + reconcile per-domain; (4) revoke legacy. **Gate lịch per-domain, không retention.** `create_income_expense_v1` cần đổi sang `authorize_tenant_action_v3` — **FIX NGAY được** (đổi 1 call, seed TENANT_OWNER role_permissions cho `income_expenses.*`).

---

## 3. HIGH — Membership lifecycle & tenant invariants (foundation, không retention)

### 3.1 Staff lifecycle hard-delete (§10.1/10.2, §11.2, §20.14, §21 P1)
- `delete_staff_member`: `DELETE FROM auth.users` + `DELETE FROM roles` → **hard-delete auth principal, mất audit identity**. anon+auth-EXECUTE. Không có `invite/accept/suspend/remove/update_member_authorization` RPC. `organization_membership_events` không tồn tại. **0 trigger last-owner guard** → suspend/revoke owner cuối của org ACTIVE không bị chặn.
- **Đóng:** thêm 5 lifecycle RPC + đổi `delete_staff_member` thành soft-disable (SUSPENDED/REVOKED + valid_to, giữ auth.users) + deferred CONSTRAINT TRIGGER last-owner (lock org row). Revoke anon. **FIX NGAY được** phần last-owner guard + revoke anon; lifecycle RPC là build mới (T6b, gate nhưng không retention).

### 3.2 Org NOT NULL + composite (§16.8, §20.1)
- Data zero-null đạt (6 bảng money = 0 NULL) NHƯNG `attnotnull=f` (có thể regress). Composite `UNIQUE(org,id)` chỉ có ở `income_expenses`. `_autofill_org` chỉ INSERT (UPDATE set-NULL lọt) + fallback gán CỨNG PROD org thay vì fail-closed. `cron_runs/notifications/public_room_events` còn NULL.
- **Đóng:** phân loại 3 bảng còn NULL → `SET NOT NULL` + composite UNIQUE cho 5 bảng còn lại; sửa `_autofill_org` fail-closed + mở rộng UPDATE. **FIX NGAY được** (transaction, NOT VALID→VALIDATE tránh lock). Reversible.

### 3.3 Canonical admin RPC dual-write (§11) + authorization_version (§11.7)
- 0 function ghi 5 bảng RBAC mới; admin mutate qua PostgREST trực tiếp, không propagate; `authorization_version` **không bao giờ bump** (chỉ 1 lần one-off migration) → cache key `(user,org,version)` không invalidate dù resolver v3 đã đọc `organization_memberships` live (drift authoritative input).
- **Đóng:** routine `bump_authorization_version(org)` in-tx + AFTER trigger trên role_permissions/memberships/bindings/overrides/scopes; canonical admin RPC dual-write legacy↔new. **FIX NGAY được** (additive trigger), nên làm sớm vì resolver đã đọc bảng mới.

### 3.4 Payments finality (§21 P1/2, §20.11)
- Positive half DONE (`reverse_invoice_payment_v3`). Negative half hở: guard `a00_payment_canonical_link_guard` là **NO-OP** (0/965 payment canonical-linked); `payments` GRANT DELETE/UPDATE/TRUNCATE cho authenticated, RLS not forced, policy `payments_delete_rbac` cho `invoices.delete` → hard-DELETE bất kỳ payment. Legacy `invoice_audit_log` **0 freeze trigger + GRANT TRUNCATE cho anon/authenticated** → xoá sạch audit (RLS không cover TRUNCATE).
- **Đóng FIX NGAY:** thêm freeze trigger `invoice_audit_log` (BEFORE TRUNCATE/UPDATE/DELETE raise) hoặc revoke TRUNCATE/UPDATE/DELETE; siết DELETE/UPDATE payments (freeze trigger vô điều kiện, không phụ thuộc flow-owned). Additive, reversible.

---

## 4. HIGH/MEDIUM — Approval engine DB integrity (đa số FIX NGAY, additive constraint)

Engine đã APPLIED nhưng chưa live (`approval_requests`=0 row). Các gap là invariant DB thiếu — **fix bằng constraint/trigger additive, làm ngay, reversible, không data hiện tại vi phạm**:

- **§12.4/12.5 composite FK candidate (DEFECT):** `approval_decisions` thiếu FK 5-cột tới candidates + FK actor `(org,membership_id,user_id)`→memberships; `decide_financial_request_v1` ghi enum `CHECKER_*` với `candidate_id/generation=NULL` → nhánh FK là dead code. **Fix:** ghi candidate thật + đổi enum về APPROVE/REJECT (kích hoạt CHECK) + add 2 FK; hoặc constraint trigger. Cần backfill 0 row (dễ).
- **§12.4 unique index one-normal-per-generation (DEFECT):** predicate `WHERE decision IN('APPROVE','REJECT')` không bao giờ khớp row engine ghi (`CHECKER_*`) → NO-OP. **Fix:** mở predicate + dùng cột non-null trong key.
- **§12.4 state↔posting cross-check (DEFECT):** không CHECK `(state='POSTED')=(posted_event_id IS NOT NULL)`; `reverse_financial_request_v1` ghi row POSTED với posted metadata NULL. **Fix:** set posted_event_id/posted_at cho reversal + add CHECK.
- **§12.5 precedence (DEFECT):** `submit_financial_request_v1` chỉ `ORDER BY priority ASC LIMIT 1` — không DENY-first, không force-REQUIRE_APPROVAL-first, không tie-break. Đã có inversion sống (priority 100 AUTO_POST thắng 200 REQUIRE_APPROVAL cho phiếu SENSITIVE). **Fix:** `ORDER BY (effect='DENY') DESC, (force AND effect='REQUIRE_APPROVAL') DESC, priority ASC, specificity DESC, id ASC`.
- **§12.3 ANY⇒min=1 (PARTIAL):** không lớp nào enforce; ANY step min=5 thỏa ở 1 approval. **Fix:** CHECK `(mode<>'ANY' OR min_approvals=1)`.
- **§12.3 shape CHECK approver (MISSING):** thêm CHECK null-shape per approver_type (4 row hiện thỏa sẵn).
- **§12.7 reversal ledger (PARTIAL):** reversal chỉ ghi approval_requests row, không post compensating income_expenses leg → nếu wire as-is sẽ double-count. **Fix trước khi wire.**

**(d)** Tất cả reversible, additive; nên gom 1 batch "approval-engine-integrity forward-fix" apply trước khi engine nhận traffic. Không retention.

---

## 5. HIGH/MEDIUM — Test/observability/reconciliation harness (T4a, IN_DESIGN)

- **§18 test matrix (PARTIAL):** thiếu JWT-over-HTTP membership matrix, full Cartesian role×scope×resource×channel, Storage/R2/Edge deny test (18.5), perf p95<20ms (18.6), property-based fast-check authz (18.7 — grep=RỖNG).
- **§19.2 metrics/alert (MISSING):** `track_functions=none`, `log_min_duration=-1`; không metrics view cho 9 chỉ số, không alert pipeline cho emergency-override/cross-org/duplicate-posting/audit-mutation.
- **§20.15/§27.6 reconciliation (PARTIAL):** `reconcile-money.mjs` chỉ phủ 1 chỉ tiêu (income); thiếu EXPENSE/invoice/deposit/salary/KQKD/cashbook-gate. `canonical_write_operations` chỉ 3 row (payment demo).
- **§9.3/§20.16 CI gate (PARTIAL):** 4 harness script tồn tại nhưng **KHÔNG CI nào chạy** (workflow duy nhất là `workflow_dispatch` deploy); thiếu search_path pin check.

**Cách đóng:** build T4a harness (JWT thật, full-domain reconcile fail-closed, metrics view + alert, CI gate on:pull_request chạy 4 harness). **FIX NGAY được** phần CI wiring + mở rộng reconcile + bật track_functions; là điều kiện gate cho mọi canary domain kế tiếp. Không retention.

---

## 6. MEDIUM — Permission model residuals & edge config

- **§15 send-push (DEFECT):** `jwtRole()` decode JWT KHÔNG verify chữ ký, `isService = token===serviceKey || jwtRole==='service_role'` → tin claim. **FIX NGAY:** bỏ nhánh decode, so sánh constant-time serviceKey, token khác → `auth.getUser()`. Pin `verify_jwt=true` vào `config.toml` + CI assert qua Management API. (Runtime hiện verify_jwt=true nên chưa exploit, nhưng regression-guard thiếu.)
- **§11.7/§11.1 get_my_permissions (PARTIAL):** vẫn first-assignment-wins `ORDER BY … LIMIT 1` + `__superadmin` sentinel cho tenant owner; chưa có DTO context. **Fix:** DTO `get_my_permission_context()` backed by scoped resolver + retire legacy. (Chỉ lớp UI/defense-in-depth vì writer secdef override lúc enforce → medium.)
- **§11.5/11.6 authorize_v2 orphan (DEFECT low):** body drop role-DENY + unscoped override, nhưng **0 caller, revoked khỏi authenticated** → dead code. **Fix:** drop hoặc thin-wrapper gọi `authorize_tenant_action_v3`. Không exploitable.
- **§16.4 permission_definitions (PARTIAL):** seed 213 key nhưng không freeze registry + còn 12 legacy JSONB key drift không map. **Fix:** add registry/deprecate + validation + immutability trigger.
- **§16.6 shadow parity (PARTIAL):** `authorize_v2` orphan không chạy shadow; `effective_perms_v2` **không phải parity oracle** (bỏ role-DENY + scope). **Fix:** build scoped parity harness, persist mismatch, gate zero-mismatch §27.6.
- **§20.17 reservation transitions (PARTIAL):** có create + no-double-hold (gist exclusion) nhưng **thiếu reject/cancel/expiry writer + audit trigger + SLA alert**; `create_contract_v1` approve không revalidate expires_at. **Fix:** build state machine transitions.

---

## 7. Bị khoá theo lịch / retention (KHÔNG fix ngay)

| Mục | Lý do khoá |
|---|---|
| Revoke 8 legacy money writer + table DML (§20.5, §8.3, §27.4 bước 6-8) | Cần per-domain canary drain (FE wiring xong) → gate lịch per-domain |
| Storage T8 migration (§14.1-14.4, §20.13) | Cần T2 authorize_v2 + T6a org-derivation + owner slice; build được nhưng cutover cần gate |
| Xoá nguồn storage cũ (§14.3 bước cuối) | Sau retention (T9) |
| §21 P3 (rename created_by, append-only posting lines, external audit sink) | **Retention 90 ngày post-cutover-stable (T9)** — đúng deferred, low, không action bây giờ |
| Independent audit sign-off + Sprint 7 gate (§17) | Cần recovery VERIFIED (hiện ACCEPTED_LOCAL) + reviewer độc lập + zero P0/P1 |
| R2 worker upload-intent + private-class (§14.4) | Infra Cloudflare, cần build; một phần (allowlist/cron-auth) đã có trong repo |

---

## 8. Tóm tắt "làm được NGAY" (forward-fix, additive, reversible — chỉ cần owner apply lệnh)

Không cần retention, không cần FE, không cutover — chỉ siết/thêm guard:

1. **Definer ACL (§1.3):** REVOKE anon/PUBLIC khỏi ~11 trigger/helper fn + ưu tiên `delete_staff_member`/`record_invoice_payment_v2`/`super_admin_force_cancel_invoice`. ⚡ lớn nhất, mở khoá T8.
2. **approve_voucher self-approve (§1.1):** gỡ nhánh `ie.user_id=auth.uid()` + guard `approval_status`/`amount`.
3. **invoice_audit_log immutability + payments hard-delete guard (§3.4):** freeze trigger / revoke TRUNCATE.
4. **Approval-engine integrity batch (§4):** ~7 constraint/trigger/precedence fix (0 row vi phạm hiện tại).
5. **authorization_version bump trigger + org NOT NULL/composite (§3.2, §3.3).**
6. **send-push verify-signature fix + config.toml pin (§6).**
7. **last-owner guard trigger + revoke anon delete_staff_member (§3.1).**
8. **Tracker/EVIDENCE sync mega-window (§1.4) + drop orphan authorize_v2 + orphan room-sale-images policy.**
9. **CI gate wiring 4 harness + reconcile mở rộng + track_functions (§5).**
10. **create_income_expense_v1 → authorize_tenant_action_v3 (§2 cuối).**

**Cần FE-per-domain (gate lịch, không retention):** refund/salary/invoice-status/invoice-create/meter+invoice/contract renew-transfer-terminate wiring → rồi mới drain + revoke legacy.

**Quyết định owner cần trước tiên (mục 0):** hạ 10 flag non-payment về OFF/SHADOW **hoặc** revoke 12 wrapper khỏi authenticated — để runtime thôi vi phạm §27.7. Đây là hành động reversible, tác động thấp (UI vẫn chạy legacy), khôi phục ngay boundary bảo mật.

*Lưu ý: mọi "fix ngay" ở trên vẫn nằm trong chương trình authz → phải có lệnh owner apply lên production theo CLAUDE.md; "fix ngay" nghĩa là kỹ thuật sẵn sàng, reversible, không chờ retention/FE, không phải tự ý apply.*
---

# PHỤ LỤC — 89 gap thô (severity | §mục | status :: requirement || fix)

[critical] §21 / 2 / 8 PARTIAL :: P0: cấm direct creation/update APPROVED trên financial paths (client không tự ghi APPROVED)  || FIX: Sau canary/cutover: revoke table DML nghiệp vụ (INSERT/UPDATE/DELETE) khỏi authenticated/anon trên income_expenses + revoke approve_voucher; hoặc mở rộng freeze/RLS thành provenance-scoped guard chặn mọi transition sang APPROVED trên cả row chưa-claim (WITH CHECK cấm approval_status='APPROVED' từ current_user authenticated/anon, chỉ cho phép qua canonical writer/transition token).

[critical] §21 P1 / 8.1.1 DEFECT :: P1: approve_voucher không được cho creator tự duyệt (maker-checker mặc định)  || FIX: Route qua T3 approval contract v2 (decide_financial_approval / decide_financial_voucher) enforce maker-checker (actor != maker) + narrow closed exception (ordinary expense dưới hạn mức + account maker nắm giữ + exact permission + hàng hậu kiểm). Sau khi state machine live và có UI caller, REVOKE EXECUTE approve_voucher(uuid) khỏi authenticated hoặc bỏ nhánh ie.user_id=auth.uid(). Trước đó có thể forward-fix bằng provenance guard chặn self-approve cho force-approval categories.

[critical] §8.3 / 8.1.3 MISSING :: Invariant #1: client KHÔNG có quyền INSERT/UPDATE income_expenses/invoices/payments/accounts (POSTED, approved_by, totals); revoke direct DML, mọi write qua canonical writer.  || FIX: Sinh allowlist table/column→canonical writer; sau canary từng domain (T7): revoke INSERT/UPDATE trực tiếp money tables khỏi authenticated+anon, drop/thu hẹp các RBAC policy insert/update rộng, và thêm guard cho totals(amount)+approval_status trên row legacy (hoặc buộc mọi write qua canonical writer role). Đến lúc đó Invariant #1 mới đóng.

[critical] §14.2 DEFECT :: SELECT/signed URL phải authorize(<resource>.view, org, resource); policy org/resource thay cho authenticated-wide; PII bucket có key riêng.  || FIX: Tạo storage_object_links(bucket_id, object_name, organization_id, resource_type, resource_id) + backfill; thay 7 policy *_select_authenticated (và INSERT/UPDATE/DELETE tương ứng) bằng policy JOIN storage_object_links → authorize_v2(resource.view, org, resource). Tách PII bucket key riêng. Chạy negative test: user tenant A SELECT + tạo signed URL cho exact object của tenant B phải DENY; chỉ khi deny mới coi DONE. Cũng inventory/cleanup 4 policy mồ côi room-sale-images (Public view = SELECT public) để bucket tái tạo không thừa hưởng.

[critical] §20.12 DEFECT :: Definer/ACL hardening: ZERO internal/helper/trigger function executable bởi anon/PUBLIC  || FIX: REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated trên toàn bộ trigger fn (handle_new_user, set_user_id_from_auth, recompute_invoice_after_*, trg_ie_reconcile_room, trg_room_status_reconcile, _invoice_audit_invoices, _termination_pick_account) — trigger vẫn chạy vì fire dưới quyền table owner, không cần EXECUTE grant cho caller role. Với helper (is_admin/is_super_admin/staff_can): move impl sang app_private hoặc chỉ grant authenticated nếu RLS policy cần, revoke anon+PUBLIC. Thiết lập allowlist public RPC rồi burn-down count từ 100→allowlist, đổi definer-acl-baseline thành enforce allowlist thay vì chỉ freeze số lượng. Đây là điều kiện mở khoá T8.

[critical] §20.5 DEFECT :: No direct posting/balance mutation: client không thể post APPROVED/POSTED/ledger ngoài canonical RPC  || FIX: Complete canary observation → drain real-org traffic onto canonical v4 writers → REVOKE EXECUTE from authenticated on all 8 legacy money writers (T7 cutover). Until then criterion 20.5 fails. Verify post-revoke with has_function_privilege('authenticated', oid, 'EXECUTE')=false for each.

[critical] §27.1 DEFECT :: Áp callable RPC / behavior flip lên production CHỈ khi owner ra lệnh RIÊNG cho ĐÚNG tranche, sau review bằng chứng/gate (§27.1 dòng 1960, §27.6 dòng 2050). ACCEPTED_LOCAL: KHÔNG blanket; mỗi apply phải có owner exception riêng bind exact tranche/slice/SHA/hash/window.  || FIX: Yêu cầu owner exception riêng cho từng flag/tranche, bind exact SHA/migration_sha256/maintenance_window có ends_at hữu hạn và cap max_operation_count/max_single_amount_vnd thực tế (không INT_MAX/1e12); hoặc rollback về OFF các flag chưa qua §27.6 GO gate cho tới khi có lệnh owner per-tranche + evidence. Thay approval_reference blanket bằng ref duy nhất từng tranche.

[critical] §27.4 DEFECT :: Cutover theo domain: KHÔNG dùng một lần khổng lồ. Mỗi domain (invoice, payment, thu-chi, cashbook, deposit/contract, salary) phải đi qua cùng gate 8 bước riêng, có canary org + freeze procedure + maintenance window riêng.  || FIX: Đưa 10 flag chưa qua canary về OFF/SHADOW. Chạy lại từng domain (invoice, thu-chi, cashbook, deposit/contract, salary, reverse) qua gate 8 bước §27.4 riêng: window+canary org riêng, chụp reconcile pre-state, OFF→CANARY→(quan sát deny/RPC error/idempotency/drift vs abort threshold)→ON, mỗi domain 1 maintenance_window_id + commit + migration_sha riêng, không gộp chung MEGA-20260718.

[critical] §27.7 DEFECT :: Trạng thái là NO-GO cho runtime cutover. Bước hợp lệ đầu tiên chỉ là prepare/review T1a + T1b payment hardening; CHƯA wire approval prototype, CHƯA revoke DML/RLS/Storage/ACL. Không coi '0 UI caller' / 'flag OFF' / 'RPC additive' là security boundary.  || FIX: REVOKE EXECUTE 12 wrapper khỏi authenticated, trả về service_role/postgres-only (self-authorizing KHÔNG được coi là containment); đặt lại 11 flag money-domain về OFF/SHADOW, hoặc CANARY scoped canary_orgs + cap VND thật cho ĐÚNG domain đã vượt §27.6 gate (per-tranche owner command, cross-tenant REST negative test 2 org thật, reconciliation per-domain). Ngừng coi '0 UI caller' làm boundary. Ủy quyền 'lam-het-mot-luot' không thỏa 'lệnh owner riêng cho đúng tranche' — cần owner ra lệnh per-tranche bind exact SHA/hash/window/canary/cap trước khi giữ bất kỳ grant/flip nào.

[high] §21 / 8.1.2 PARTIAL :: P0: payment/voucher/item phải atomic cho single/bulk/refund/salary (không nhiều request tách transaction)  || FIX: Wire FE refund (useDeletePayment.ts) qua reverse_invoice_payment_v3 và salary (useManagerSalary.ts) qua salary_payout_v1 để reversal & salary payout thành 1 RPC atomic; sau đó T7 drain/revoke legacy v2 (anon!) + v3 + direct DML. Cập nhật tracker: route giờ đã global CANONICAL cho org thật (mode ON), không còn canary-only demo.

[high] §21 / 2 PARTIAL :: P0: cấm direct accounts.initial_amount/initial_date và cashbook lock/archive mutation ngoài state machine  || FIX: Đóng đường direct: REVOKE UPDATE(initial_amount,initial_date,lock_date) ON public.accounts FROM authenticated, anon (hoặc thêm BEFORE UPDATE trigger raise nếu các cột này đổi ngoài SECURITY DEFINER state machine — dùng app_private session guard). Sau cutover: REVOKE EXECUTE create_opening_adjustment FROM authenticated. Kèm test PostgREST PATCH phải 403/không đổi được.

[high] §21 / 8 PARTIAL :: P0: meter reading APPROVED + invoice generation phải cùng transaction (dialog đơn + Excel batch)  || FIX: Tạo writer canonical duyệt-chỉ-số-và-tạo-hoá-đơn atomic (một RPC/transaction bao INSERT reading APPROVED + generate invoice, rollback đồng bộ); wire GenerateInvoiceDialog.tsx + useExcelInvoiceData.ts + useUnapproveMeterReading qua RPC, flag-gate qua evaluate_feature_route; drain direct-INSERT/UPDATE meter_readings; siết RLS insert/update_rbac để ép ghi qua writer; revoke legacy bulk_create_meter_readings sau khi FE hết caller.

[high] §21 / 2 PARTIAL :: P0: contract/deposit/reservation orchestration phải atomic (tránh split-brain room/contract/tenant/invoice/ledger)  || FIX: (1) Viết canonical atomic writer cho renew/transfer/terminate (renew_contract_v1/transfer_contract_v1/terminate_contract_v1) bọc toàn bộ mutation room+contract+tenant+invoice+ledger trong một transaction. (2) Wire FE (useContracts create, useContractOperations renew/transfer/terminate/register-moveout) qua canonical writer thay direct .from() và legacy RPC. (3) Canary từng domain (T6b/T7), rồi revoke EXECUTE legacy definers khỏi authenticated để drain parachute.

[high] §21 P1 MISSING :: P1: 110 secdef anon-executable phải classify/revoke theo allowlist  || FIX: Chạy T8/Section 9: export full catalog (identity args, prosecdef, ACL), REVOKE EXECUTE ON ALL FUNCTIONS IN SCHEMA public FROM PUBLIC, anon trên staging, grant lại theo signature-allowlist các endpoint public thực sự có test; thêm CI gate fail nếu có secdef mới PUBLIC/anon-executable hoặc thiếu entry allowlist. Ưu tiên trước: revoke anon khỏi delete_staff_member/record_invoice_payment_v2/super_admin_force_cancel_invoice.

[high] §21 P1 / 2 PARTIAL :: P1: unapprove/hard-delete payment không được phá finality (chỉ reversal đối ứng, giữ bản gốc)  || FIX: Đóng đường destructive (forward-fix, gated owner cutover): revoke EXECUTE unapprove_voucher khỏi authenticated; siết DELETE/UPDATE/TRUNCATE trên payments (revoke grant hoặc thêm freeze trigger chặn hard-DELETE vô điều kiện, không phụ thuộc flow-owned). Lưu ý guard hiện tại vô hiệu vì 0/965 payment được canonical-link — cần bảo vệ cả payment chưa migrate vào canonical flow, không chỉ payment đã claim flow ownership.

[high] §21 P1 DEFECT :: P1: hard-delete auth user qua delete_staff_member phải bị loại/hạn chế (thay bằng suspend/revoke membership, giữ audit identity — plan mục 595/1545/1732)  || FIX: REVOKE EXECUTE ON delete_staff_member FROM anon, authenticated (nằm trong allowlist 110 secdef anon-executable ở P1 item 1757). Thay hard-delete bằng writer soft-disable: đổi body sang set membership status SUSPENDED/REVOKED (không DELETE auth.users, không DELETE roles) hoặc chuyển sang admin edge fn gated (super-admin + owner-last protection). Giữ audit identity/provenance theo plan mục 595.

[high] §21 P1 / 8.1.1-8.1.2 PARTIAL :: P1: write path invoice/payment legacy (usePayments, useInvoices, invoiceHelpers, useContracts) + invoice status lifecycle phải sau RPC atomic/state machine  || FIX: Viết + apply RPC state machine approve_invoice/unapprove_invoice/cancel_invoice (permission invoices.approve, server tự set actor, idempotent nếu đã auto-approved) theo plan 8.1.1; wire useApproveInvoice/useUnapproveInvoice/useCancelInvoice/useBulkApproveInvoices/useCheckOverdueInvoices qua RPC; drain direct .update({status}); sau đó revoke direct DML trên invoices.status (RLS). Đồng thời wire invoice/contract legacy write path còn lại (invoiceHelpers, useContracts, useContractOperations) qua canonical writer.

[high] §21 P1 MISSING :: P1: Storage SELECT 7 bucket TO authenticated theo bucket + document-templates folder-scope; send-push service-role JWT decode phải harden  || FIX: T8: tạo storage_object_links(bucket,object,org,resource) + rewrite 7 SELECT policy dùng authorize(<resource>.view, org, resource) qua DB metadata thay vì bucket_id-only; siết document-templates sang resource-scope; cleanup 4 orphan room-sale-images (đặc biệt Public view). send-push: bỏ tin decode JWT claim role=service_role, dùng verified service secret + auth.getUser() cho token còn lại, thêm deployment test verify_jwt không bị tắt. Cần prepared SQL + edge fn deploy — hiện chưa có artifact nào.

[high] §8.2 / 8.1 DEFECT :: Bỏ creator self-approve bypass của approve_voucher; thay unapprove_voucher bằng reverse_financial_posting; đóng pay_draft_fee_voucher account guard.  || FIX: Sau khi wire frontend sang decide/reverse_financial_request_v1 + canary: REVOKE EXECUTE approve_voucher/unapprove_voucher/pay_draft_fee_voucher/restore_income_expense khỏi authenticated (và restore_income_expense khỏi anon); GRANT các *_financial_request_v1 cho authenticated. Trước đó tối thiểu gỡ nhánh `ie.user_id = auth.uid()` trong approve_voucher để chặn self-approve ngay cả khi chưa cutover.

[high] §9.1 / 9.3 PARTIAL :: Giảm bề mặt SECURITY DEFINER anon-executable (baseline 110), REVOKE ALL ... FROM anon rồi allowlist theo signature; không grant trigger/helper cho anon.  || FIX: Chạy (staging→prod) REVOKE EXECUTE ON ALL FUNCTIONS IN SCHEMA public FROM anon; sau đó GRANT EXECUTE lại theo allowlist tường minh từng signature cho các RPC client-callable hợp lệ. Ưu tiên revoke ngay 9 definer money/nhạy cảm còn hở. Xoá grant anon cho trigger/recompute/legacy definer. Ghi evidence count anon-definer trước/sau và giữ regression check.

[high] §10.1 / 10.2 DEFECT :: 'Xóa nhân viên' = revoke membership (SUSPENDED/REVOKED + valid_to), KHÔNG hard-delete auth principal; thay chuỗi lifecycle bằng invite/accept/update_authorization/suspend/remove RPC.  || FIX: Thêm 5 lifecycle RPC §10.2 (invite/accept/update_authorization/suspend/remove) trên organization_memberships. Chuyển delete_staff_member thành set status=SUSPENDED/REVOKED + valid_to, giữ nguyên auth.users (audit retained); hard-delete auth chỉ break-glass platform. Revoke EXECUTE khỏi anon (giữ authenticated hoặc gắt hơn theo members.remove permission). Owner cuối không được tự remove/suspend.

[high] §7 PARTIAL :: Mọi action elevated + mutation phải có backend permission key CHÍNH XÁC; direct write bị khóa cho invoices.approve/cancel/record_payment (không giữ action elevated chỉ làm button gate).  || FIX: Sau canary từng domain: revoke direct UPDATE trên invoices (hoặc siết policy/guard freeze status+approved_by+paid_amount cho role authenticated), ép approve/cancel/record_payment qua approve_invoice/record_invoice_payment_v4/decide_financial_request; rewire useApproveInvoice/useUnapproveInvoice/useCheckOverdue + tắt fallback contracts.terminate->edit (§5.4).

[high] §4.3 / (T8) PARTIAL :: Đóng authenticated-wide storage read (7 bucket), R2 worker allowlist/org-scope/quota, cleanup 4 policy mồ côi room-sale-images, salary-v5-cron kiểm caller.  || FIX: Tranche T8: thay 7 SELECT policy authenticated bucket_id-only bằng object-link/org-scope (vd foldername[1] = uid hoặc thuộc current_visible_owner_ids), đặc biệt customer-id-cards + payment-receipts (rò rỉ cross-tenant PII/chứng từ). DROP 4 policy mồ côi room-sale-images (đã chuyển sang R2). Worker allowlist + cron-auth đã có trong repo — chỉ cần xác nhận đã deploy đúng bản; cân nhắc key server-derived thay client-provided prefix và thêm org-scope thực (hiện chỉ per-uid folder).

[high] §11.2 MISSING :: Deferred constraint trigger guarantees every ACTIVE org has >=1 ACTIVE OWNER; 'không xóa/suspend owner cuối' enforced in same transaction, locking the org row, covering both organizations.status and membership member_type/status/valid_to/organization_id transitions.  || FIX: Add a DEFERRABLE INITIALLY DEFERRED constraint trigger firing on organization_memberships (INSERT/UPDATE/DELETE of member_type,status,valid_to,organization_id) and on organizations.status change. In the trigger function, SELECT ... FOR UPDATE the organizations row (serialize concurrent last-owner races), then if the org is ACTIVE and count of memberships with member_type='OWNER' AND status='ACTIVE' AND valid_to IS NULL < 1, RAISE. Enforce the same guard inside the RPC/transaction that mutates membership so the error surfaces as a controlled 4xx.

[high] §10.2 / 11.2 / 11.7 MISSING :: Membership lifecycle RPCs (invite_organization_member, accept_organization_invitation, update_member_authorization, suspend_organization_member, remove_organization_member), two-step owner transfer, and append-only membership episode history.  || FIX: Implement section 10.2 lifecycle RPCs as versioned, audited, transactional SECURITY DEFINER wrappers (invite/accept/update_member_authorization/suspend/remove + two-step owner transfer with pending->accept), wire an invitation writer, and add the organization_membership_events append-only table (or a tstzrange GiST exclusion constraint) to model membership as non-overlapping episodes rather than a single mutable row.

[high] §11 (task: canonical admin RPC dual-write) MISSING :: Admin authorization mutations write through a canonical routine and keep new RBAC tables in sync (dual-write) so evaluator inputs stay authoritative during migration.  || FIX: Build a canonical admin RPC (SECURITY DEFINER, app_private) that atomically upserts/deletes both legacy staff_assignments/roles AND the new organization_memberships/role_bindings/role_permissions/member_permission_overrides rows in one transaction, bumping organizations.authorization_version in the same txn; cut the admin UI over to it. Alternatively add BEFORE/AFTER triggers on staff_assignments/roles that mirror changes into the new tables until UI cutover. Backfill-once + no propagation must not remain, since authorize_income_expense_on_building already reads organization_memberships live.

[high] §12.4 / 12.5 (line 1159, schema 1092-1125) DEFECT :: approval_decisions phải có composite 5-cột FK (org, candidate_id, candidate_generation, request_step_id, actor_membership_id) → approval_request_step_candidates(org, id, generation, request_step_id, membership_id), với decision APPROVE/REJECT bắt buộc candidate_id/candidate_generation NOT NULL, đảm bảo binding candidate-generation ở tầng DB (FK/trigger).  || FIX: Ghi candidate_id/candidate_generation thực (từ is_eligible_current_candidate_v1) vào approval_decisions khi decide, chuyển enum về APPROVE/REJECT để kích hoạt CHECK candidate_id NOT NULL, và thêm composite 5-cột FK (org,candidate_id,candidate_generation,request_step_id,actor_membership_id)→approval_request_step_candidates(org,id,generation,request_step_id,membership_id). Nếu giữ enum CHECKER_*, thay bằng constraint trigger BEFORE INSERT validate candidate-generation-membership cùng row/generation (plan 1159 chấp nhận trigger). Cần backfill/migrate rows CHECKER_* hiện có (candidate NULL) trước khi siết NOT NULL.

[high] §12.4 (dòng 1123-1126) DEFECT :: UNIQUE index approval_decisions_one_normal_per_generation chống hai normal decision cùng actor/step/generation (append-only, một quyết định/generation).  || FIX: Mở rộng partial index sang CHECKER_APPROVE/CHECKER_REJECT VÀ dùng cột non-null trong key (candidate_generation hiện luôn NULL cho path này → NULL distinct nên index vẫn không unique; thay bằng request_version hoặc coalesce, hoặc điền generation thật). Hoặc thống nhất enum normal về APPROVE/REJECT + set candidate_generation. Đồng thời giữ count(distinct) như defense-in-depth.

[high] §14.1 MISSING :: Bảng metadata storage_object_links(bucket_id, object_name, organization_id, resource_type, resource_id, uploaded_by, classification, created_at) làm nguồn ánh xạ object→org/resource; không dùng user-id folder làm tenant boundary.  || FIX: Tạo bảng public.storage_object_links đúng cột spec §14.1 + RLS org-scoped; backfill từ storage.objects hiện có (parse bucket_id/name→org qua parent FK/resource ref), row không map được đưa vào quarantine report (§14.3 bước 2). Đây là hạng mục T8 (IN_DESIGN/BLOCKED per EVIDENCE-INDEX) — cần lệnh owner slice riêng trước khi apply.

[high] §14.2 PARTIAL :: Signed URL TTL ngắn cho PII/private class; re-authorize trước khi ký; không log URL/token.  || FIX: PII/private delivery đã dùng Supabase signed URL (TTL 3600s) + không log token — 2/3 clause DONE. Clause thiếu là RE-AUTHORIZE trước khi ký: thay các policy SELECT authenticated-wide trên storage.objects (customer-id-cards / payment-receipts / income-expense-attachments / customer-images / meter-images / job-attachments) bằng policy org/resource-scoped để createSignedUrl thực sự authorize resource; cân nhắc rút ngắn TTL cho ID card/receipt. (Cloudflare R2 /sign vẫn 501 nhưng KHÔNG phải đường phục vụ PII.)

[high] §14.3 MISSING :: Migration storage: inventory→map object→org/resource (unresolved vào quarantine)→copy theo org→dual-read signed URL→thay authenticated-wide policy bằng org/resource→negative test A đọc B→retention rồi mới xóa nguồn.  || FIX: Gate T8 sau khi T2 authorize_v2 + T6a org derivation + recovery VERIFIED. Rồi: snapshot inventory bucket/object + DB refs; tạo bảng storage_object_links (org/resource/nonce/expected key) + backfill map, unresolved→quarantine report; prepared SQL copy theo org (giữ nguồn); dual-read signed URL; thay 7 authenticated-wide SELECT policy + 'Public view room sale images' bằng org/resource policy qua authorize(<res>.view); negative test user A đọc exact path B là gate bắt buộc; chỉ xóa nguồn sau retention (T9).

[high] §14.4 PARTIAL :: R2 Worker containment P0: (1) server dựng prefix/key ngẫu nhiên, cấm client chọn full key; (2) resolve org/building + exact permission qua backend; (3) giới hạn Content-Length/MIME/magic bytes/quota; (5) upload intent/link metadata DB trước upload; (6) audit actor/org/hash/size; (7) xử lý R2 origin public.  || FIX: Chuyển sang upload-intent: backend tạo intent (nonce/org/resource/expected-key server-derived random/expiry/max-bytes/MIME) ghi DB trước; worker chỉ nhận intent id + resolve org/building/permission qua backend thay vì dùng uid folder; thêm audit actor/org/hash/size; tách private-class (không public URL, signed capability ngắn hạn) khỏi public sale-image; rà bucket public-origin để /file allowlist thực sự chặn truy cập trực tiếp.

[high] §16.8 / 16.3 PARTIAL :: Org backfill zero-null trên money/PII table; sau backfill thêm NOT NULL + composite consistency (UNIQUE(org,id)/FK).  || FIX: Sau khi assertion zero-null ổn định (đã đạt), ALTER TABLE ... ALTER COLUMN organization_id SET NOT NULL cho cả 6 bảng; và thêm composite UNIQUE(organization_id, id) cho invoices/payments/customers/contracts/income_expense_items (income_expenses đã có) để hỗ trợ composite FK tenant-scoping. Chạy trong transaction, validate NOT VALID→VALIDATE nếu cần tránh lock lâu.

[high] §20.1 PARTIAL :: Explicit tenant: mọi money/PII/business row có organization_id NOT NULL hoặc chứng minh platform/public  || FIX: Phân loại/backfill 3 bảng còn NULL (cron_runs, notifications, public_room_events) là platform/public hay tenant; sau đó ALTER TABLE ... ALTER COLUMN organization_id SET NOT NULL (hoặc CHECK) trên toàn bộ money/PII tables. Thay nhánh PROD-default fallback trong _autofill_org bằng RAISE (fail-closed) và mở rộng enforcement sang UPDATE để chặn set-NULL. Đóng T6a 'NOT NULL money/PII'.

[high] §20.4 PARTIAL :: Backend exact action: 100% mutation/elevated action trong catalog có server-side permission check tương ứng (exact action); frontend-only không tính đạt  || FIX: Sinh writer-map machine-readable (T4a §5) đối chiếu MỌI mutation RPC/direct-DML với exact permission catalog; thay nhánh ie.user_id=auth.uid() trong approve_voucher/unapprove_voucher/pay_draft_fee_voucher bằng gate maker-checker đúng (self-approve chỉ khi dưới hạn mức + account maker nắm giữ + exact permission + hậu kiểm); sau khi UI drain sang canonical writer thì REVOKE EXECUTE 3 hàm legacy khỏi authenticated.

[high] §20.3 DEFECT :: Cross-tenant zero access: ma trận direct REST/RPC/Storage/Edge A↔B đều deny kể cả biết UUID/path  || FIX: T8: thay các *_select_authenticated policy trên storage.objects bằng policy org-scoped (derive org từ object path/owner, join membership) — bỏ authenticated-wide PII read. T4a: fixture 2 org non-null + JWT thật qua PostgREST HTTP, full-domain REST/RPC + Storage/R2/Edge negative matrix (đọc cross-org object bằng path phải deny).

[high] §20.9 PARTIAL :: Atomic/idempotent money: commit một lần, retry không duplicate; sai được hoàn tác bằng operation đối ứng, không hard-delete  || FIX: Cutover org thật payment sang v4 (bỏ v3-fallback) sau observation window, verify canonical_write_operations nhận traffic thật + reconcile-money zero-diff. Sau đó wire UI từng domain (invoice.create/reverse, cashbook, salary, contract/deposit) sang canonical writer, canary + reconcile từng cái, rồi mới revoke legacy parachute.

[high] §20.8 PARTIAL :: Maker-checker có ngoại lệ đóng: mặc định maker không normal approve; self-approve chỉ phiếu thường dưới hạn mức + account maker giữ; emergency có reason/re-auth/audit/alert  || FIX: Cutover T3 state machine cho từng domain: wire canonical decide (grant decide_financial_voucher hoặc wrapper self-authorizing gọi decide_financial_request_v1 + self_approve_within_limit_v1 + emergency_approve_financial_v1), sau đó REVOKE public.approve_voucher khỏi authenticated để đóng nhánh ie.user_id=auth.uid(). Test live: maker self-approve phiếu force-category DENY; self-approve phiếu thường trên hạn mức DENY; emergency approve sinh row emergency_override_events + alert. Ghi evidence §18.3 case 5-10.

[high] §20.13 MISSING :: Storage/Edge isolation: object + service action cùng org/resource scope; Supabase Storage + R2 + Vercel + VPS worker; authenticated-wide PII read hết  || FIX: Thực thi T8: (a) thay policy *_select_authenticated flat bằng org/resource-scoped (foldername[1] = org/owner qua current_visible_owner_ids hoặc join membership); (b) R2 private delivery — upload-intent DB metadata (nonce/org/resource/expected key/expiry/max bytes/MIME), server dựng key ngẫu nhiên, signed capability ngắn hạn, không trả public URL cho PII; (c) worker authorize resource id→org→exact permission, bỏ R2_PUBLIC_BASE public cho private class; (d) service identity separation Edge/Vercel/VPS (allowlist caller, không share service key ra caller ngoài trusted); (e) revoke/guard zalo_*/fa_*/get_public_* definer về đúng allowlist + org scope; (f) cross-org Storage negative test §18.5 (user A đọc/ghi exact key của B → deny). Gate: chờ recovery đạt + lệnh owner theo §27.

[high] §20.14 PARTIAL :: Staff lifecycle: invite/suspend/revoke thay browser signUp/hard delete; owner cuối được bảo vệ  || FIX: Sprint 6 #1: viết membership lifecycle RPC (invite/accept/suspend/remove + update_member_authorization) trên organization_memberships/organization_invitations đã có; thêm deferred CONSTRAINT TRIGGER last-owner guard (lock org row) trên thay đổi member_type/status/valid_to và organizations.status; retire hard-delete (delete_staff_member -> revoke membership, không DELETE auth.users); revoke EXECUTE khỏi anon cho delete_staff_member + handle_new_user (chỉ để trigger/definer nội bộ gọi); wire frontend useDeleteStaff/StaffPage sang revoke RPC; test §18.1 case suspended/revoked bị chặn.

[high] §20.15 (mục 20 tiêu chí 15, scoped bởi §19.1) PARTIAL :: Data parity: account/invoice/payment/KQKD/salary/profit reconciliation khớp baseline trước cutover  || FIX: T4a: mở rộng reconcile-money.mjs ra full domain (EXPENSE, invoice paid/remaining/status, payment/credit/excess, deposit, salary/profit paid+locked, KQKD item-level loại deposit/internal, cashbook balance nâng từ info-line thành gate, meter→invoice, reversal đối ứng). Mỗi chỉ tiêu ≥2 nguồn độc lập + verdict delta=0/INCONCLUSIVE fail-closed. Breakdown theo org/building/account/month/status (§19.1). Ghi batch_id + query-hash + before/after counts + operator id (không PII/token). Wire vào CI như gate.

[high] §18 PARTIAL :: Test matrix: identity/scope/approval/atomicity/RLS-RPC-Storage-Edge/performance/property-based  || FIX: Hoàn thiện T4a harness: (1) JWT thật qua PostgREST cho 18.1 membership matrix + 18.2 full Cartesian scope; (2) storage/R2/edge cross-org deny tests (18.5) — gate cùng T8; (3) authorize p95<20ms + EXPLAIN(ANALYZE,BUFFERS) trên 10x scale (18.6); (4) Vitest+fast-check properties deny-precedence/scope-union/rule-determinism/state-machine-POSTED-immutable/idempotency (18.7). Bind evidence vào exact SHA + môi trường on-target.

[high] §19.2 MISSING :: Runtime metrics + alert tức thời cho emergency override, cross-org probe, duplicate posting, audit mutation attempt  || FIX: Xây metrics view lớp trên authorization_audit_events + audit_log tables (aggregate 9 chỉ số §19.2) + alert pipeline (pg_cron job hoặc edge function polling → notify) cho emergency override / cross-org probe / duplicate posting / audit mutation attempt; bật track_functions và wire log-analytics. Thuộc phạm vi T4a (đang IN_DESIGN).

[high] §17 PARTIAL :: 8-sprint plan gates (Sprint 0→7); Sprint 7 gate = acceptance criteria §20 + independent audit sign-off + no P0/P1 open  || FIX: Close gates sequentially per §27.3: recovery to VERIFIED (extension-target restore + off-machine fault-domain + independent reviewer) before any full cutover; revoke legacy definers (approve_voucher/unapprove_voucher/record_invoice_payment_v3) after per-domain canary drain; obtain independent audit sign-off and confirm zero P0/P1 before Sprint 7 acceptance (§20).

[high] §27.6 PARTIAL :: Chỉ được canary một tranche khi tất cả điều kiện gate liên quan đều đạt (canary org/transaction-class + VND cap). Cutover phải qua canary trước khi mở toàn tổ chức.  || FIX: Không phải sửa evaluate_feature_route (ON short-circuit + cap-inert-dưới-ON là đúng thiết kế). Sửa ở tầng state/process: revert 10 flag non-payment về CANARY (hoặc OFF) với canary_orgs + cap VND thật + window hữu hạn, chạy observation + reconcile per-domain, rồi mới promote ON — đúng đường payment đã đi (W1). Cần lệnh owner per-tranche theo 27.6 line 2050 trước mỗi lần mở org-wide.

[high] §27.6 PARTIAL :: Dashboard, alert, abort criteria, freeze/forward-fix/incident runbook, backup/PITR và maintenance window phải sẵn sàng trước canary.  || FIX: Trước khi giữ bất kỳ money flag nào ON làm canary thật: (1) bật kênh telemetry máy — track_functions=pl hoặc log_min_duration_statement hợp lý, hoặc dựng bảng ops_metrics + trigger đếm deny/RPC-error/idempotency-conflict/drift trong app_private; (2) dựng dashboard + alert threshold khớp abort §7 (deny-rate, RPC-error >1%/5min, p95>2x, drift≠0); (3) nhân bản runbook cutover/abort cho từng domain đang ON (invoice/reversal/cashbook/salary/contract-deposit) chứ không chỉ payment; (4) wire check-definer-acl/check-view-invoker/reconcile-money/test-cross-tenant vào CI như gate. Nâng T4a từ IN_DESIGN lên có evidence máy trước khi mở gate canary domain kế.

[high] §27.6 PARTIAL :: Cross-tenant REST/RPC/Storage/Edge tests PASS trên HAI organization thật, non-null org và JWT thật cho tranche được canary.  || FIX: Trước hoặc ngay khi giữ flag ON org-wide, chạy direct REST negative matrix với 2 org thật + GoTrue JWT thật cho mỗi 10 domain: (a) actor org A gọi wrapper với account/sổ/loại/contract của org B → phải 42501/TARGET_CROSS_ORG; (b) missing/null org → deny; (c) happy-path in-org 200. Ghi evidence per-domain kiểu W1. Nếu chưa chạy được, cân nhắc hạ 10 flag về CANARY (canary_orgs cụ thể) thay vì ON org-wide để khớp gate §27.6.

[high] §22 PARTIAL :: Mỗi PR triển khai phải kèm: catalog before/after diff (ACL/policies/functions/triggers), direct API negative tests, financial reconciliation output nếu chạm money path, audit sign-off cho P0/P1/elevated action.  || FIX: Tạo dossier mega-window MEGA-20260718 đầy đủ §22 cho từng domain: (a) catalog before/after diff ACL grant 11 wrapper + 10 flag flip; (b) direct REST/PostgREST negative test với JWT thật per-domain (không phải SET ROLE harness); (c) reconcile output money-path cho income_expense/invoice/cashbook-adjust/salary/contract/deposit khớp baseline; (d) sửa release-identity: mỗi flag phải mang commit_sha/migration_sha256 CỦA ĐÚNG domain (hiện đang dùng hash payment), và audit sign-off P0 thực chất thay cho chuỗi 'OWNER-DELEGATED'. Nếu không kịp, thu hồi (flip OFF hoặc revoke wrapper) các domain chưa có artifact, giữ lại payment vốn đã có W1.

[medium] §21 / 9.2 PARTIAL :: P0 (mở rộng): các internal helper cùng lớp không được để anon-callable (cụ thể _termination_pick_account)  || FIX: REVOKE ALL ON FUNCTION public._termination_pick_account(uuid,uuid) FROM PUBLIC, anon, authenticated; đưa vào cùng pattern migration 20260710130500 (kiểu internal-only). Đồng thời classify/revoke toàn bộ 100 anon-secdef còn lại theo allowlist P1 (mục 1757 + 9.3 điểm 2/9) và thêm CI gate 9.3 điểm 9 chống regress khi CREATE OR REPLACE reset ACL.

[medium] §21 P1 MISSING :: P1: pay_draft_fee_voucher account scope phải phản ánh shared/scoped permission đúng (thay guard user_id/is_admin bằng authorize(cashbooks.post, account))  || FIX: Thay account guard trong pay_draft_fee_voucher bằng resolver quyền cashbook thực (authorize(cashbooks.post, p_account_id) hoặc tương đương trong T2 resolver v3), đưa vào T3/T5 domain redesign khi cashbook writer stack hoàn thiện. Hiện chưa có primitive authorize nên phải xây/kết nối resolver trước.

[medium] §21 P2 PARTIAL :: P2: first-assignment-wins permission resolution phải thay bằng deny-precedence/scoped resolver  || FIX: Retire/cutover legacy public.get_my_permissions(): thay LIMIT-1 first-assignment bằng union tất cả assignment với deny-precedence, hoặc dựng permission-map từ cùng stack authorize_tenant_action_v3 (role_permissions + member_permission_overrides + scopes) để UI khớp enforcement. Đồng thời fix bản sao ai_copilot_perms_for(uuid) (plan mục 5.2) và dọn orphan public.authorize_v2 (dead, gây nhầm). Không đụng authorize_tenant_action_v3 — đã đúng và live.

[medium] §3.2 / 0.1 DEFECT :: Threat model: mọi live fact phải chụp lại + evidence hash trước mỗi tranche; tracker không được tin mù  || FIX: Cập nhật AUTHORIZATION-IMPLEMENTATION-STATUS.md: đổi trạng thái T5-writer/T3-wire/T6b-T7 (và income_expense/invoice/cashbook/salary/contract/deposit) từ BLOCKED sang APPLIED-mega-window (canary/full-org ON, config_version, cap, commit e4eec85, migration_sha256 5041ac2e…); sửa section 'Production gate đang áp dụng' để phản ánh 11/11 flag ON + 12 writer granted + legacy parachute chưa revoke. Thêm entry EVIDENCE-INDEX cho mega-window MEGA-20260718 (approval_reference 90450d5f-29b6-4897-bdef-cdb5fb53f339, flag set, grant list, reconcile money-invariant real-org==baseline, observation state) với evidence hash. Đồng bộ trước khi audit tiếp.

[medium] §6 / 8.1 PARTIAL :: Mọi đường tạo hóa đơn/voucher/payment/deposit đi qua RPC atomic canonical; UI không direct-write; chỉ payment mới wire.  || FIX: Wire frontend adapter từng domain (income-expense/invoice/cashbook/salary/contract/deposit) sang canonical writer rồi drain legacy — T5 writer flip + T7 drain, cần owner gate. Sau drain thì revoke legacy v3 + các legacy definer để backend ép boundary.

[medium] §8.1.2 MISSING :: 3 đường ghi ledger phụ (invoiceHelpers.ts:747 sinh HĐ tháng, useContracts.ts:651 insert invoices APPROVED, useContractOperations.ts:270 insert excess_amounts âm) phải nằm trong allowlist revoke direct DML Sprint 5 cùng nhóm invoice/payment.  || FIX: Khi build generated allowlist §8.1.3 (AST/grep + runtime audit), đảm bảo bắt cả 3 path này: header+items invoice, insert invoices APPROVED khi tạo hợp đồng, và insert excess_amounts âm (consume credit/cọc). Gom REVOKE direct DML trên public.invoices và public.excess_amounts vào cùng batch per-signature với payment ở T7 cutover, sau khi drain proof = 0 legacy traffic; đồng thời siết RLS insert policy (bỏ {public} INSERT, chỉ để canonical writer definer). Thêm CI check enumerate mọi .insert/.update chạm invoices/excess_amounts để chặn regress.

[medium] §9.3 PARTIAL :: CI fail nếu function mới SECURITY DEFINER + PUBLIC/anon execute, thiếu pinned search_path, hoặc thiếu allowlist entry; grant theo signature.  || FIX: Trong T4a trước cutover: (a) tạo workflow on:pull_request chạy check-definer-acl.mjs làm required check (chặn merge khi drift); (b) mở rộng script đọc proconfig để assert search_path pinned (pg_catalog,public...) cho mọi definer mới; (c) cân nhắc mở rộng probe sang authenticated (không chỉ anon) để gate bề mặt canonical wrapper; (d) thêm assert allowlist entry bắt buộc cho definer mới.

[medium] §8.3 PARTIAL :: Invariant 6/7/14: account cùng org + caller có cashbooks.post; maker chỉ chọn sổ mình sở hữu; assert_cashbook_period_open cho mọi leg.  || FIX: Sau khi wire UI qua create_cashbook_v1/lock_cashbook_period_v1/request_opening_balance_adjustment_v1: revoke direct DML trên accounts (drop/thu hẹp accounts_insert/accounts_update/accounts_staff_insert/accounts_staff_update, chỉ để canonical writer role ghi), hoặc thêm BEFORE INSERT/UPDATE trigger enforce assert_cashbook_period_open + ownership + org cho mọi write path.

[medium] §11.7 (authorization_version) MISSING :: authorization_version increments in the SAME transaction as any membership/role/permission/binding/scope/override/area-membership change, via a centralized routine/trigger doing atomic authorization_version = authorization_version + 1, to invalidate the (user_id, org_id, authorization_version) cache; DTO carries it; backend does not trust client version.  || FIX: Add a centralized SECURITY DEFINER routine (e.g. app_private.bump_authorization_version(org_id)) that all authz writers call in-transaction, OR AFTER INSERT/UPDATE/DELETE triggers on role_permissions, memberships, role_bindings, member_permission_overrides, member_override_scopes, area_buildings that do atomic authorization_version = authorization_version + 1 on public.organizations for the affected org, plus a post-commit NOTIFY/invalidation. Ensure the DTO (get_my_permissions replacement) carries authorizationVersion and backend keys cache on server-read version, never client-supplied.

[medium] §11.7 / 11.1 PARTIAL :: Replace get_my_permissions() with a permission-context DTO {organizationId, membershipId, memberType, authorizationVersion, permissions, scopes}; no __superadmin sentinel for tenant owner; no 'first assignment wins'.  || FIX: Ship a get_my_permission_context() DTO backed by effective_perms_v2 (scoped) plus organizationId/membershipId/memberType and authorizationVersion (incremented in-txn per 11.7 line 882); wire frontend to it; then retire get_my_permissions(), the __superadmin owner sentinel (keep platform super-admin on a separate context endpoint), and the ORDER BY ... LIMIT 1 first-assignment selection. Also fix the duplicate fail-open branch in ai_copilot_perms_for() (plan line 270) in the same change.

[medium] §11.7 / §27.6 gate (shadow parity zero-mismatch) PARTIAL :: Shadow-evaluate new RBAC vs legacy permissions and prove zero mismatch before cutover.  || FIX: Build a scoped parity harness comparing app_private.authorize_tenant_action_v3 output (per real membership × permission key × scope) against legacy get_my_permissions/can_do_on_building; persist mismatch rows to an authz_private parity/report table; classify explained vs unexplained; gate any RBAC cutover on zero unexplained mismatch per §27.6. Do NOT use effective_perms_v2 as the oracle — it omits role-level DENY and scope.

[medium] §11.5 / 7 (consistency) DEFECT :: All canonical writers authorize through the same normalized deny-precedence + scoped-override engine (authorize_tenant_action_v3).  || FIX: Seed TENANT_OWNER role_permissions/role_bindings for income_expenses.* (keys already in permission_definitions), then change create_income_expense_v1 to authorize via app_private.authorize_tenant_action_v3(actor, org, 'income_expenses.create' / '.restricted_create', building scope) instead of app_private.authorize_income_expense_on_building, matching the 7 sibling T5 writers; retire the legacy function once cutover verified.

[medium] §12.3 / 4.7 PARTIAL :: Candidate materialization từ 6 approver_type; generation bump đóng row cũ; ANY min=1 / ALL snapshot count / QUORUM bound; fail-closed khi 0 candidate hoặc quorum bất khả thi.  || FIX: Thêm CHECK (mode <> 'ANY' OR min_approvals = 1) trên approval_rule_steps VÀ approval_request_steps; hoặc assert trong publish_rule_set_v1 (reject DRAFT có ANY-step min_approvals<>1) và/hoặc trong materialize_step_candidates_v1 (raise 55000 khi v_step.mode='ANY' AND min_approvals<>1). Ưu tiên CHECK constraint vì catch cả insert legacy.

[medium] §12.4 (dòng 1161) / 12.7 DEFECT :: State/metadata cross-checks tối thiểu: POSTED iff có posted_at/posted_event_id; terminal không-post không có posting metadata; AUTO_POST→POSTED; DENY→DENIED. Reversal là posting mới có posted metadata.  || FIX: Hai lớp: (a) reverse_financial_request_v1 phải set posted_event_id (gen_random_uuid() posting mới) + posted_at=clock_timestamp() cho row REVERSAL state='POSTED' — reversal là posting độc lập. (b) Thêm CHECK constraint bắt invariant: CHECK ((state='POSTED') = (posted_event_id IS NOT NULL AND posted_at IS NOT NULL)) và CHECK (state IN ('DENIED','REJECTED','CANCELLED','REVERSED') → posted_event_id IS NULL) để terminal-no-post/terminal không mang posting metadata. Cân nhắc dùng state riêng cho reversal nếu không muốn trộn với posting gốc.

[medium] §12.5 (bước 5, dòng 1181) DEFECT :: Precedence DENY > force REQUIRE_APPROVAL > priority nhỏ nhất; tie phân xử theo độ đặc hiệu xác định.  || FIX: Thay `order by r.priority asc` bằng `order by (effect='DENY') desc, (force_match and effect='REQUIRE_APPROVAL') desc, priority asc, <specificity score> desc, id asc limit 1`, hoặc quét DENY trước → force REQUIRE_APPROVAL → priority; thêm tie-break xác định (specificity + id). Đồng thời dùng cột rule.effect/force_match thay vì suy force từ item types cho đúng precedence chuẩn tắc.

[medium] §12.7 PARTIAL :: Reversal là posting mới với lines đối ứng server-derive, giảm ledger/paid, original immutable; net = 0 nhưng có bút toán đối ứng thực.  || FIX: In reverse_financial_request_v1 (or a dedicated reverse_financial_posting writer), within the same txn derive & post a compensating income_expenses/posting leg from the original (server-derived amount/lines, never client amount), set original income_expenses.reversed_by_posting_id to the new posting (UNIQUE, enforcing single full reversal at the posting/ledger level), call assert_cashbook_period_open for all account legs, and only then set the reversal approval_requests row to POSTED with its posted_event_id. Add public wrapper gated by income_expenses.reverse before wiring.

[medium] §12.4 (dòng 1156) MISSING :: Bind actor identity bằng composite FK (organization_id,actor_membership_id,actor_user_id) → organization_memberships để chặn ghép membership A với user B né maker-checker.  || FIX: ALTER TABLE public.approval_decisions ADD CONSTRAINT approval_decisions_actor_membership_fkey FOREIGN KEY (organization_id, actor_membership_id, actor_user_id) REFERENCES public.organization_memberships(organization_id, id, user_id); (dùng chung unique index org_memberships_org_id_user_uidx). Cân nhắc thêm FK tương tự cho emergency_overrides nếu bảng đó cũng lưu actor_membership_id+actor_user_id.

[medium] §12 / 13 MISSING :: Approval engine phải là đường đi thực cho financial voucher (submit/decide/emergency reachable, kiểm chứng runtime).  || FIX: Khi owner mở gate: thêm public wrapper self-authorizing cho submit/decide/emergency (grant authenticated), route create_income_expense_v1/các writer qua submit_financial_request_v1, wire UI caller, chạy e2e trên org thật (tạo request → decide/emergency → post) để có bằng chứng approval_requests>0, rồi mới revoke legacy approval RPC.

[medium] §15 DEFECT :: send-push không được tin claim role='service_role' chỉ bằng decode payload JWT; chỉ chấp nhận service secret/token đã xác minh chắc chắn, token còn lại gọi auth.getUser().  || FIX: Bỏ nhánh `jwtRole(token)==='service_role'` ở dòng 81; xóa hàm jwtRole(). isService chỉ khi token khớp serviceKey bằng so sánh constant-time (crypto timing-safe equal). Mọi token khác → callerClient.auth.getUser() lấy user thật, chỉ gửi cho user.id. Thêm deployment-config test/assert verify_jwt=true cho send-push (pin trong supabase/config.toml). Bổ sung test threat-model: forged JWT role=service_role => 401/denied.

[medium] §15 MISSING :: Thêm deployment-config test chứng minh verify_jwt không bị tắt ngoài ý muốn cho các edge function nhạy cảm (send-push...).  || FIX: Thêm CI test đọc trạng thái verify_jwt qua Management API (GET /v1/projects/{ref}/functions) và assert danh sách nhạy cảm {send-push, admin-create-user, llm-proxy} đều verify_jwt=true; đồng thời pin cấu hình vào supabase/config.toml dạng [functions.send-push] verify_jwt = true (+ các fn khác) để deploy declarative không bị trôi. Có thể bổ sung guard trong scripts/deploy-edge-fn.mjs từ chối --no-verify-jwt cho slug thuộc allowlist nhạy cảm.

[medium] §16.4 PARTIAL :: Seed permission_definitions từ registry và freeze danh sách key; validate JSONB key trong roles.permissions.  || FIX: Bổ sung registry cho 12 legacy key còn drift HOẶC map/deprecate chúng tường minh, rồi thêm CHECK/validation flatten roles.permissions+staff_assignments.permissions == permission_definitions (fail nếu key lạ). Thêm trigger immutability/freeze trên permission_definitions và stamp mapping version. Không phải live authz hole (legacy JSONB vẫn là path enforce, T2 core BLOCKED) nên medium.

[medium] §16.5 MISSING :: Backfill approval state: materialize legacy status → target (APPROVED+approver→POSTED với provenance LEGACY_VERIFIED_METADATA; UNAPPROVED→DRAFT/PENDING/review theo bảng), KHÔNG tạo APPROVE decision cho row thiếu approver, ghi MIGRATION_IMPORTED audit count/hash.  || FIX: Khi T3 đạt gate cutover: chạy backfill materialize legacy approval_status→POSTED (APPROVED+approver, provenance LEGACY_VERIFIED_METADATA) / DRAFT|PENDING|review (UNAPPROVED theo bảng §16.5), điền posting_id/posted_at_v2/approval_request_id; KHÔNG tạo approval_decisions APPROVE cho ~550 expense + ~1038 income thiếu approver; ghi bản ghi MIGRATION_IMPORTED (count + hash) vào authorization_audit_events/income_expense_audit_log để đối chiếu.

[medium] §16.6 PARTIAL :: Shadow: authorize_v2 chạy song song helper cũ, ghi mismatch nhưng chưa deny; reconciliation so decision samples; cutover chỉ khi mismatch phân loại + P0/P1=0.  || FIX: Wire authorize_v2 chạy shadow trên các read path/RLS mẫu (không deny), ghi mismatch vào bảng log chuyên dụng (legacy_decision vs v2_decision + resource/permission/org), sinh reconciliation report so decision samples + row/sum theo org/building/account/status; phân loại mismatch, đạt P0/P1=0 trước khi coi 16.6 đạt và trước cutover.

[medium] §20.6 PARTIAL :: State machine: DRAFT/PENDING_APPROVAL/POSTED/DENIED/REJECTED/CANCELLED/REVERSED tách nghĩa; posted immutable, sửa bằng reversal  || FIX: Wire canonical writer (create_income_expense_v1) as the sole real income-expense write path so rows get registered in income_expense_flow_ownership and the owned-payload freeze bites; revoke/retire legacy mutators (update_income_expense_quick, unapprove_voucher, approve_voucher, restore_income_expense) or make them refuse APPROVED/POSTED rows; then prove no POSTED->mutable path exists on real (owned) data via the v2 reversal-only lifecycle.

[medium] §20.7 PARTIAL :: Rule correctness: force categories luôn approval; no-match=require; internal auto-post chỉ allowlist cân bằng  || FIX: Cutover approval engine for income-expense: wire create/verify UI path to submit_financial_request_v1 (or make canonical writer route force categories through it), REVOKE legacy verify_income_expense/update_income_expense_quick from authenticated+anon, then run §18.3 case 1-4 (boundary/precedence/no-match/force) live on real-org to prove commission/refund/salary always require approval. Also note submit impl raises on no-match (fail-closed) vs plan wording 'no match=require' — reconcile once engine is driven.

[medium] §20.10 PARTIAL :: Invoice governance: auto_approve_invoice default ON có thể tắt, server áp setting cho mọi create path; revision APPROVED-unpaid lock/version/reason/diff/audit + hậu kiểm  || FIX: Wire UI tạo hoá đơn (useInvoices.useCreateInvoice + GenerateInvoiceDialog + batch/contract/meter paths) qua create_invoice_v1, gỡ hard-code status APPROVED + approved_by/approved_at client-side; sau canary revoke RLS direct-INSERT invoices. Riêng prong revision: build writer revise_invoice_v1 cho APPROVED-unpaid với lock hàng invoice, version bump, reason bắt buộc, diff snapshot + audit row + hàng hậu kiểm; chặn revision nếu invoice đã có payment history. Test race §18.4.

[medium] §20.11 PARTIAL :: Audit provenance: không giả mạo approver; decision/state/security event không UPDATE/DELETE bởi app roles  || FIX: Add immutability triggers on public.invoice_audit_log mirroring canonical: BEFORE TRUNCATE raise + BEFORE INSERT-not-allowed-except-definer / UPDATE / DELETE guard; and/or REVOKE UPDATE,DELETE,TRUNCATE ON public.invoice_audit_log FROM authenticated, anon. Longer term route invoice/voucher mutations through canonical writer so the guarded append-only chain covers them. Verify with pg_trigger + role_table_grants after fix.

[medium] §20.16 PARTIAL :: Operational readiness: metrics, alerts, freeze/reversal/runbook, catalog snapshot và rollback gate đã diễn tập  || FIX: Dựng dashboard/alert theo §19.2 (deny rate, cross-org mismatch, orphan session, approval SLA, emergency override count, idempotency conflict, reconciliation mismatch, 42501/invalid-transition rate, signed-URL denied) + alert tức thời cho emergency override/cross-org probe/duplicate posting/audit mutation. Wire CI gate (workflow mới hoặc mở rộng) chạy 4 script harness + typecheck:baseline + related Vitest như blocking gate, lưu artifact bằng chứng mỗi tranche. Diễn tập rollback từng domain (income-expense/invoice/reversal/cashbook/salary/contract-deposit), không chỉ payment.

[medium] §20.17 PARTIAL :: Reservation hold: cọc PENDING hold phòng độc quyền 24h, không double-hold; approve/reject/cancel/expiry có lock+audit+idempotency  || FIX: Prepare + apply the missing reservation transition state machine: reject_reservation_hold_v1 / cancel_reservation_hold_v1 (lock hold FOR UPDATE, status→REJECTED/CANCELLED, idempotency + audit row), plus an EXPIRED transition (cron sweep or lazy) with audit and SLA-near-24h alert per §1296. Add expires_at revalidation in create_contract_v1's hold-consume (don't approve an expired/other-party hold). Then wire a UI caller and run a live canary org: 24h exclusive hold + concurrent no-double-hold (expect 23P01) + approve-after-expiry revalidate, capturing production evidence.

[medium] §27.6 PARTIAL :: Mọi SECURITY DEFINER trong tranche có signature ACL explicit, pinned search_path; baseline exposure KHÔNG tăng và có burn-down plan.  || FIX: Lập burn-down cụ thể (danh sách legacy writer + tiêu chí drain + thứ tự revoke theo domain, gắn T7), thực thi revoke legacy sau khi drain xong caller; hoặc pin baseline allowlist với net-exposure justification cho từng writer mới để chứng minh không có unexplained callable definer.

[medium] §27.5 DEFECT :: Tracker chính thức §27.5 phản ánh trạng thái tranche; STATUS.md là canonical runtime tracker, mỗi đổi trạng thái phải ghi đủ evidence (SHA/hash/window/observation/owner-gate).  || FIX: Thêm entry mega-window vào STATUS.md + reclassify §27.5 T1b-T9: ghi maintenance_window_id=MEGA-20260718, approval_reference=OWNER-DELEGATED-lam-het-mot-luot, updated_by uuid, updated_at 2026-07-17T17:48Z, 11 flag ON org-wide (cap=2147483647), 13 wrapper grant, commit SHA + migration_sha256 tương ứng, observation window, owner-gate. §27.5 phải bỏ 'Chưa' cho các tranche đã APPLIED org-wide.

[medium] §27.2 PARTIAL :: §27.2.1/§27.6: Hóa đơn mới mặc định APPROVED, setting tổ chức auto_approve_invoice được server enforce trên MỌI create path; missing row = abort.  || FIX: Wire useCreateInvoice + Excel bulk qua create_invoice_v1, rồi revoke direct INSERT / siết invoices_insert_rbac để không client path nào insert được invoice row mang status bypass setting. Chứng minh mọi create path (dialog + excel + auto-create-after-contract) tôn trọng auto_approve_invoice.

[medium] §27.4 PARTIAL :: Bước 6-8 gate cutover: drain writer cũ, revoke direct DML đúng domain/signature; reconcile post-state; nếu ledger mới đã post thì forward-fix/counter-entry, không xoá.  || FIX: Chọn một trong hai để đóng mâu thuẫn 'flag ON + legacy callable': (a) hạ flag về SHADOW/OFF nếu muốn giữ parachute callable; hoặc (b) đi trọn gate 27.4 bước 6-8 theo từng domain — drain writer cũ, REVOKE EXECUTE đúng signature cho từng legacy writer money, reconcile post-state, và chỉ forward-fix/counter-entry nếu ledger mới đã post. Ưu tiên payment trước (canonical v4 đã canary) rồi lần lượt income-expense/invoice/cashbook/salary/contract-deposit, mỗi domain một maintenance window + reconcile riêng.

[low] §21 P2 MISSING :: P2: permission cache 5 phút phải có version invalidation; JSONB permission normalized/FK; org-global resources bỏ visible-owner graph; AdminOnlyRoute tách platform/tenant  || FIX: Hoàn tất T2 core (resolver v3 consume role_permissions normalized + deny-precedence bỏ first-assignment-wins + cột/version invalidation, wire get_my_permissions sang bảng normalized) và T6a core trước tenant/approval cutover; frontend: thêm version key cho useMyPermissions cache + tách AdminOnlyRoute thành PlatformAdminRoute/TenantAdminRoute.

[low] §21 P3 MISSING :: P3: rename user_id->created_by; voucher-as-ledger -> append-only posting lines; external tamper-evident audit sink  || FIX: Keep deferred to T9 post-cutover (>=90 days stable). No action needed now; low severity.

[low] §11.5 / 11.6 DEFECT :: The exposed/public authorization function must not silently drop a precedence rule (role DENY must beat role ALLOW; overrides must be scoped).  || FIX: Drop public.authorize_v2, HOẶC thay body thành thin wrapper gọi app_private.authorize_tenant_action_v3(auth.uid(),...). Đồng thời sửa mô tả evidence: hàm KHÔNG client-callable (EXECUTE đã revoke khỏi authenticated/anon/public) — đây là latent hygiene risk, không phải live bypass.

[low] §11.6 PARTIAL :: Canonical API authorize(permission_key,org,resource_type,resource_id)->bool and assert_authorized(...)->void raising 42501; helper authorized_building_ids(permission_key,org) for RLS SELECT.  || FIX: Rename/alias the wired path to the contract: expose a public assert_authorized(permission_key,org,resource_type,resource_id) that derives actor from auth.uid(), calls the v3 evaluator, and RAISES 42501 on deny; and a public authorize(...) boolean thin-wrapper (either wire the existing authorize_v2 by granting authenticated + routing writers through it, or drop authorize_v2 to avoid dead-code confusion). Add a new-model authorized_building_ids(permission_key,org) SETOF uuid for RLS SELECT and migrate *_select_rbac policies off the legacy permitted_building_ids(_table,_action). Keep the p_actor variant app_private-only.

[low] §11.2 / 11.0 PARTIAL :: platform_administrators as a separate table (not an org role); prerequisite extensions citext/btree_gist idempotent; historical-episode integrity (exclusion constraint or organization_membership_events).  || FIX: Foundation-hardening slice (not a live authz hole): (1) introduce platform_administrators integrated with the org model OR explicitly document super_admins as the canonical platform table in the plan; (2) citext optional — current text+lower()-unique is plan-sanctioned, no action required; (3) relocate btree_gist to extensions schema or document public-schema resolution; (4) add organization_membership_events append-only OR a tstzrange GiST exclusion constraint (needs btree_gist) to enforce non-overlapping historical episodes.

[low] §12.3 MISSING :: approval_step_approvers per-type null-shape CHECK (MEMBER⇒membership_id not null & rest null; ROLE⇒role_id; PERMISSION⇒permission_key; *_APPROVER⇒scope_id).  || FIX: Add the shape CHECK exactly as plan §12.3 lines 1004-1010: ALTER TABLE public.approval_step_approvers ADD CONSTRAINT approval_step_approvers_shape_check CHECK ((approver_type='MEMBER' AND membership_id IS NOT NULL AND role_id IS NULL AND permission_key IS NULL AND scope_id IS NULL) OR (approver_type='ROLE' AND role_id IS NOT NULL AND membership_id IS NULL AND permission_key IS NULL AND scope_id IS NULL) OR (approver_type='PERMISSION' AND permission_key IS NOT NULL AND membership_id IS NULL AND role_id IS NULL AND scope_id IS NULL) OR (approver_type IN ('CASHBOOK_APPROVER','AREA_APPROVER','BUILDING_APPROVER') AND scope_id IS NOT NULL AND membership_id IS NULL AND role_id IS NULL AND permission_key IS NULL)). Existing 4 rows already satisfy it, so ADD will not fail validation. Optionally also add the missing composite FKs mandated by §12.3.

[low] §12.4 PARTIAL :: approval_request_steps invariant candidate_count >= min_approvals (schema check).  || FIX: Thêm CONSTRAINT TRIGGER DEFERRABLE INITIALLY DEFERRED cưỡng chế candidate_count>=min_approvals lúc commit (cho phép two-phase insert), HOẶC ghi tường minh trong dossier rằng schema check này được thay bằng runtime enforcement trong materialize_step_candidates_v1 (transactional fail-closed) — đây là lệch có chủ đích so với plan 12.4.

[low] §27.6 PARTIAL :: Reconciliation pre/post khớp cho INCOME/EXPENSE, invoice/payment/credit/deposit, salary/profit, cashbook, meter/invoice và operation đối ứng.  || FIX: Chạy reconcile-money.mjs (SUM SQL thật vs canonical) SAU khi mỗi domain có write canonical thật per-org (không chỉ Demo org), gồm cả operation đối ứng của reversal; lưu evidence per-domain trước khi coi 27.6 đạt cho tranche tương ứng. Reconcile 0==0 do 0 traffic không tính là pass.