# Tiếp tục plan Copilot của phiên Claude Code — 06/09/2026

## Nguồn quyết định

Plan gốc trên máy: `C:/Users/Nguyen Tam/.claude/plans/ki-m-tra-nh-gi-adaptive-hearth.md`.
Bản được nhập vào repo: [plan G0–G5](../superpowers/plans/2026-09-02-ai-copilot-va-nen-den-toan-quyen-muc-3.md).
Đã đối chiếu văn bản sau chuẩn hoá xuống dòng: toàn bộ 246 dòng plan gốc khớp nguyên vẹn;
bản repo chỉ thêm 6 dòng giới thiệu. Banner “G0 đang thi công” là ảnh chụp ngày 02/09,
không phải trạng thái triển khai hiện tại. Không sửa plan gốc để làm mất dấu yêu cầu ban đầu.

Mục tiêu là **Mức 3, toàn quyền L5**, với duyệt kế hoạch, PIN step-up và standing grants;
superadmin trước, vai khác sau qua policy/flag. L6 vẫn ngoài phạm vi Copilot.
Mức 2 là bước diễn tập. Tiêu chí nghiệm thu nằm tại từng exit gate của plan gốc.

## Bằng chứng kiểm lại trong phiên này

- Worktree: `C:/Users/Nguyen Tam/whiteboard-copilot-g0`, nhánh `copilot/g0-va-nen`.
- Khi tiếp nhận: HEAD, `origin/main`, `origin/production` và meta `build-sha` của preview
  cùng là `3350509dca0a8f0490582f0a5dd12d00f543728a`.
- [CI Copilot E2E của SHA đó](https://github.com/zxGreenxz/whiteboard-ihomecrm/actions/runs/34005597525)
  có 2 failed / 5 passed ở nhóm smoke–confirmation–draft; nhóm hành động–kế hoạch–L5 có
  20 passed / 10 skipped. Ca 6 và ca 7 L5 đã pass, nhưng toàn workflow vẫn failure.
- Đọc policy thật ngày 06/09: `max_direct_risk=L5`, `standing_grants_enabled=true`,
  `allowed_roles=[superadmin]`, revision 218. Đây không phải bằng chứng mọi org đã được mở:
  phạm vi còn phụ thuộc flag từng hành động.
- Đọc registry thật: 24 action `direct_l5_v1`, 3 L3 nonce, 3 L4 nonce, 1 maker-submit.
  Đây là số đo tại thời điểm tiếp nhận, không phải baseline cố định.
- Ledger DEMO nối với registry `direct_l5_v1`: 11 sự kiện, sớm nhất
  `2026-09-06T01:12:48.761467Z`, muộn nhất `2026-09-06T02:11:25.037359Z`.
  Chưa đủ bằng chứng quan sát hai tuần theo exit gate G5.

## Nguyên nhân hai ca confirmation bị đỏ

Đã tái hiện trên Chromium headless, cùng preview SHA trên:

1. Preference mặc định dùng 9Router: proxy trả HTTP 401, upstream báo thiếu API key.
   Chat chưa chạy được, nên chưa có bước preview/nonce/thẻ. Không sửa cấu hình VPS trong phiên này.
2. Dùng model OpenRouter đang hoạt động, câu test cũ vẫn nhận `toa_nha_khong_thay`:
   test ghi “DEMO A”, tên dữ liệu là “DEMO Toà A”.
3. Với toà “DEMO Toà A”, hạng mục “Xử lý Bồn Cầu” và cùng model OpenRouter,
   preview trả 200 và thẻ xác nhận hiện trên bản build cũ. Không cần sửa component để dựng thẻ.

Bản sửa spec ghim riêng preference model trong phản hồi đọc của browser context, không đổi
preference lưu trên server; mô hình, preview, execute và readback nghiệp vụ vẫn chạy thật.
Test phải nhận phản hồi mô hình hoàn chỉnh, kiểm mọi vòng gọi và lỗi bên trong SSE;
không được dùng “không thấy request ghi” khi mô hình chưa hoạt động làm bằng chứng an toàn.
Phiếu thực sự tạo phải được đọc lại là UNAPPROVED/UNPOSTED và huỷ qua `ie_compat_cancel_v2`
trong `finally`, giữ audit nhưng không để fixture sống.

## Kết quả xác minh bản sửa

- `npm run typecheck:baseline`: đạt, 0 fingerprint lỗi TypeScript.
- Playwright `--list`: nạp được đủ 3 ca; `git diff --check` trên file sửa đạt.
- `npm run graph:detect-changes`: không phát hiện symbol thay đổi; spec E2E nằm ngoài
  graph. Đây không thay thế kết quả chạy test.
- Vitest `run src/copilot --maxWorkers=4`: 45 file / 912 test đạt. Lượt mặc định trước đó
  có hai timeout khi import registry; chạy riêng hai file đạt 91/91, rồi cả bộ đạt với 4 worker.
- Deno 2.9.4: `llm-proxy/index.test.ts` đạt 39/39. Đọc DB thật xác nhận mock disabled,
  finalize có clamp không âm, reserve nhận và dùng `p_organization_id`.
- `npm run gate:truoc-push -- --khong-dao-strict`: 41/41 gate đạt, gồm sinh types/manifests
  từ DB thật. Kết quả này không thay thế E2E hoặc các exit gate G0–G5 còn thiếu.
- Đột biến qua `scripts/dot-bien.mjs`: chèn stream HTTP 200 có `error` và `[DONE]`;
  ca injection đỏ đúng thông điệp `Provider trả lỗi trong stream HTTP 200` (exit 1).
  SHA-256 file `9396056d77ea` → `0b9224ccd442` → `9396056d77ea` sau khôi phục.
  Đây là kiểm khả năng bắt lỗi của harness; không tính là model thật đạt.
- Review độc lập phần diff E2E: không còn phát hiện cần sửa; vẫn cần kết quả chạy thật.
- Trong các lượt chẩn đoán, đã thấy preview/thẻ thật, execute tạo phiếu thật và hai ca
  không ghi đạt. Readback ban đầu lỗi PGRST205 vì PostgREST mặc định schema `api`;
  đã thêm header schema `public` giống client ứng dụng.
- Lượt chạy đầy đủ sau bản sửa cuối: cả 3 ca dừng tại HTTP 403 do hết hạn mức token
  Copilot trong ngày, trước bước preview. **Chưa có bằng chứng cả bộ test cuối cùng đạt**.
  Không thay đổi hạn mức hoặc chuyển tài khoản để vượt chặn.
- Ba phiếu DEMO được tạo trong các lượt chẩn đoán đã huỷ và đọc lại trạng thái CANCELLED.
- Bản sửa được lưu cục bộ, chưa push hay triển khai. Tiếp tục bằng cách chạy lại
  `copilot-confirmation.spec.ts` khi quota được cấp lại, rồi chạy gate trước khi tích hợp.

## Phần phải tiếp tục đối chiếu trước khi gọi hoàn thành toàn bộ

- G4/G5: golden eval với model thật chưa được chứng minh phủ hết. Khoảng trống quota đã ghi tại
  [known gaps](../../tooling/known-gaps.yaml), mục `copilot-golden-eval-real-lane-daily-quota`.
- `scripts/copilot-ledger-audit.mjs` còn danh sách L5 cố định gồm 8 action đợt đầu, trong khi
  registry đang có 24 action L5 trực tiếp. Cần mở độ phủ bộ đối chiếu trước khi dùng nó nghiệm thu G5.
- G5: cần đủ cửa sổ canary hai tuần, không unintended-write/duplicate/wrong-org;
  kiểm báo cáo standing grants hằng ngày và diễn tập rollback/runbook của các action còn lại.
- 9Router hiện chưa hoạt động qua đường mặc định. Test dùng OpenRouter không chứng minh
  nhà cung cấp mặc định đã được phục hồi.
- G2-D/E còn thiếu wrapper room-pass, Zalo mark-sale, metadata sổ quỹ, ghi chú khách/phòng,
  nháp hoá đơn, tạo hợp đồng, trạng thái phòng và tạo khách. Danh sách 31 action hiện tại
  không chứng minh phủ toàn bộ phạm vi được duyệt.
- G5: CHECK thêm trong commit `336476bc` cấm standing grant với **mọi** direct L5,
  trong khi plan gốc chỉ bắt nhóm phân quyền/tổ chức luôn qua PIN. Đã đọc các lượt user
  trong task nhập từ Claude: không tìm thấy quyết định sau đó thu hẹp yêu cầu này.
  Cần sửa bằng migration tiến tới và kiểm chứng quyền/constraints, không coi ADR hẹp hơn
  là bằng chứng user đã bỏ yêu cầu.
- G4: badge “Do AI nộp” đã có component nhưng RPC inbox chưa trả `system_source`;
  G5: báo cáo grant hiện chỉ tải khi mở tab quản trị, chưa có bằng chứng chạy định kỳ.
- Golden “real-model” cũ chỉ đo model chọn tool ở vòng đầu; bốn trường kết quả còn lại
  suy từ câu hỏi. Latency dừng trước khi đọc xong response body và tính cả lượt bị chặn.
  Cần đo toàn bộ luồng thực tế với oracle độc lập; không dùng các số đó để chốt SLA.
- `UNKNOWN_EFFECT`/reconcile đã có thân triển khai từ migration `20260903212610`;
  ghi chú `not_implemented` trong known-gaps/README đã cũ. Phần thiếu là kiểm chứng
  hành vi thực, không phải viết lại thân RPC.
- Những mục được nhật ký cũ ghi “complete” phải đối chiếu tiêu chí plan và bằng chứng tương ứng;
  việc có code, đã apply migration hoặc policy đã bật không tự chứng minh exit gate đạt.

Không mở rộng rollout ngoài DEMO chỉ từ kết quả sửa ba ca confirmation.

## Cập nhật tiếp tục: model và bộ kiểm nhật ký

- User yêu cầu kiểm thử qua 9Router theo thứ tự `ag/gemini-3.6-flash-high(high)`,
  `ag/gemini-3.7-flash-high(high)`, `ag/gemini-3.8-flash(high)`. Ưu tiên 3.6;
  chỉ nâng khi có bằng chứng thiếu năng lực. Không tự fallback khi lỗi transport/quota.
- Kiểm trực tiếp localhost `127.0.0.1:20128/v1`, ngày 06/09/2026 khoảng 15:20 UTC:
  catalog trả 14 model cx/GPT. Hai request không key trả 401, chưa tới provider.
  Sau khi dùng key Codex đã cấu hình (không ghi key vào chứng cứ),
  `cx/gpt-5.4-mini` trả HTTP 200, câu `OK`, finish `stop`, 4335 ms;
  `cx/gpt-5.6-sol` timeout tại 55003 ms. Đây là hai smoke request trực tiếp,
  không phải kết quả bộ nghiệm thu CRM hay bằng chứng mọi model đều hoạt động.
- Gọi đúng `ag/gemini-3.6-flash-high(high)` cùng endpoint/key trả HTTP 404,
  `model_not_found`: `No active credentials for provider: antigravity`.
  Chưa thể đánh giá chất lượng 3.6, không có lý do nâng lên 3.7/3.8 từ lỗi này.
- Bộ audit nhật ký đã tích hợp cục bộ qua `50944180` và `6dde0815`: đọc registry
  hiện hành, phân trang đầy đủ theo thời gian, kiểm bằng chứng L5 và chuỗi external
  queue/reconciliation. Review lại không còn finding; 33/33 test đạt, năm đột biến
  qua helper chính thức bắt đúng lỗi và khôi phục hash. PostgreSQL 17 fixture đạt.
- Migration mới `20260906144028_copilot_ledger_audit_read_v1.sql` đã qua
  idempotency trên catalog production (hai lượt + ROLLBACK), dry-run lane,
  cùng gate stable/ACL/catalog trước triển khai. Chưa apply tại thời điểm ghi mục này.
  Phân loại native fixture và catalog dry-run không thay cho HTTP kiểm quyền thật.
- Gate tích hợp phát hiện hai test node:test bị gán nhầm Vitest; đã sửa wiring
  trong worktree audit, đang review/chạy lại gate. Chưa push/promote.
- G0 đang bổ sung hai bộ test mount component thật và sửa readonly smoke có thể
  xanh nhờ khớp chính câu hỏi user. SLA đề xuất 5s/20s/60s vẫn chưa được user chốt.

### Chỉ đạo mới hơn: dùng sáu model tô xanh

User đã thay lựa chọn Gemini bằng sáu model trong ảnh. Gọi trực tiếp có key trên
localhost đều HTTP 200, nội dung `OK`, finish `stop`:

| Model | Thời gian một request ngắn |
|---|---:|
| `cx/gpt-5.6-luna(max)` | 2178 ms |
| `cx/gpt-5.6-terra(max)` | 4257 ms |
| `cx/gpt-5.6-luna-review(max)` | 3577 ms |
| `cx/gpt-5.6-terra-review(max)` | 2836 ms |
| `cx/gpt-5.5` | 6209 ms |
| `cx/gpt-5.5-review` | 3639 ms |

Default test mới là `9router:cx/gpt-5.6-luna(max)`, không tự chuyển model. Các số này
chỉ là health check trực tiếp, không phải p50/p95 hoặc nghiệm thu CRM. Không đổi
provider/default production. Hai kết nối Antigravity cục bộ đang tắt; yêu cầu dùng
nhóm xanh đã thay thế hướng kiểm thử Gemini trước đó.

Gate tích hợp audit đã đạt 41/41, review wiring CI đạt, commit parent `1e420cd8`.
Hai lần chạy lane apply lúc 15:24 và 15:27 UTC đều dừng ở backup: pg_dump bị đóng
kết nối khi đọc catalog (SSL EOF / server closed connection). **Chưa apply RPC**;
không bỏ backup, chưa push/promote. Kiểm tra psql riêng qua cùng session pooler
với SSL bắt buộc sau đó đọc pg_proc thành công trong 591 ms; điều đó chỉ chứng minh
kết nối ngắn hoạt động, chưa chứng minh một bản dump đầy đủ hoàn tất.

G0 commit `d199fd29` trong worktree riêng có 26 ca mounted và 18 ca oracle,
956/956 Copilot test đạt. Negative proof browser qua preview attested `3350509d`
bắt đúng SSE HTTP 200 error; SHA file `2440a1edf79e` → `2d78ae09836e` →
`2440a1edf79e` được khôi phục. Review phát hiện hai lỗ oracle (mã phòng bịa thêm/
substring và so Markdown thô với DOM); đang fix round 1, chưa tích hợp commit này.

### Chốt bản G0 đã review và trạng thái backup

Các commit G0 đã tích hợp vào nhánh làm việc: `75b4b974`, `e88aa783`, `4a5aafe4`.
Hai vòng sửa review đã xử lý mã phòng bịa/substring, tool result không khớp, link
Markdown và phân biệt cột mã phòng với số liệu giá/diện tích/tầng. Review cuối duyệt;
61/61 test tập trung được reviewer chạy độc lập đạt. Toàn bộ Copilot 968/968 đạt ở
bản vòng 1; vòng 2 thay hai file oracle/test đã chạy focused + typecheck và hai
mutation proof. Không gọi kết quả focused là một lần chạy lại toàn bộ suite.

Default test `9router:cx/gpt-5.6-luna(max)` nằm trong
`.e2e-fleet/specs/copilotTestModel.ts`; sáu model xanh và env override được giữ,
không ghi preference lên server. Chưa có lượt nghiệm thu CRM thật hoàn tất với
cấu hình này. Model health trực tiếp không thay cho xác minh nghiệp vụ.

Lượt backup thứ ba bắt đầu 15:38 UTC với SSL bắt buộc vẫn kẹt ở pha catalog, file
dump 0 byte sau hơn sáu phút. Đã xác minh PID pg_dump thuộc đúng helper/lần migration
này rồi dừng riêng tiến trình đó; helper thoát lỗi và dọn file. Đọc DB sau đó xác
nhận RPC mới chưa tồn tại; không còn file dump lỗi của ba lượt. Không thay đổi schema,
không bỏ backup. Cần sửa đường backup trước khi deploy RPC và chạy HTTP readback.

Sau tích hợp, gate phát hiện hash CRLF của worktree audit khác bytes LF đã commit.
Không đổi SQL: đã sinh lại provenance từ đúng bản LF, SHA-256
`a1430ecb3f5e78c8b0dc488ada916506e75df6461410e09bdab2402e9038c7c6`.
Idempotency trên catalog production đã chạy lại với đúng bytes này: 1/1 đạt,
hai lượt rồi ROLLBACK. Lượt gate tổng hợp gần nhất đạt 40 mục, chỉ đỏ provenance;
sau sinh lại, provenance + unknown-review + doc-counts đều đạt. Chưa push/promote.

### Tiếp tục 16:38 UTC — preview, VPS, backup và HTTP thật

Gate tổng hợp cuối ở `e32df7bc` đạt 41/41. Commit đó đã push lên `origin/main`;
ba workflow CI Gates (34044117655), Migration Validation (34044117515) và
Migration Restore Drill (34044117572) đều success. Preview đã xác nhận đúng SHA.
Nhánh production vẫn ở `3350509d`; chưa promote app.

User chốt VPS cho production, localhost để test sáu model xanh. Đo trực tiếp VPS
Minh: 9Router 0.5.69 và cloudflared đang active; `https://ai.chillhome.io.vn/v1`
trả models 200 với key VPS, 401 khi không có key. VPS có một tài khoản Antigravity
active, một inactive, chưa có Codex. Localhost vẫn giữ nhóm Codex. Chưa chuyển
tài khoản: đang chờ chọn chuyển riêng một tài khoản cx hoặc đăng nhập bộ riêng.
Không chạy cùng một tài khoản trên hai instance. Supabase có đúng base URL nhưng
digest API key không khớp key VPS hiện hành; chưa đổi secret/default production.

Backup chẩn đoán có quan sát kết nối đã thành công sau 238s, 519 TABLE DATA,
không bỏ bảng. File 0 byte tới 148s trong khi truy vấn catalog vẫn tiến triển;
đến 161s đã ghi dữ liệu. `ClientRead` riêng lẻ không chứng minh treo. Nguyên nhân
ba lần đứt kết nối trước chưa được xác định; không đổi timeout hay đường backup.

Lane migration chính thức chạy ở worktree sạch `copilot-ledger-apply-e32`, đúng
SQL LF đã review. Backup MỚI trong chính lượt apply hoàn tất 150s, 519 TABLE DATA,
SHA `2e8624da442c72425bf407b59ae5e0f97156084a8f595aa42d6696f27a25648d`.
RPC `copilot_ledger_audit_page_v1` đã apply lúc 16:29:52 UTC. HTTP thật:
anonymous 401, người dùng không phải super admin 403, super admin 200. Thân hàm
server khớp từng byte với SQL đã review; 31 registry, 24 hành động L5.

Đối chiếu cửa sổ 23/08–06/09 đọc 632 dòng ledger và 100 audit; không thấy ghi
ngoài ý muốn, trùng, nhầm org hay nhầm actor. Kết quả vẫn `incomplete`: 33 audit
dùng tên cũ `tao_phieu_thu_chi_nhap`, trong khi ledger/registry đã dùng canonical
`income_expense.create_draft` (L4). Đã truy ra phép join so thẳng hai tên khác
nhau. Chưa bỏ qua các dòng này, chưa sửa lịch sử; cần sửa read-side mapping rồi
đo lại. Truy vấn lịch sử không chứng minh đủ thời lượng canary đang hoạt động.

Phát hiện riêng: runner gọi collector catalog ở chế độ chỉ in rồi đọc inventory
cũ trên đĩa. Các fingerprint before/after trong evidence apply vì vậy KHÔNG hợp
lệ. Đã sửa collector call thêm `--write`, kiểm regression 11/11 và đột biến
chính thức bắt đúng stale fingerprint, review độc lập duyệt. Commit parent
`468e594f`. Readback mới sinh fingerprint
`e47d6122772238c045511c5aaec866117d7dbb6781706c6291e8082a51bb74f2`;
không dùng nó để giả tạo lại before snapshot đã mất. Chi tiết máy đọc:
`docs/generated/copilot-ledger-readback-2026-09-06.json`.

Audit G1 theo nghĩa nghiệp vụ chỉ xác nhận 20/42 module có đường đọc ở source;
cần ít nhất 14 module thêm để đạt ngưỡng 80%. Chưa mở triển khai tính năng G1
trước exit G0. Bản sửa nền golden real-flow ở worktree riêng mới có executor
C01/C13; 73 executor còn thiếu, không phải chỉ thiếu credential. Review đang
sửa ràng buộc đúng toà và dừng khi quota nằm trong SSE HTTP 200. Chưa tích hợp,
chưa có nghiệm thu thật đủ 75 ca. SLA và positive CRM model run vẫn chưa chốt.

### Chỉ đạo mới nhất — Gemini trên VPS, cx bổ sung sau

User đã chốt dùng `ag/gemini-3.6-flash-high(high)`,
`ag/gemini-3.7-flash-high(high)`, `ag/gemini-3.8-flash(high)` trên VPS; cx sẽ
tự bổ sung sau. Chỉ đạo này thay thế câu hỏi chuyển tài khoản và lựa chọn cx
cho CRM/VPS. Giữ ưu tiên 3.6; không tự nâng model vì lỗi mạng/quota.

Đo 16:47:46 UTC: đúng ID 3.6(high) trên public VPS trả HTTP 200, nội dung `OK`,
finish `stop`, 5673ms; upstream báo `gemini-3.6-flash-tiered`. Đây chỉ là health
check ngắn, không phải p50/p95, chất lượng nghiệp vụ hay toàn bộ nghiệm thu CRM.
Hai model còn lại có trong listing; chưa kiểm thử chất lượng và chưa nâng sang.

Đã đồng bộ riêng `NINEROUTER_API_KEY` trên Supabase lúc 16:52:46 UTC (POST 201,
đọc lại digest khớp key VPS); base URL giữ nguyên. Không chuyển tài khoản.
Danh sách model DB vẫn là cx tại mốc này, đang sửa trong worktree
`copilot-vps-gemini`: migration cấu hình có backup trước apply, default 3.6,
test preference và xử lý preference cũ dựa trên model còn được server cho phép.
Chưa tuyên bố CRM đã gọi được Gemini trước khi đường này được đo thật.

Gate sau đồng bộ schema/provenance đạt 41/41 (73s), typecheck 0 fingerprint.
Provenance đã ghi biên nhận apply của RPC audit; bỏ ghi chú pre-apply lỗi thời.
Golden foundation đã sửa hai lỗi review cũ; còn một lỗi địa chỉ dự phòng của
oracle đang chờ sửa. Chưa tích hợp foundation và chưa coi 73 ca thiếu là hoàn tất.

### Cập nhật 07/09 lúc 00:56 giờ Việt Nam — cấu hình đã áp dụng, G0 còn lỗi stream

Các mốc dưới đây thay thế trạng thái đang chờ ở đoạn trước; giữ nguyên plan gốc.
Canonical worktree vẫn là `C:/Users/Nguyen Tam/whiteboard-copilot-g0`, nhánh
`copilot/g0-va-nen`. HEAD tích hợp `b6dd353a`; `origin/main` ở `a28aced5`,
production vẫn `3350509d`. Các commit mới chưa push/promote.

Migration `20260906165423_copilot_9router_vps_gemini_v1.sql` đã áp dụng lúc
17:18:33 UTC ngày 06/09, sau backup full mới trong cùng lượt apply (519 bảng có
dữ liệu, 185s). Đọc lại xác nhận đúng ba model ag/Gemini theo chỉ đạo, default
3.6; chín provider khác giữ nguyên toàn bộ dòng. Biên nhận và readback ở
`docs/generated/schema-change-evidence/20260906165423_copilot_9router_vps_gemini_v1.json`
và `docs/generated/copilot-gemini-vps-readback-2026-09-07.json`. Frontend xử lý
preference cx cũ đã tích hợp, chưa phát hành app mới.

Sau quota reset tự nhiên 17:00 UTC, chạy thật headless trên preview `a28aced5`
bằng 3.6. Ca G0 đầu tiên đã qua hai vòng model: 13.512 và 14.036 token, upstream
2.950ms và 2.451ms, nhưng harness dừng vì `Stream missing DONE`; ba ca còn lại
chưa chạy. Không bấm chấp nhận và không có ghi nghiệp vụ. Không tính thời gian
toàn test (21,679s, gồm đăng nhập) thành SLA model.

Probe stream nhỏ trực tiếp trên VPS cũng trả HTTP 200, finish `stop` và usage,
nhưng thiếu `[DONE]` khi EOF bình thường (1.861ms). Đọc source bản 9Router 0.5.69
đang cài xác nhận nhánh Gemini/antigravity bỏ sentinel này. Đây là lỗi tương
thích giao thức; chưa có bằng chứng cần nâng 3.7/3.8. Đang sửa adapter hẹp ở
`llm-proxy`, chỉ bổ sung sentinel khi EOF bình thường và terminal hợp lệ,
không biến lỗi/quota/timeout/abort thành thành công. Giữ oracle nghiêm ngặt.
Readback Edge trước sửa: version 32, verify_jwt=true, index.ts SHA-256
`4a26f7bc8ad51ba69ea242c584e1f534962fcba0333253d10e79d818713d44b3`.

Nền golden real-flow đã tích hợp C01/C13 và các sửa review. Canonical strict
`typecheck:e2e` từng lộ lỗi khai báo mjs; vòng sửa tiếp theo bổ sung khai báo và
runtime guards, được review độc lập duyệt tại `4cdcd8a5` (parent `b6dd353a`).
Kiểm ở worktree sửa: typecheck/matrix xanh, 43 Node/browser và 47 oracle xanh,
hai mutation bị bắt đúng và khôi phục digest. Nền vẫn chỉ có hai executor;
73 ca chưa triển khai. Chưa có SLA được người dùng chốt hay nghiệm thu 75 ca.

Migration read-side `20260906170939_copilot_ledger_legacy_identity_v1.sql` đã áp
dụng 17:47:11 UTC sau backup full mới 156s, 519 bảng có dữ liệu, SHA-256
`0c0eb422bf6eb7861f504c8fb82e013d5171cf1326774a7aa9ef1e78bb185e1e`.
Hai thân hàm khớp SQL đã review; helper private không có quyền execute cho
anon/authenticated/service_role; HTTP reader thật: 401/403/200 đúng vai.

Cửa sổ cố định 23/08–06/09 16:30:18.810 UTC vẫn có 632 ledger và 100 audit.
Đã kiểm từng ID của 33 audit tên cũ: tất cả ánh xạ đúng canonical L4 và có đúng
một execution. Kiểm chặt hơn lộ 17 ledger replay thiếu audit_id, tạo 34 findings
(17 thiếu audit + 17 thiếu execution gốc); vẫn `incomplete`, không kết luận
canary xanh. Bản trước chỉ lưu tổng và findings, không có toàn bộ RPC pages,
nên không tuyên bố đã so từng byte mọi dòng trước/sau. Bằng chứng:
`docs/generated/copilot-ledger-identity-readback-2026-09-07.json`.

Đọc lại producer đang chạy lúc 18:04:35 UTC: legacy, wrapper và plan executor
đều khớp từng byte body trong migration nguồn. Legacy replay đã kiểm audit gốc
nhưng bỏ `audit_id` khỏi kết quả trả về. Số execution bằng 0 trong reader có
nghĩa không chứng minh được liên kết, không phải đã chứng minh execution gốc
không tồn tại. Cần sửa riêng kết quả replay cho tương lai; không backfill 17
dòng cũ. Chi tiết: `docs/generated/copilot-replay-producer-readback-2026-09-07.json`.

Xác minh tích hợp ở parent: `typecheck:e2e` xanh, `typecheck:baseline` 0
fingerprint, build 4.993 module / 23,93s, `gate:bundle` 509 chunk / 97 trang
lazy xanh. `gate:truoc-push` chạy đầy đủ cả strict và mọi gate tĩnh xanh; lượt
đầu chỉ đỏ phần đếm trong INDEX do tài liệu đã bẩn sẵn từ generator trước.
Đã stage đúng bốn tài liệu do task sở hữu rồi chạy lại
`check-doc-counts --nguon-index`: 18/18 khớp. Không sửa migration EOL lịch sử.
Catalog mới đọc lại có 1.495 hàm, fingerprint
`96470e534ddf41f8c47e42b94110e268d2b1ea694836b17a8d2fd439739641b5`.


### Cập nhật 2026-09-06T18:55:35.454Z — Gemini 3.6 đã có bằng chứng G0

Canonical HEAD là 2d605589; origin/main f498ca80 đã qua ba workflow CI và
Preview khớp SHA. App production vẫn 3350509d. Localhost dành cho kiểm thử;
VPS dùng đúng ba model ag/Gemini, mặc định 3.6, chưa nâng 3.7/3.8.

Adapter DONE được review độc lập và deploy Edge 33. Ba ca xác nhận G0 đã
chạy thật thành công; ca đọc ban đầu vướng banner rồi phép đối chiếu text.
Probe Chromium cục bộ xác nhận Response.text đọc sai tiếng Việt khi SSE
thiếu charset, trong khi TextDecoder của app đọc đúng cùng byte UTF-8.
Bản sửa Content-Type đã qua 77/77 Deno và hai mutation, được review rồi
deploy Edge 34 lúc 2026-09-06T18:43:22.558Z. Readback khớp source SHA-256
99c09b46abcf0b9a253b4fdb59877846ae6933d18ee3d8e8b9ea96d25781759c,
verify_jwt=true. Ca đọc G0 nguyên bản sau đó xanh: hai vòng model, RPC
copilot_available_rooms_v1 thật trên DEMO, không ghi nghiệp vụ. Tổng bốn ca
đã có kết quả xanh qua hai lượt; chưa có một lượt đầy đủ cả bốn xanh.
Không tính thời gian toàn test thành SLA phản hồi. Evidence máy đọc ở
docs/generated/copilot-g0-gemini-real-smoke-2026-09-07.json và các receipt
copilot-gemini-stream-deploy-*; bản version33 được lưu riêng.

Sửa replay tương lai (08eba81f, parent f9866c02) chỉ trả thêm audit_id gốc
đã xác thực, không thêm execution hay sửa 17 ledger lịch sử thiếu liên kết.
Migration 20260906182318_copilot_ledger_replay_audit_id_v1.sql đã được review,
sáu PGlite test xanh, production preflight idempotency/lock/frozen-ledger
xanh. Official dry-run đã ROLLBACK và readback body/metadata giữ nguyên.
Apply tree 9519ad62 đang chạy backup mới qua apply-reviewed-migration.mjs;
chưa coi là đã apply trước khi có receipt và readback. Live DEMO nonce
replay/plan propagation đang được chuẩn bị riêng, không tốn model quota.

Snapshot quota lúc 2026-09-06T18:47:21Z: 204846/300000 token cho tài khoản
DEMO chủ nhà trong ngày 07/09. Chưa đổi cap hay tài khoản. SLA vẫn chờ chọn;
golden foundation chỉ có C01/C13, còn 73 executor. Lỗi so timestamp snapshot
với thời điểm trước RPC đã tái hiện offline, chưa sửa; chưa quy nó là nguyên
nhân của lượt đọc đã xanh. Toàn bộ G0–G5/canary vẫn chưa nghiệm thu xong.

Migration replay đã áp dụng lúc 2026-09-06T18:56:48.127Z qua lane chính thức sau
backup full mới (519 bảng có dữ liệu). Readback xác nhận thân
legacy mới 039be505… và toàn bộ owner/ACL/volatility/search_path giữ nguyên;
wrapper và plan executor không đổi. Biên nhận: docs/generated/schema-change-evidence/20260906182318_copilot_ledger_replay_audit_id_v1.json.
Kiểm chứng nonce/plan thực tế trên DEMO đang chờ; 17 liên kết lịch sử vẫn thiếu.

Kiểm chứng replay thực tế hoàn tất lúc 2026-09-06T19:14:36.227Z: nonce cũ bị
chặn đúng confirmation_already_used; nonce mới cho canonical giống hệt và
luồng public plan create/approve/execute/get đều giữ đúng audit/entity gốc.
Chỉ một action_executed; sau ROLLBACK, truy vấn độc lập theo ID xác nhận cả
sáu nhóm phiếu/item/audit/ledger/plan/step đều bằng 0. Không có ghi nghiệp vụ
đã commit. Đây là real RPC dưới authenticated role qua Management API trong
transaction, chưa phải browser/PostgREST E2E. Bằng chứng: docs/generated/copilot-replay-audit-id-live-proof-2026-09-07.json.

### Cập nhật 2026-09-06T19:27:41.557Z — replay đã kiểm chứng, sửa timestamp đã tích hợp

Parent HEAD `a181baa0` tích hợp bản sửa timestamp `1ff98fef` đã được review độc lập.
`fetchCopilotAvailability` lấy mốc thời gian sau RPC, giữ nguyên hạn 60 giây,
chặn snapshot tương lai, sai org hoặc lỗi. 25/25 test xanh; TDD bắt đúng hai lỗi
cũ; mutation đã bị bắt và khôi phục. TypeScript app có 0 fingerprint, E2E types
xanh; build 4.993 module trong 15,67 giây và bundle 509 chunk / 97 trang lazy xanh.

Main `75042fc1` đã xanh cả CI Gates 34053486924, Migration Validation 34053486986
và Restore Drill 34053486962. Lượt Restore đầu lỗi connection refused trước
khi nạp SQL; chạy lại riêng job không đổi mã đã xanh. Preview khớp `75042fc1`;
app production vẫn `3350509d`. Bằng chứng CI nằm ở
docs/generated/copilot-gemini-replay-ci-2026-09-07.json.

Replay đã kiểm chứng thành công lúc 19:14:36 UTC (đoạn trên có chi tiết),
không còn chờ chạy. 17 liên kết lịch sử vẫn thiếu, không có backfill.
Bản sửa timestamp đang chờ push cùng bằng chứng. Hai ca injection nâng cao
đang được sửa ở worktree `copilot-live-injection-proof`: ghim 3.6, đòi model trả
lời thành công, dọn chính xác kế hoạch DEMO do test tạo. Bản cũ chỉ chờ cố
định 20/25 giây rồi kiểm không có RPC cấm, nên có thể xanh dù model bị quota/mạng
chặn. Chưa chạy model thêm; SLA vẫn chưa được chốt, 73 executor golden còn
thiếu, G0 còn chờ workflow thực chạy bản mới.


### Cập nhật 2026-09-06T19:52:57.403Z — rà soát fixture và phạm vi DENY

Bản kiểm thử injection đầu tiên `1430fb0d` chưa tích hợp: review độc lập yêu cầu sửa hai điểm. Không được hủy kế hoạch cũ chỉ vì RPC trả thành công (`da_ton_tai=true`); khi timeout phải thu đủ phản hồi tạo đang dở trước khi dọn. Agent đang sửa trong worktree `copilot-live-injection-proof`, chỉ chạy transport giả lập cục bộ, không gọi model hay DB thật.

Đã lưu bằng chứng bản sửa timestamp đã review và kiểm thử: docs/generated/copilot-availability-arrival-time-2026-09-07.json. Parent vẫn `a181baa0`; main `75042fc1` và production `3350509d` chưa đổi.

Rà soát foundation phát hiện helper `copilot_org_scope_buildings_v1` mở rộng `org_wide` thành mọi tòa, có thể đưa lại tòa bị DENY mà resolver đã loại. Catalog Supabase lúc 19:45:50 UTC xác nhận 32/32 hàm gọi cùng helper/resolver khớp source, không có sai lệch. Đây là bằng chứng mã đang triển khai, chưa chứng minh rò dữ liệu thực tế. Worktree `copilot-building-deny-scope` và brief đã chuẩn bị; chưa triển khai. Graph đã fresh ở a181baa0 nhưng không tìm thấy symbol SQL, risk UNKNOWN; không dùng kết quả đó để tuyên bố an toàn.


### Cập nhật 2026-09-06T20:08:07.340Z — bốn ca injection đã review và tích hợp

Parent `fd385cff` đã tích hợp chuỗi source `1430fb0d`, `acb16c65`, `ae937078` sau review độc lập chấp thuận. Hai lỗi cleanup P1/P2 đã giải quyết: chỉ hủy DRAFT vừa tạo có bằng chứng; giữ và chờ các request tạo đang dở; kết quả transport không rõ phải báo thất bại. Bỏ marker không làm sót fixture đã chứng minh sở hữu; một lỗi không ngăn dọn các fixture hợp lệ khác.

Kiểm lại toàn workflow tìm thêm ca L5 PIN injection và draft-matrix injection ngoài hai ca ban đầu. Cả bốn hiện ghim Gemini 3.6 bằng phản hồi GET riêng trong browser, chọn DEMO, kiểm model/org thực gửi và đòi một vòng model hoàn chỉnh. Không thay profile, PIN hay quota. 13/13 kiểm thử transport/cleanup cục bộ xanh; E2E TypeScript xanh ở cả source và cây tích hợp; bốn spec liệt kê được 33 test, chưa coi là chạy live. Bằng chứng: docs/generated/copilot-live-injection-verification-2026-09-07.json.

Workflow giữ nguyên concurrency group và chuyển cancel-in-progress=false, để lượt mới không hủy lượt đang dọn DEMO. Source 43cee6af đã được review riêng; YAML và hai gate workflow/test-matrix xanh. Đang chuẩn bị gate tích hợp trước push, sau đó mới gọi workflow thật trên đúng Preview SHA. Main vẫn 75042fc1; production vẫn 3350509d.

Bản sửa foundation DENY là nhiệm vụ triển khai duy nhất đang chạy ở worktree copilot-building-deny-scope; chỉ dùng SQL cục bộ, chưa có migration live hay G1 tool mới. Toàn bộ plan vẫn chưa nghiệm thu.


### Tiếp tục 07/09/2026 — sửa log workflow trước exit gate G0

Đã đối chiếu trực tiếp plan gốc `ki-m-tra-nh-gi-adaptive-hearth.md`: mục tiêu vẫn Mức 3 / L5, L6 ngoài phạm vi; không bỏ qua exit gate và thời gian canary. Phần đang xử lý thuộc G0-D.

Worktree `codex-worktrees/copilot-live-injection-workflow-r2` tiếp nhận commit 317c347a + 188201da. Review R3 xác nhận ba lỗi Important và hai lỗi Minor đã xử lý, nhưng header Digest/escaped quote còn lọt đuôi credential. Bản tiếp theo che cả phần còn lại của dòng và chặn luồng log nếu giá trị Authorization bị đẩy xuống dòng sau. Test mới đã đỏ trước sửa; cả bộ Chromium cục bộ đạt 27/27 không skip; bốn đột biến của hai lớp bảo vệ đều bị bắt và khôi phục hash. Review cuối đã thông qua, 41/41 gate trước push đạt trong 148 giây; CI tích hợp và nghiệm thu live còn chờ.

API lúc tiếp nhận: main/Preview trước đó 2caa5438, production 3350509d, workflow 348581418 vẫn disabled_manually. Quota DEMO ngày 07/09 là 300971/300000 token/người; không thay cap, model, PIN hay policy và chưa dispatch lượt live mới. G0 chưa được nghiệm thu; toàn bộ G0–G5 chưa hoàn thành. Bằng chứng sửa log được nối vào `docs/generated/copilot-live-injection-verification-2026-09-07.json`, mục workflowSafetyRepair.
