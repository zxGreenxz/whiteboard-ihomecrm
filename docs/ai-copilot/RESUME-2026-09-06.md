# Tiếp tục plan Copilot của phiên Claude Code — 06/09/2026

## Nguồn quyết định

Plan gốc trên máy: `C:/Users/Nguyen Tam/.claude/plans/ki-m-tra-nh-gi-adaptive-hearth.md`.
Bản được nhập vào repo: [plan G0–G5](../superpowers/plans/2026-09-02-ai-copilot-va-nen-den-toan-quyen-muc-3.md).
Đã đối chiếu văn bản sau chuẩn hoá xuống dòng: toàn bộ 246 dòng plan gốc khớp nguyên vẹn;
bản repo chỉ thêm 6 dòng giới thiệu. Banner “G0 đang thi công” là ảnh chụp ngày 02/09,
không phải trạng thái triển khai hiện tại. Không sửa plan gốc để làm mất dấu yêu cầu ban đầu.

Mục tiêu là **Mức 3, toàn quyền L5**, với duyệt kế hoạch, PIN step-up và standing grants;
superadmin trước, vai khác sau qua policy/flag. L6 vẫn ngoài phạm vi Copilot.
Mức 2 là bước diễn tập. Tiêu chí nghiệm thu nằm tại từng exit gate của plan gốc.

## Bằng chứng kiểm lại trong phiên này

- Worktree: `C:/Users/Nguyen Tam/whiteboard-copilot-g0`, nhánh `copilot/g0-va-nen`.
- Khi tiếp nhận: HEAD, `origin/main`, `origin/production` và meta `build-sha` của preview
  cùng là `3350509dca0a8f0490582f0a5dd12d00f543728a`.
- [CI Copilot E2E của SHA đó](https://github.com/zxGreenxz/whiteboard-ihomecrm/actions/runs/34005597525)
  có 2 failed / 5 passed ở nhóm smoke–confirmation–draft; nhóm hành động–kế hoạch–L5 có
  20 passed / 10 skipped. Ca 6 và ca 7 L5 đã pass, nhưng toàn workflow vẫn failure.
- Đọc policy thật ngày 06/09: `max_direct_risk=L5`, `standing_grants_enabled=true`,
  `allowed_roles=[superadmin]`, revision 218. Đây không phải bằng chứng mọi org đã được mở:
  phạm vi còn phụ thuộc flag từng hành động.
- Đọc registry thật: 24 action `direct_l5_v1`, 3 L3 nonce, 3 L4 nonce, 1 maker-submit.
  Đây là số đo tại thời điểm tiếp nhận, không phải baseline cố định.
- Ledger DEMO nối với registry `direct_l5_v1`: 11 sự kiện, sớm nhất
  `2026-09-06T01:12:48.761467Z`, muộn nhất `2026-09-06T02:11:25.037359Z`.
  Chưa đủ bằng chứng quan sát hai tuần theo exit gate G5.

## Nguyên nhân hai ca confirmation bị đỏ

Đã tái hiện trên Chromium headless, cùng preview SHA trên:

1. Preference mặc định dùng 9Router: proxy trả HTTP 401, upstream báo thiếu API key.
   Chat chưa chạy được, nên chưa có bước preview/nonce/thẻ. Không sửa cấu hình VPS trong phiên này.
2. Dùng model OpenRouter đang hoạt động, câu test cũ vẫn nhận `toa_nha_khong_thay`:
   test ghi “DEMO A”, tên dữ liệu là “DEMO Toà A”.
3. Với toà “DEMO Toà A”, hạng mục “Xử lý Bồn Cầu” và cùng model OpenRouter,
   preview trả 200 và thẻ xác nhận hiện trên bản build cũ. Không cần sửa component để dựng thẻ.

Bản sửa spec ghim riêng preference model trong phản hồi đọc của browser context, không đổi
preference lưu trên server; mô hình, preview, execute và readback nghiệp vụ vẫn chạy thật.
Test phải nhận phản hồi mô hình hoàn chỉnh, kiểm mọi vòng gọi và lỗi bên trong SSE;
không được dùng “không thấy request ghi” khi mô hình chưa hoạt động làm bằng chứng an toàn.
Phiếu thực sự tạo phải được đọc lại là UNAPPROVED/UNPOSTED và huỷ qua `ie_compat_cancel_v2`
trong `finally`, giữ audit nhưng không để fixture sống.

## Kết quả xác minh bản sửa

- `npm run typecheck:baseline`: đạt, 0 fingerprint lỗi TypeScript.
- Playwright `--list`: nạp được đủ 3 ca; `git diff --check` trên file sửa đạt.
- `npm run graph:detect-changes`: không phát hiện symbol thay đổi; spec E2E nằm ngoài
  graph. Đây không thay thế kết quả chạy test.
- Vitest `run src/copilot --maxWorkers=4`: 45 file / 912 test đạt. Lượt mặc định trước đó
  có hai timeout khi import registry; chạy riêng hai file đạt 91/91, rồi cả bộ đạt với 4 worker.
- Deno 2.9.4: `llm-proxy/index.test.ts` đạt 39/39. Đọc DB thật xác nhận mock disabled,
  finalize có clamp không âm, reserve nhận và dùng `p_organization_id`.
- `npm run gate:truoc-push -- --khong-dao-strict`: 41/41 gate đạt, gồm sinh types/manifests
  từ DB thật. Kết quả này không thay thế E2E hoặc các exit gate G0–G5 còn thiếu.
- Đột biến qua `scripts/dot-bien.mjs`: chèn stream HTTP 200 có `error` và `[DONE]`;
  ca injection đỏ đúng thông điệp `Provider trả lỗi trong stream HTTP 200` (exit 1).
  SHA-256 file `9396056d77ea` → `0b9224ccd442` → `9396056d77ea` sau khôi phục.
  Đây là kiểm khả năng bắt lỗi của harness; không tính là model thật đạt.
- Review độc lập phần diff E2E: không còn phát hiện cần sửa; vẫn cần kết quả chạy thật.
- Trong các lượt chẩn đoán, đã thấy preview/thẻ thật, execute tạo phiếu thật và hai ca
  không ghi đạt. Readback ban đầu lỗi PGRST205 vì PostgREST mặc định schema `api`;
  đã thêm header schema `public` giống client ứng dụng.
- Lượt chạy đầy đủ sau bản sửa cuối: cả 3 ca dừng tại HTTP 403 do hết hạn mức token
  Copilot trong ngày, trước bước preview. **Chưa có bằng chứng cả bộ test cuối cùng đạt**.
  Không thay đổi hạn mức hoặc chuyển tài khoản để vượt chặn.
- Ba phiếu DEMO được tạo trong các lượt chẩn đoán đã huỷ và đọc lại trạng thái CANCELLED.
- Bản sửa được lưu cục bộ, chưa push hay triển khai. Tiếp tục bằng cách chạy lại
  `copilot-confirmation.spec.ts` khi quota được cấp lại, rồi chạy gate trước khi tích hợp.

## Phần phải tiếp tục đối chiếu trước khi gọi hoàn thành toàn bộ

- G4/G5: golden eval với model thật chưa được chứng minh phủ hết. Khoảng trống quota đã ghi tại
  [known gaps](../../tooling/known-gaps.yaml), mục `copilot-golden-eval-real-lane-daily-quota`.
- `scripts/copilot-ledger-audit.mjs` còn danh sách L5 cố định gồm 8 action đợt đầu, trong khi
  registry đang có 24 action L5 trực tiếp. Cần mở độ phủ bộ đối chiếu trước khi dùng nó nghiệm thu G5.
- G5: cần đủ cửa sổ canary hai tuần, không unintended-write/duplicate/wrong-org;
  kiểm báo cáo standing grants hằng ngày và diễn tập rollback/runbook của các action còn lại.
- 9Router hiện chưa hoạt động qua đường mặc định. Test dùng OpenRouter không chứng minh
  nhà cung cấp mặc định đã được phục hồi.
- G2-D/E còn thiếu wrapper room-pass, Zalo mark-sale, metadata sổ quỹ, ghi chú khách/phòng,
  nháp hoá đơn, tạo hợp đồng, trạng thái phòng và tạo khách. Danh sách 31 action hiện tại
  không chứng minh phủ toàn bộ phạm vi được duyệt.
- G5: CHECK thêm trong commit `336476bc` cấm standing grant với **mọi** direct L5,
  trong khi plan gốc chỉ bắt nhóm phân quyền/tổ chức luôn qua PIN. Đã đọc các lượt user
  trong task nhập từ Claude: không tìm thấy quyết định sau đó thu hẹp yêu cầu này.
  Cần sửa bằng migration tiến tới và kiểm chứng quyền/constraints, không coi ADR hẹp hơn
  là bằng chứng user đã bỏ yêu cầu.
- G4: badge “Do AI nộp” đã có component nhưng RPC inbox chưa trả `system_source`;
  G5: báo cáo grant hiện chỉ tải khi mở tab quản trị, chưa có bằng chứng chạy định kỳ.
- Golden “real-model” cũ chỉ đo model chọn tool ở vòng đầu; bốn trường kết quả còn lại
  suy từ câu hỏi. Latency dừng trước khi đọc xong response body và tính cả lượt bị chặn.
  Cần đo toàn bộ luồng thực tế với oracle độc lập; không dùng các số đó để chốt SLA.
- `UNKNOWN_EFFECT`/reconcile đã có thân triển khai từ migration `20260903212610`;
  ghi chú `not_implemented` trong known-gaps/README đã cũ. Phần thiếu là kiểm chứng
  hành vi thực, không phải viết lại thân RPC.
- Những mục được nhật ký cũ ghi “complete” phải đối chiếu tiêu chí plan và bằng chứng tương ứng;
  việc có code, đã apply migration hoặc policy đã bật không tự chứng minh exit gate đạt.

Không mở rộng rollout ngoài DEMO chỉ từ kết quả sửa ba ca confirmation.
