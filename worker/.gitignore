# Artifact tải về / sinh ra khi chạy — không phải mã nguồn.

# Bộ font cho ảnh bảng phòng trống. Tải bằng `node worker/tai-font.mjs`.
# KHÔNG commit: ~670KB nhị phân, và mỗi máy tự tải được trong vài giây.
fonts/

# Ảnh do `node worker/kiem-anh.mjs` ghi ra để người vận hành soi bằng mắt.
kiem-anh-phong-trong.png

# Cookie phiên Zalo của công ty, mã hóa AES-256-GCM (xem lib/session-store.js).
# Là BÍ MẬT runtime — commit lên git là chuyển sang chế độ "chỉ an toàn nhờ key" vĩnh viễn.
sessions/
