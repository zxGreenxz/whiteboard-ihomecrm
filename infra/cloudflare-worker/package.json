{
  "name": "ihome-storage-worker",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "private": true,
  "description": "Cloudflare Worker - cổng lưu trữ R2 (upload/sign) cho ihomecrm",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "wrangler": "^4.105.0"
  }
}
