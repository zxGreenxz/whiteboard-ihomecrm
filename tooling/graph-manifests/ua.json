{
  "$comment": "Hộ chiếu cho graph Understand Anything trong .ua/ — PROJECT_CONTRACT §12 luật #3 (mỗi graph phải ghi baseCommit, analyzedAt, scope, tool version, config digest). Công cụ UA KHÔNG tự ghi scope/toolVersion/configDigest, nên sidecar này khai thay, và scripts/check-graph-hygiene.mjs cưỡng chế nó KHỚP artifact: baseCommit phải bằng cả ba nguồn trong .ua/ (meta.json, fingerprints.json top-level, knowledge-graph.json→project), analyzedFiles phải bằng số khoá trong fingerprints.files, configDigest phải bằng git blob OID hiện tại. Sai bất kỳ chỗ nào ⇒ đỏ. Một manifest không ai đối chiếu thì chỉ là lời khai.",
  "schemaVersion": 1,
  "graph": "ua",
  "artifactDir": ".ua",
  "baseCommit": "20c8f3911c152bc74427a63408c8a90367f8e9a1",
  "analyzedAt": "2026-09-02T02:30:33.462412Z",
  "analyzedFiles": 3091,
  "scope": {
    "description": "Toàn repo tại 20c8f391 (02/09/2026, sau đợt đại tu dọn dẹp: xóa 26 file orphan src, .kiro/, toàn bộ OpenClaw code từ 30/08) trừ các mẫu trong .ua/.understandignore (lần này loại 9 file — nhóm vendor/runtime-state/data-dump khai 08/08 ĐÃ ÁP DỤNG). Dựng lại TOÀN BỘ (--full --language vi), không tăng dần: 3.091/3.091 file quét đều có node cấp file. Khác bản ca31186e ở ba điểm đo được: (1) OpenClaw chỉ còn 101 node — toàn bộ đến từ 16 migration sổ đóng băng + migration xoá 30/08, KHÔNG còn node code (bản trước 364); (2) đủ 735 file supabase/migrations hiện có (batch-70 tách 8 part, mỗi part một agent; edge `migrates` chỉ nối trong cùng lát nên còn thiếu liên kết xuyên lát — xem knownGaps); (3) 5.714 cạnh `imports` = importMap 5.713 + 1 rewire của assemble-reviewer, phủ 1.302 file có import. Tổng 7.864 node · 16.021 cạnh · 14 tầng · 18 bước tour.",
    "excludesFrom": ".ua/.understandignore",
    "exclusionsPendingNextRefresh": {
      "$note": "ĐÃ ÁP DỤNG ở bản này (02/09/2026). Giữ khoá để ghi lịch sử: nhóm vendor/runtime-state/data-dump khai 08/08 giờ có hiệu lực thật — filteredByIgnore = 9 (không phải ~1 như dự đoán, vì đến 02/09 trên đĩa có thêm artifact runtime bị mẫu bắt trúng — đúng mục đích của mẫu). Tổng file quét 3.042 → 3.091 là tăng ròng do repo lớn thêm 128+ commit, không phải mẫu loại ít đi."
    },
    "coverageEvidence": {
      "filesScanned": 3091,
      "fileLevelNodes": 3514,
      "filesWithFileLevelNode": 3091,
      "importEdges": 5714,
      "importEdgesExpectedFromImportMap": 5713,
      "importEdgeRecoveredByMergeScript": 0,
      "danglingEdgesAfterMerge": 0,
      "duplicateNodeIds": 0,
      "validatorIssues": 0,
      "validatorOrphanWarnings": 598,
      "assembleReviewerFixes": {
        "edgesRestored": 99,
        "nodesRecovered": 2
      }
    }
  },
  "toolVersion": "2.9.4",
  "toolVersionSource": "measured",
  "toolVersionNote": "ĐO ĐƯỢC 02/09/2026: trường `version` của C:/Users/Nguyen Tam/.understand-anything-plugin/package.json = 2.9.4 — cùng bản đã dựng ca31186e. Artifact trong .ua/ vẫn không tự ghi version (trường `version: 1.0.0` là SCHEMA version).",
  "configDigest": {
    ".ua/config.json": "git:bf23efcb9a6caea2bf2347cdb5cfb2d32e09af1a",
    ".ua/.understandignore": "git:9cdb86df3b342b5f6b88bfb0922c4bf3f10db23b"
  },
  "configDigestNote": "git blob OID, KHÔNG phải sha256 nội dung worktree. Trên Windows worktree có thể là CRLF trong khi blob là LF, nên sha256 worktree sẽ lệch một cách vô nghĩa và gate sẽ đỏ giả. Blob OID là thứ git thật sự lưu. Gate tính bằng `git hash-object <path>` trên WORKTREE (không phải `HEAD:<path>`), nên sửa file cấu hình mà quên cập nhật đây là đỏ NGAY, chưa cần commit. Đã kiểm chứng sống 08/08/2026: bật nhóm mẫu vendor/runtime-state/data-dump trong .understandignore ⇒ gate đỏ đúng chỗ, ghi rõ cả hai OID. `.ua/config.json` vẫn nguyên từ bản d0ffb045; `.ua/.understandignore` đổi ngày 08/08 (xem scope.exclusionsPendingNextRefresh).",
  "artifactVsRuntimeState": {
    "$note": "Trả lời câu hỏi của plan PHẦN V §20: file nào trong .ua/ là ARTIFACT CHIA SẺ (đáng commit, người khác dùng lại được) và file nào là TRẠNG THÁI CHẠY (dựng lại được, không nên commit). Đo 08/08/2026, không suy đoán.",
    "shareableArtifact": {
      "files": [
        "knowledge-graph.json",
        "meta.json",
        "config.json",
        ".understandignore"
      ],
      "vi": "Nội dung do LLM sinh ra từ mã nguồn tại một commit, không phụ thuộc máy dựng. Người khác clone về đọc được ngay và dùng đúng nghĩa. Đây là thứ đáng nằm trong git."
    },
    "runtimeState": {
      "files": [
        ".ua/intermediate/",
        ".ua/tmp/",
        ".ua/.trash-*/",
        ".gitnexus/**"
      ],
      "vi": "Dựng lại được từ mã nguồn, và LỚN. Đo: .gitnexus/ chiếm 508 MB với 0 file tracked (đúng — nó là parse cache, `graph:analyze` dựng lại toàn bộ); .ua/ chiếm 53 MB trên đĩa nhưng chỉ 5 file được commit."
    },
    "fingerprintsJson": {
      "phanLoai": "ARTIFACT ĐƯỢC COMMIT, NHƯNG KHÔNG DI ĐỘNG GIỮA HỆ ĐIỀU HÀNH",
      "doDuoc": "Trong 197 file đang là CRLF trên đĩa Windows: 127 file có contentHash khớp BYTES THÔ (kể cả \\r\\n), 5 khớp bản chuẩn hoá LF, 65 không khớp cái nào (đã đổi từ lúc dựng — graph cũ 38 commit lúc đo).",
      "heQua": "fingerprints.json băm bytes worktree, nên CÙNG MỘT COMMIT cho hash khác nhau giữa Windows (CRLF, core.autocrlf=true) và Linux (LF). CI dựng lại fingerprints từ cùng commit sẽ thấy gần như MỌI file 'đã đổi' và phân loại là STRUCTURAL — đúng cách hỏng mà Phase 7 của skill cảnh báo (leo thang FULL_UPDATE mỗi commit).",
      "quyetDinh": "VẪN COMMIT — gate:graph-hygiene đối chiếu baseCommit qua cả ba nguồn và cần file này; bỏ đi là mất một phép kiểm thật. Nhưng KHÔNG được coi nó là bằng chứng di động: đừng so fingerprints dựng trên máy khác OS với bản đã commit rồi kết luận mã đã đổi. Muốn biết mã đổi gì thì dùng git, không dùng file này.",
      "muonSuaTanGoc": "Chuẩn hoá CRLF→LF trước khi băm trong build-fingerprints.mjs (thuộc plugin ngoài repo), hoặc thêm `*.json text eol=lf` cho .ua/ vào .gitattributes. Cả hai đều là thay đổi ngoài phạm vi lát này."
    }
  },
  "knownGaps": [
    "Hàm export ngắn: vài batch (24–26, 50–54) vẫn đọc luật 'export → luôn tạo node' thành AND với ngưỡng 10 dòng nên bỏ node cho helper ngắn; từ nhóm g18 trở đi prompt đã nhắc lại luật OR. Hệ quả đo được: assemble-reviewer phải phục hồi 35 cạnh `calls` trỏ `function:src/integrations/supabase/client.ts:supabase` (singleton export bị 12 file mô hình như hàm) + 1 node `dispatch` của use-toast.",
    "Batch 24–26: summary cấp function sinh theo rule (verb + ngữ cảnh + params) thay vì đọc từng hàm — file-level summary vẫn đọc tay. Chất lượng function summary ở 3 batch này thấp hơn phần còn lại.",
    "Migration (batch-70, 8 lát × ~92 file): edge `migrates` chỉ nối khi file tạo bảng nằm CÙNG lát; ALTER trỏ bảng tạo ở lát khác không có edge. Lát 7 (20260731070000 mega-migration 4.079 dòng, 36 hàm) chỉ lấy ~12 RPC tiêu biểu. extract-structure.mjs đọc sai tên bảng (`public`/`app_private` thay vì tên thật) — mọi lát đã tự grep CREATE TABLE để sửa.",
    "Docs narrative (he-thong, plans): trần 20 edge `documents` tới file code mỗi doc, và bỏ tham chiếu tới supabase/migrations/*.sql (quan hệ migration↔doc chính xác hơn nằm ở docs/generated/schema-change-evidence, batch 83–87 nối 1:1). 57 ảnh .webp trong huong-dan-su-dung có node tối thiểu suy từ tên file, không đọc ảnh.",
    "importMap của công cụ vẫn bỏ sót `export … from`, `export default <Identifier>`, `React.lazy(() => import(...))`, import trong .mts/.vue — như bản trước; agent bù bằng `depends_on`.",
    "598 node cô lập (không cạnh) theo validator — chủ yếu document/ảnh/config lẻ và evidence json; bản trước cũng có nhóm này, không phải lỗi mới."
  ]
}
