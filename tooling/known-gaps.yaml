# Sổ đăng ký các khoảng trống ĐÃ BIẾT và cố ý chưa gating.
#
# Vì sao cần: một bước `continue-on-error: true` hay một suite chỉ chạy được trên
# Windows là quyết định hợp lý tại thời điểm đó — nhưng không có ngày hết hạn thì
# nó thành vĩnh viễn, và sau vài tháng không ai còn nhớ nó là quyết định hay là
# thứ bị bỏ quên. Mỗi mục dưới đây phải có ngày hết hạn; quá hạn thì
# scripts/check-known-gaps.mjs kêu, và cách xử là ĐÓNG nó hoặc GIA HẠN CÓ LÝ DO,
# không phải xoá dòng cho yên.
#
# Cố ý giữ schema tối thiểu: id / expires_at / why / exit_condition. Bỏ `owner`
# (repo một người) và `evidence` (optional) — thêm field chỉ làm người ta lười
# cập nhật.
#
# Kiểm: npm run gate:known-gaps

schemaVersion: 1
gaps:
  - id: supabase-cli-replay-broken
    where: .github/workflows/network-center-validation.yml (bước "Measure the historical Supabase replay")
    expires_at: 2026-11-30
    why: >
      Replay 640 file migration qua Supabase CLI từ database trắng KHÔNG thể thành công
      và chưa bao giờ thành công — bốn nguyên nhân độc lập ghi ở đầu
      scripts/network-center-disposable-db.mjs. Bước này giữ lại như phép ĐO
      (continue-on-error) chứ không phải gate; nửa gating là --local-cluster.
    exit_condition: >
      Đợt 3 dựng xong production baseline + forward-only lane. Khi đó môi trường mới
      dựng từ baseline chứ không replay lịch sử, và bước đo này hết lý do tồn tại.

  - id: cross-tenant-fixture-non-gating
    where: .github/workflows/network-center-validation.yml (nhánh `cross-tenant` của bước matrix)
    expires_at: 2026-11-30
    why: >
      Ma trận 20 ca xuyên-tenant dựng payload inventory dạng legacy-v1 mà đường tương thích
      hiện tại từ chối trên database vừa dựng mới. Bản thân schema ĐÚNG — mọi migration
      Network Center đều apply — nên cái thiếu là DỮ LIỆU FIXTURE, không phải tính chất bảo
      mật. Vì vậy bước này nuốt lỗi bằng `|| { echo "::warning::" }` và vẫn chạy để ghi nhận
      thay vì giấu đi.
      Quyết định này đã được giải thích trong comment ngay tại chỗ từ trước, nhưng CHƯA có
      trong sổ này — tức không có ngày hết hạn. Đúng thứ mà chính file này nói là cách biến
      một quyết định có thời hạn thành khoảng trống vĩnh viễn. Đăng ký 06/08/2026.
    exit_condition: >
      Dựng fixture inventory đúng định dạng hiện hành cho 20 ca, rồi bỏ `|| { echo ... }` để
      bước này gating thật.

  - id: deployment-assets-windows-only
    where: >
      infra/network-center-worker/test/deploymentAssets.test.ts và
      infra/network-center-worker/test/deploymentPowerShellReconciliation.test.ts
    expires_at: 2026-10-31
    why: >
      Các test shell ra powershell.exe vô điều kiện nên FAIL trên runner Linux. Đây là
      tín hiệu có chủ đích: suite deployment-asset chỉ kiểm được trên Windows, tức là
      không kiểm được trên chính nền tảng mà release chạy. Che nó đi bằng skip sẽ biến
      một khoảng trống nhìn thấy được thành khoảng trống vô hình.

      MỞ RỘNG 06/08/2026 — phạm vi lớn hơn nhiều so với "3 test" ghi ban đầu:
      (a) deploymentPowerShellReconciliation.test.ts dựng đường dẫn bằng
          `.pathname.slice(1)` ở TOP-LEVEL nên trên Windows cả file không load được và
          vitest báo "0 test" — 76 test biến mất không dấu vết. Đã sửa bằng fileURLToPath.
      (b) Trên Linux, `powershell.exe` không tồn tại ⇒ spawnSync trả `status = null`, mà
          phần lớn ca ở đó khẳng định `expect(status).not.toBe(0)` — `null !== 0` là ĐÚNG,
          nên chúng XANH mà chưa từng chạy PowerShell. Đã thêm guard ném lỗi khi
          spawn thất bại, để xanh-giả thành đỏ-thật.
      (c) Máy dev chỉ có Windows PowerShell 5.1; 29/76 test cần ngữ nghĩa PowerShell 7
          (chính header file đã lường trước nhưng resolvePowerShellEditions() vẫn nhét
          powershell.exe làm edition[0] vô điều kiện).
    exit_condition: >
      Port sang cross-platform, hoặc tách job Windows CÓ PowerShell 7 riêng trong CI để
      cả 76 test thực sự chạy. Đóng được khi suite xanh ở đâu đó, không phải khi nó im.

  - id: pitr-disabled-accepted-risk
    where: Supabase project tryymsxyyckgbrmmvozx
    expires_at: 2027-02-06
    why: >
      PITR TẮT và đây là lựa chọn có chủ đích (06/08/2026): add-on tính phí riêng, chủ dự án
      chọn phương án miễn phí. Chỉ có backup vật lý hằng ngày ⇒ RPO tối đa ~24 giờ trên dữ
      liệu tiền thật. Giảm nhẹ: scripts/backup-before-schema.mjs bắt buộc chạy trước mọi
      thao tác schema (đã diễn tập restore thật 06/08).
      PHẦN CHƯA ĐƯỢC CHE: sự cố đến từ code chạy hằng ngày — dump thủ công không giúp gì,
      đường lùi vẫn là 24 giờ. Đây là rủi ro ĐƯỢC CHẤP NHẬN, không phải đã giải quyết.
    exit_condition: >
      Bật PITR khi quy mô tiền/rủi ro đủ lớn để 100 USD/tháng là rẻ; hoặc rà lại sau 6 tháng
      xem RPO 24 giờ còn chấp nhận được không.

  - id: ci-gates-mot-job-thay-vi-ba
    where: .github/workflows/ci-gates.yml (job `quality-gates`)
    expires_at: 2026-12-31
    why: >
      Plan §31 đặt tên ba job `contract-gates` / `schema-gates` / `docs-freshness`. Repo có ba
      STEP mang đúng tên đó nhưng nằm trong MỘT job. Đây là lựa chọn có chủ ý, không phải bỏ sót.
      Plan §0.6 vốn muốn gộp 8 job thành 3 để "giảm bề mặt bảo trì và số nơi có thể kẹt merge";
      gộp về một job đi cùng hướng đó, chỉ xa hơn.
      Phần giá trị DUY NHẤT còn thiếu là tên JOB, và tên job chỉ dùng được khi bật required check
      — điều bất khả ở tier hiện tại: repo private trên GitHub Free, đã đo và ghi ở
      docs/generated/external-controls.json (branch protection không enforce được).
      Chia thành ba step CÓ TÊN lấy lại phần giá trị thực tế: giao diện PR chỉ ra ngay NHÓM nào
      đỏ thay vì một khối 19 lệnh phải mở log mới biết. Ba runner khởi động thì không lấy lại gì.
      Đăng ký 07/08/2026 để deliverable này không treo lơ lửng giữa "đã làm" và "chưa làm".
    exit_condition: >
      Repo chuyển sang tier bật được branch protection (GitHub Team/Pro, hoặc public). Khi đó tách
      ba step thành ba job và đặt chúng làm required check — lúc ấy tên job mới có tác dụng.

  - id: ua-graph-stale
    where: .ua/ (knowledge-graph.json, fingerprints.json, meta.json) — baseCommit d0ffb045, 29/07/2026
    expires_at: 2026-09-30
    why: >
      Graph UA committed một lần duy nhất rồi không refresh: nay CŨ 488 commit / 1.345 file
      lệch / thiếu 118 migration, và có 18 subsystem KHÔNG có node nào — gồm TRỌN VẸN
      Network Center lẫn OpenClaw, tức hai hệ phức tạp nhất repo lại là hai hệ graph mù
      hoàn toàn. Đăng ký 07/08/2026 vì luật #2 (chủ dự án bổ sung) đặt UA ở mức CẢNH BÁO
      mặc định, nên mức lệch này KHÔNG làm đỏ CI — đúng thiết kế, nhưng cảnh báo không có
      ngày hết hạn thì thành tiếng ồn nền rồi không ai đọc nữa.
      ĐÃ ĐƯỢC CHE: scripts/check-graph-freshness.mjs in đủ bốn con số mỗi lần chạy;
      `--nhiem-vu onboarding|architecture|domain-review|generated-docs` là HARD nên bốn
      loại việc thực sự dựa vào graph sẽ bị chặn thay vì đọc dữ liệu 488 commit trước.
      Trần hard-code trong script (200/500/30) bảo đảm không ai nới policy cho mức lệch
      hiện tại đi qua.
      CHƯA ĐƯỢC CHE: manifest ghi toolVersion 2.9.4 với toolVersionSource "attested" —
      artifact KHÔNG tự ghi version, con số đó lấy từ pin của plan §18 chứ không phải đọc
      ra từ graph. Nếu graph thật ra sinh bởi bản khác thì không cách nào biết cho tới lần
      refresh tới.
    exit_condition: >
      Refresh UA trong PR RIÊNG (luật #6 — không kèm vào PR tính năng), manifest mới ghi
      toolVersion đọc THẬT từ công cụ thay vì attested, và
      `npm run gate:graph-freshness -- --nhiem-vu architecture` exit 0.

  - id: contract-type-handwritten-drift
    where: src/hooks/useContracts.ts (2 chỗ `as unknown as ContractWithRelations`)
    expires_at: 2026-11-30
    why: >
      ContractWithRelations là kiểu VIẾT TAY, không khớp hình dạng thật DB trả: một số cột
      nullable ở DB được khai không-null, và quan hệ lồng biểu diễn khác. Ratchet any-cast đã
      về 0 (07/08/2026) và hai chỗ còn lại là khẳng định HẸP, khác hẳn `(supabase as any)` cũ:
      chuỗi select nay là hằng LITERAL nên tên bảng/cột được kiểm kiểu thật, chỉ hình dạng
      cuối cùng là lời khai. Phần chưa che: nếu DB đổi một cột thành nullable, hai chỗ này
      không đỏ.
    exit_condition: >
      Hoà giải ContractWithRelations với kiểu sinh từ types.ts — hoặc bỏ hẳn kiểu viết tay và
      dùng kiểu suy ra từ select. Là một lượt riêng vì đụng nhiều consumer của kiểu đó.

# ĐÃ ĐÓNG 06/08/2026 — claude-agents-md-still-full.
# CLAUDE.md 211 -> 62 dòng, AGENTS.md 146 -> 43, chỉ còn phần đặc thù agent
# (trailer commit, cách gọi công cụ browser) + pointer về Contract.
# Trước khi cắt đã đối chiếu CƠ HỌC từng chi tiết vận hành với Contract và phát
# hiện `deno.lock` chưa được nhắc ở đâu — nếu cắt trước rồi kiểm sau thì tri thức
# đó biến mất không dấu vết. Vì vậy MUST_MENTION của check-agent-contract nâng từ
# 16 lên 29 invariant, và đã đột biến để chắc nó bắt được.



# ĐÃ ĐÓNG 07/08/2026 — baseline-not-verified-on-supabase.
#
# Đã diễn tập thật trên một Supabase project trắng (PostgreSQL 17.6, cùng bản
# chính với production) do chủ dự án cấp. Kết quả cuối: bảng 439/439 ·
# view 14/14 · policy 1193/1193 · trigger 493/493.
#
# NHƯNG lần chạy ĐẦU TIÊN thất bại, và đó mới là giá trị của việc diễn tập:
#   → policy chỉ dựng được 922/1193 (mất 271, tức mất phần lớn hàng rào RLS)
#   → 249 lỗi 'role "…" does not exist'
# Nguyên nhân: pg_dump --schema-only KHÔNG dump role (role thuộc cấp cluster),
# nên baseline tham chiếu 7 role riêng của ứng dụng mà không tạo chúng.
#
# Ghi chú CŨ của mục này nói lỗi khi restore lên Postgres trần "đều do target
# thiếu role của Supabase — trên project Supabase thật thì chúng có sẵn". Vế
# đầu đúng, SUY LUẬN SAU SAI: các role thiếu là role của CHÍNH ỨNG DỤNG
# (openclaw_function_owner, ie_canonical_writer…), Supabase không có sẵn. Chỉ
# diễn tập thật mới phân biệt được hai điều đó.
#
# Và manifest luôn ghi đủ số đếm (439 bảng, 1193 policy) vì nó đếm trên FILE,
# không đếm trên KẾT QUẢ khôi phục — nên trên giấy baseline lúc nào cũng đầy đủ.
#
# ĐÃ SỬA: supabase/baseline/roles.sql (sinh bởi capture-schema-baseline.mjs),
# và quy trình khôi phục ba bước ghi ở supabase/baseline/README.md — roles.sql
# trước, rồi schema.sql HAI LƯỢT (lượt 2 dựng nốt object tham chiếu tiến, ví dụ
# public.rooms có cột sinh phụ thuộc public.room_sort_key).
# Chạy lại bất cứ lúc nào: node scripts/dien-tap-khoi-phuc-baseline.mjs --dich "…"

# ĐÃ ĐÓNG 06/08/2026 — buildingfilterselect-test-failing.
# Nguyên nhân: commit 5b14e9a0 thêm option `includeVirtual` nhưng không sửa test;
# hai test đó chỉ nói về cờ `enabled` lại so khớp TOÀN BỘ đối số nên vỡ theo.
# Đã tách theo mối quan tâm (objectContaining) + thêm 2 test phủ `includeVirtual`
# vốn ship không kèm test nào. 4/4 xanh, kiểm bằng đột biến hai chiều.
# Giữ lại ghi chú này thay vì xoá trắng: biết một gap ĐÃ từng tồn tại và đóng thế
# nào có ích hơn là để nó biến mất không dấu vết.
