diff --git a/index.ts b/index.ts
--- a/index.ts
+++ b/index.ts
@@ -4,5 +4,6 @@ import {
   defineBundledChannelEntry,
   loadBundledEntryExportSync,
 } from "openclaw/plugin-sdk/channel-entry-contract";
+import { registerPrivateOutboundRpc } from "./src/bridge/outbound-rpc.js";
 
 function createZalouserTool(context?: unknown): AnyAgentTool {
@@ -31,5 +32,6 @@ export default defineBundledChannelEntry({
   },
   registerFull(api) {
     api.registerTool((ctx) => createZalouserTool(ctx), { name: "zalouser" });
+    registerPrivateOutboundRpc(api, "zalouser.bridge.send");
   },
 });
diff --git a/src/send.ts b/src/send.ts
--- a/src/send.ts
+++ b/src/send.ts
@@ -1,3 +1,4 @@
 // Zalouser plugin module implements send behavior.
+import { requirePrivateRpcContext } from "./bridge/outbound-rpc.js";
 import { createZalouserSendReceipt } from "./send-receipt.js";
 import { parseZalouserTextStyles } from "./text-styles.js";
@@ -59,3 +60,8 @@ export async function sendMessageZalouser(
             textStyles: chunk.styles,
           };
+    requirePrivateRpcContext(
+      chunkOptions.mediaUrl
+        ? { kind: "media", url: chunkOptions.mediaUrl }
+        : { kind: "text", text: chunk.text },
+    );
     const result = await sendZaloTextMessage(threadId, chunk.text, chunkOptions);
@@ -91,5 +99,6 @@ export async function sendLinkZalouser(
   url: string,
   options: ZalouserSendOptions = {},
 ): Promise<ZalouserSendResult> {
+  requirePrivateRpcContext({ kind: "link", url });
   return await sendZaloLink(threadId, url, options);
 }
@@ -112,3 +121,4 @@ export async function sendReactionZalouser(params: {
   isGroup?: boolean;
 }): Promise<ZalouserSendResult> {
+  requirePrivateRpcContext({ kind: "reaction", emoji: params.emoji, providerMessageId: params.msgId });
   const result = await sendZaloReaction({
