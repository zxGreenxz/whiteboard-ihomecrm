diff --git a/src/zalo-js.ts b/src/zalo-js.ts
index f518a45..a266989 100644
--- a/src/zalo-js.ts
+++ b/src/zalo-js.ts
@@ -37,6 +37,7 @@ import type {
   PreparedProviderCallV1,
   ProviderSinkV1,
 } from "./bridge/send-context.js";
+import { resolveZaloEgressAgent } from "./bridge/egress-agent.js";
 import { normalizeZaloReactionIcon } from "./reaction.js";
 import { createZalouserSendReceipt } from "./send-receipt.js";
 import type {
@@ -1024,6 +1025,7 @@ async function ensureApi(
       throw new Error(`No saved Zalo session for profile "${profile}"`);
     }
     const zalo = await createZalo({
+      agent: resolveZaloEgressAgent(),
       logging: false,
       selfListen: false,
     });
@@ -1938,7 +1940,11 @@ export async function startZaloQrLogin(params: {
       let capturedCredentials: Omit<StoredZaloCredentials, "createdAt" | "lastUsedAt"> | null =
         null;
       try {
-        const zalo = await createZalo({ logging: false, selfListen: false });
+        const zalo = await createZalo({
+          agent: resolveZaloEgressAgent(),
+          logging: false,
+          selfListen: false,
+        });
         const api = await zalo.loginQR(undefined, (event: LoginQRCallbackEvent) => {
           const current = activeQrLogins.get(profile);
           if (!current || current.id !== login.id) {
diff --git a/src/zca-client.ts b/src/zca-client.ts
index 4686085..866ed66 100644
--- a/src/zca-client.ts
+++ b/src/zca-client.ts
@@ -242,7 +242,11 @@ export type API = {
   sendSeenEvent(messages: DeliveryEventMessages, type?: number): Promise<unknown>;
 };
 
-type ZaloCtor = new (options?: { logging?: boolean; selfListen?: boolean }) => {
+type ZaloCtor = new (options?: {
+  agent?: unknown;
+  logging?: boolean;
+  selfListen?: boolean;
+}) => {
   login(credentials: Credentials): Promise<API>;
   loginQR(
     options?: { userAgent?: string; language?: string; qrPath?: string },
