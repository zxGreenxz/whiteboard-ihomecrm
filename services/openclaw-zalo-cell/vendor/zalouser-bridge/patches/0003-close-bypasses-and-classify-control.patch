diff --git a/src/channel.adapters.ts b/src/channel.adapters.ts
index 89fb477..10b0989 100644
--- a/src/channel.adapters.ts
+++ b/src/channel.adapters.ts
@@ -50,6 +50,12 @@ const loadZalouserChannelRuntime = createLazyRuntimeModule(() => import("./chann
 
 const ZALOUSER_TEXT_CHUNK_LIMIT = 2000;
 
+function privateRpcRequired(): never {
+  throw Object.assign(new Error("business sends require the private bridge RPC"), {
+    code: "PRIVATE_RPC_REQUIRED",
+  });
+}
+
 type ZalouserSendTextContext = {
   to: string;
   text: string;
@@ -127,6 +133,7 @@ async function sendZalouserTextFromContext({
   cfg,
   onDeliveryResult,
 }: ZalouserSendTextContext) {
+  privateRpcRequired();
   const { sendMessageZalouser } = await loadZalouserChannelRuntime();
   const account = resolveZalouserAccountSync({ cfg, accountId });
   const target = parseZalouserOutboundTarget(to);
@@ -153,6 +160,7 @@ async function sendZalouserMediaFromContext({
   mediaReadFile,
   onDeliveryResult,
 }: ZalouserSendMediaContext) {
+  privateRpcRequired();
   const { sendMessageZalouser } = await loadZalouserChannelRuntime();
   const account = resolveZalouserAccountSync({ cfg, accountId });
   const target = parseZalouserOutboundTarget(to);
@@ -258,6 +266,7 @@ export const zalouserMessageActions: ChannelMessageActionAdapter = {
     if (action !== "react") {
       throw new Error(`Zalouser action ${action} not supported`);
     }
+    privateRpcRequired();
     const { sendReactionZalouser } = await loadZalouserChannelRuntime();
     const account = resolveZalouserAccountSync({ cfg, accountId });
     const threadId =
@@ -442,6 +451,7 @@ export const zalouserPairingTextAdapter = {
   message: "Your pairing request has been approved.",
   normalizeAllowEntry: createPairingPrefixStripper(/^(zalouser|zlu):/i),
   notify: async ({ cfg, id, message }: { cfg: OpenClawConfig; id: string; message: string }) => {
+    privateRpcRequired();
     const { sendMessageZalouser } = await loadZalouserChannelRuntime();
     const account = resolveZalouserAccountSync({ cfg });
     const authenticated = await checkZcaAuthenticated(account.profile);
diff --git a/src/monitor.ts b/src/monitor.ts
index 1a10ba4..b1eeccf 100644
--- a/src/monitor.ts
+++ b/src/monitor.ts
@@ -67,6 +67,12 @@ export type ZalouserMonitorResult = {
 
 const ZALOUSER_TEXT_LIMIT = 2000;
 
+function privateRpcRequired(): never {
+  throw Object.assign(new Error("business sends require the private bridge RPC"), {
+    code: "PRIVATE_RPC_REQUIRED",
+  });
+}
+
 function buildNameIndex<T>(items: T[], nameFn: (item: T) => string | undefined): Map<string, T[]> {
   const index = new Map<string, T[]>();
   for (const item of items) {
@@ -435,6 +441,7 @@ async function processMessage(
           logVerbose(core, runtime, `zalouser pairing request sender=${senderId}`);
         },
         sendPairingReply: async (text) => {
+          privateRpcRequired();
           await sendMessageZalouser(chatId, text, { profile: account.profile });
           statusSink?.({ lastOutboundAt: Date.now() });
         },
@@ -765,6 +772,7 @@ async function deliverZalouserReply(params: {
   accountId?: string;
   tableMode?: MarkdownTableMode;
 }): Promise<{ visibleReplySent: boolean }> {
+  privateRpcRequired();
   const { payload, profile, chatId, isGroup, runtime, core, config, accountId } = params;
   const tableMode = params.tableMode ?? "code";
   let visibleReplySent = false;
diff --git a/src/send.ts b/src/send.ts
index cbee547..ccdb950 100644
--- a/src/send.ts
+++ b/src/send.ts
@@ -1,6 +1,7 @@
 // Zalouser plugin module implements send behavior.
 import { createHash } from "node:crypto";
 import { loadOutboundMediaFromUrl } from "openclaw/plugin-sdk/outbound-media";
+import { classifyControlTraffic } from "./bridge/control-traffic.js";
 import { assertAuthorizedProviderCall } from "./bridge/outbound-rpc.js";
 import type {
   MediaBusinessFrameV1,
@@ -89,7 +90,19 @@ export async function sendTypingZalouser(
   threadId: string,
   options: Pick<ZalouserSendOptions, "profile" | "isGroup"> = {},
 ): Promise<void> {
-  await sendZaloTypingEvent(threadId, options);
+  const frame = classifyControlTraffic({
+    version: 1,
+    kind: "typing",
+    sink: {
+      accountProfile: options.profile?.trim() || "default",
+      conversationId: threadId,
+      isGroup: options.isGroup === true,
+    },
+  });
+  await sendZaloTypingEvent(frame.sink.conversationId, {
+    profile: frame.sink.accountProfile,
+    isGroup: frame.sink.isGroup,
+  });
 }
 
 export async function sendReactionZalouser(params: {
@@ -159,7 +172,23 @@ export async function sendDeliveredZalouser(params: {
   message: ZaloEventMessage;
   isSeen?: boolean;
 }): Promise<void> {
-  await sendZaloDeliveredEvent(params);
+  const frame = classifyControlTraffic({
+    version: 1,
+    kind: "delivery-receipt",
+    sink: {
+      accountProfile: params.profile?.trim() || "default",
+      conversationId: params.isGroup ? params.message.idTo : params.message.uidFrom,
+      isGroup: params.isGroup === true,
+    },
+    message: params.message,
+    isSeen: params.isSeen === true,
+  });
+  await sendZaloDeliveredEvent({
+    profile: frame.sink.accountProfile,
+    isGroup: frame.sink.isGroup,
+    message: frame.message,
+    isSeen: frame.isSeen,
+  });
 }
 
 export async function sendSeenZalouser(params: {
@@ -167,5 +196,19 @@ export async function sendSeenZalouser(params: {
   isGroup?: boolean;
   message: ZaloEventMessage;
 }): Promise<void> {
-  await sendZaloSeenEvent(params);
+  const frame = classifyControlTraffic({
+    version: 1,
+    kind: "seen",
+    sink: {
+      accountProfile: params.profile?.trim() || "default",
+      conversationId: params.isGroup ? params.message.idTo : params.message.uidFrom,
+      isGroup: params.isGroup === true,
+    },
+    message: params.message,
+  });
+  await sendZaloSeenEvent({
+    profile: frame.sink.accountProfile,
+    isGroup: frame.sink.isGroup,
+    message: frame.message,
+  });
 }
diff --git a/src/tool.ts b/src/tool.ts
index 1fc1763..e60cab4 100644
--- a/src/tool.ts
+++ b/src/tool.ts
@@ -13,6 +13,12 @@ import {
   listZaloGroupsMatching,
 } from "./zalo-js.js";
 
+function privateRpcRequired(): never {
+  throw Object.assign(new Error("business sends require the private bridge RPC"), {
+    code: "PRIVATE_RPC_REQUIRED",
+  });
+}
+
 const ACTIONS = ["send", "image", "link", "friends", "groups", "me", "status"] as const;
 
 const ZalouserToolSchema = Type.Object(
@@ -89,6 +95,7 @@ export async function executeZalouserTool(
   try {
     switch (params.action) {
       case "send": {
+        privateRpcRequired();
         const target = resolveZalouserSendTarget(params, context);
         if (!target.threadId || !params.message) {
           throw new Error("threadId and message required for send action");
@@ -104,6 +111,7 @@ export async function executeZalouserTool(
       }
 
       case "image": {
+        privateRpcRequired();
         const target = resolveZalouserSendTarget(params, context);
         if (!target.threadId) {
           throw new Error("threadId required for image action");
@@ -123,6 +131,7 @@ export async function executeZalouserTool(
       }
 
       case "link": {
+        privateRpcRequired();
         const target = resolveZalouserSendTarget(params, context);
         if (!target.threadId || !params.url) {
           throw new Error("threadId and url required for link action");
