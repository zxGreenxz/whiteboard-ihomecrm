# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local crawl artifacts (contains auth cookies + scraped data)
crawl-resident/
# Playwright MCP session artifacts (snapshots, console logs)
.playwright-mcp/
.claude/scheduled_tasks.lock

# Supabase service role key (DO NOT COMMIT)
.env.local

# Claude personal config (creds, PAT) — DO NOT COMMIT
CLAUDE.local.md
.claude/settings.local.json

# Excel data dumps
data excel/
dataexcel/

# Playwright MCP outputs
.playwright-mcp/

# Local scratch + RBAC refactor backups (KHÔNG commit - chứa data dump)
.scratch/
.backups/

# Draft — file dư thừa gom lại để review trước khi xoá (KHÔNG commit)
draft/

# E2E parallel browser fleet: COMMIT config + specs (đường test mặc định, xem
# CLAUDE.md/AGENTS.md); chỉ bỏ qua thư mục sinh ra khi chạy. Mật khẩu tài khoản
# test KHÔNG nằm trong repo — đọc từ env FLEET_PASS_* (giá trị ở CLAUDE.local.md).
.e2e-fleet/node_modules/
.e2e-fleet/test-results/
.e2e-fleet/playwright-report/
supabase/.temp/
test-results/

# Bản sao repo do hạm đội subagent tạo (harness tự sinh) — KHÔNG bao giờ commit
.claude/worktrees/

# OpenClaw Zalo local secrets, generated runtime state, and test artifacts
infra/openclaw-zalo/.env
infra/openclaw-zalo/secrets/
infra/openclaw-zalo/rendered/
infra/openclaw-media-gateway/.dev.vars
infra/openclaw-media-gateway/.wrangler/
services/openclaw-zalo-bridge/.data/
services/openclaw-zalo-bridge/coverage/
services/openclaw-zalo-cell/.state/
services/openclaw-zalo-cell/.release/
services/openclaw-zalo-cell/vendor/zalouser-bridge/.work/
